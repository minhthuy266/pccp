# Playground

Code thử nghiệm và ghi chú cá nhân nằm ở đây. Nội dung trong thư mục này không phải template canonical và có thể giả định precondition riêng cho từng ví dụ.

Với `BASIC.js`, hãy đọc ví dụ gọi hàm để biết input đã được sắp xếp hay chưa; các đoạn `console.log` chỉ phục vụ thử tay.
