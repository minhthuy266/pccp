# OF033 — Đường đến trường

- Nguồn đề: [Programmers lesson 42898](https://school.programmers.co.kr/learn/courses/30/lessons/42898?language=javascript)
- Pattern chính: grid path-count DP + obstacle
- Mức trong bank: CORE / representative

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Grid rộng `m`, cao `n`; từ góc trên trái tới góc dưới phải, chỉ đi phải hoặc xuống. Một số tọa độ `[x,y]` là puddle không đi được. Đếm số đường modulo `1,000,000,007`.

## 02. Bound

Số path combinatorial; enumerate không được. Mỗi cell nhận path từ top và left, DP `O(mn)`.

## 03. Brute force

DFS hai nhánh cho tới destination, dừng ở boundary/puddle. Nhiều route cùng tới một cell rồi giải lại suffix.

## 04. Bottleneck

Số cách tới cell chỉ phụ thuộc top/left và puddle status. Đây là optimal substructure/counting recurrence trực tiếp.

## 05. Pattern

```text
dp[0][0]=1
puddle => dp=0
otherwise dp[r][c]=(dp[r-1][c]+dp[r][c-1]) mod M
```

## 06. State

`dp[row][column]` là số route hợp lệ từ start tới cell đó. `blocked` Set dùng key row,column sau khi đổi input `[x,y]`.

## 07. Transition

Iterate row-major vì top và left đã được tính. Puddle được set 0 và skip. Boundary thiếu parent đóng góp 0.

## 08. Invariant

Trước khi tính cell `(r,c)`, mọi cell top/left cần cho transition đã đúng. Mỗi path tới current có bước cuối duy nhất từ top hoặc left, hai tập không giao, nên cộng count.

## 09. Complexity

Time `O(mn+p)`, space `O(mn)`. Mỗi sum dưới khoảng `2*MOD`, an toàn trong Number trước modulo.

## 10. Pseudocode

```text
blocked=Set puddles converted y-1,x-1
dp[0][0]=1
for rows then columns:
  skip start
  if blocked: dp=0
  else dp=(top+left)%MOD
return destination
```

## 11. Code bước 1 — dựng state

```js
function blockedCells(puddles) {
  return new Set(puddles.map(([x,y])=>`${y-1},${x-1}`));
}
```

Input là x,y; array là row=y, column=x.

## 12. Code bước 2 — viết transition

```js
function pathTransition(top,left) {
  return (top+left)%1_000_000_007;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function cellKey(row,column) {
  return `${row},${column}`;
}
```

Separator tránh collision như `(1,23)` và `(12,3)`.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF033.js](../../../solutions/official/OF033.js).

```js
const ROUTE_MODULO = 1_000_000_007;

function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;

  for (let row = 0; row < height; row++) {
    for (let column = 0; column < width; column++) {
      if (row === 0 && column === 0) continue;
      if (blocked.has(`${row},${column}`)) {
        dp[row][column] = 0;
        continue;
      }
      const fromTop = row > 0 ? dp[row - 1][column] : 0;
      const fromLeft = column > 0 ? dp[row][column - 1] : 0;
      dp[row][column] = (fromTop + fromLeft) % ROUTE_MODULO;
    }
  }

  return dp[height - 1][width - 1];
}
```

## 15. Dry-run

Grid 2×2 không puddle: start 1; cell phải 1; cell dưới 1; destination nhận `1+1=2`. Nếu puddle ở cell phải, destination chỉ nhận từ top/below path còn lại nên 1.

## 16. Edge cases

1. Một hàng hoặc một cột.
2. Puddle chặn route duy nhất.
3. Tọa độ x,y dễ đảo.
4. Destination nhận count đúng modulo.

## 17. Lỗi JavaScript thường gặp

- Dùng `[x][y]` thay `[row=y][column=x]`.
- Khởi tạo toàn first row/column là 1 bất chấp puddle.
- Quên modulo mỗi transition.
- Dùng `.fill(sameArray)` tạo alias rows.
- Overwrite start bằng transition zero.

## 18. Bài transfer cùng pattern

- OF032: max-sum DP, transition dùng max thay sum-count.
- OF060: 2D difference/prefix, iteration dependency khác.
- OF034: interval DP hai chiều state range.

Tín hiệu: đếm path monotone trên grid ⇒ dp cell bằng tổng predecessor, obstacle đặt zero.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF033 Đường đến trường

Phần này dùng chính contract, dry run, Blueprint và code executable của **Đường đến trường** để dựng lại lời giải; pattern kiểm soát là `Grid-DP`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Grid rộng `m`, cao `n`; từ góc trên trái tới góc dưới phải, chỉ đi phải hoặc xuống. Một số tọa độ `[x,y]` là puddle không đi được. Đếm số đường modulo `1,000,000,007`.

**Giới hạn quyết định implementation:** Số path combinatorial; enumerate không được. Mỗi cell nhận path từ top và left, DP `O(mn)`.

### Concrete dry run

**Concrete input:** `1+1=2`

Grid 2×2 không puddle: start 1; cell phải 1; cell dưới 1; destination nhận `1+1=2`. Nếu puddle ở cell phải, destination chỉ nhận từ top/below path còn lại nên 1.



Decision ledger của **Đường đến trường** nối `blocked Set và dp matrix.` với đúng block code; mỗi inner action trong input `1+1=2` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `1+1=2` | Chưa có state của Đường đến trường | Contract và input đã hợp lệ | dp[0][0]=1 nếu start không blocked; cell khác 0. | blocked Set và dp matrix. | Chưa chốt output |
| cell [row,column]. đi vào | fromTop và fromLeft, mặc định 0 ngoài biên. | Với `1+1=2`, hỏi: start cell thì continue; blocked thì giữ 0 và continue. | puddle bỏ transition; cell thường nhận top+left.; column tăng rồi row tăng để dependencies đã tính. | blocked Set và dp matrix. sau branch vừa chọn | số đường từ start tới đích modulo MOD. chỉ đổi ở branch ghi answer |
| Transition của current | blocked Set và dp matrix. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | dp[row][column] = (fromTop + fromLeft) % MOD. | blocked Set và dp matrix. đã commit current; invariant của Đường đến trường được phục hồi | số đường từ start tới đích modulo MOD. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | blocked cell không carry giá trị cũ; modulo áp ngay mỗi update.; hết matrix return dp[height-1][width-1]. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Đường đến trường**, DP state/cell hiện tại đi vào → đọc `fromTop và fromLeft, mặc định 0 ngoài biên.` từ state đang sống → hỏi đúng câu `start cell thì continue; blocked thì giữ 0 và continue.` → đi nhánh `puddle bỏ transition; cell thường nhận top+left.` → thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.` → chuyển sang state kế chỉ sau khi mọi dependency của state hiện tại đã sẵn sàng → chỉ chốt `số đường từ start tới đích modulo MOD.` ở vị trí mà invariant đã đúng → blocked cell không carry giá trị cũ; modulo áp ngay mỗi update. → hết matrix return dp[height-1][width-1]..

Với **Đường đến trường**, chuỗi “đọc cell [row,column]. → hỏi start cell thì continue; blocked thì giữ 0 và continue. → đổi dp[row][column] = (fromTop + fromLeft) % MOD. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Đường đến trường | `blocked Set và dp matrix.` | State chung mà mọi current đọc hoặc mutate | `dp[0][0]=1 nếu start không blocked; cell khác 0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `cell [row,column].` | Current của đúng iteration/candidate hiện tại | Do `row-major qua mọi cell.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `fromTop và fromLeft, mặc định 0 ngoài biên.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số đường từ start tới đích modulo MOD.` | Contract output của Đường đến trường | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const blocked = new Set(puddles.map(([x, y]) => '${y - 1},${x - 1}'));`; lựa chọn này giữ đúng thông tin mà check `start cell thì continue; blocked thì giữ 0 và continue.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `row-major qua mọi cell.`. Đây là lý do code dùng block bắt đầu bởi `for (let row = 0; row < height; row++) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “start cell thì continue; blocked thì giữ 0 và continue.”. Trong dry run `1+1=2`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (row === 0 && column === 0) continue;`.
- **Tiến triển:** column tăng rồi row tăng để dependencies đã tính.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `dp[row][column] = (fromTop + fromLeft) % MOD.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (let row = 0; row < height; row++) {`.
- **Cleanup:** blocked cell không carry giá trị cũ; modulo áp ngay mỗi update.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Đường đến trường:** Base state phải khớp định nghĩa DP; thứ tự loop phải bảo đảm dependency đã tính. Branch không hợp lệ không được ghi đè sentinel hoặc state tốt hơn đã có.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Đường đến trường | blocked Set và dp matrix. | `const blocked = new Set(puddles.map(([x, y]) => '${y - 1},${x - 1}'));` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | row-major qua mọi cell. | `for (let row = 0; row < height; row++) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | start cell thì continue; blocked thì giữ 0 và continue. | `if (row === 0 && column === 0) continue;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | dp[row][column] = (fromTop + fromLeft) % MOD. | `for (let row = 0; row < height; row++) {` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | column tăng rồi row tăng để dependencies đã tính. | `for (let row = 0; row < height; row++) {` | Sau read/update theo invariant của Đường đến trường |
| Hoàn tất và trả contract output | blocked cell không carry giá trị cũ; modulo áp ngay mỗi update. | `return dp[height - 1][width - 1];` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Đường đến trường** là **toàn bộ `schoolRouteCount` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Đường đến trường:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function schoolRouteCount(width, height, puddles) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Đường đến trường:** Những dữ kiện nào trong `blocked Set và dp matrix.` phải còn nguyên khi current kế tiếp tới? Block `const blocked = new Set(puddles.map(([x, y]) => '${y - 1},${x - 1}'));` chạy đúng một lần trước traversal. Nếu đặt nó trong `row-major qua mọi cell.`, state nào sẽ bị reset?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Đường đến trường:** Những dữ kiện nào trong `blocked Set và dp matrix.` phải còn nguyên khi current kế tiếp tới? Block `const dp = Array.from({ length: height }, () => new Uint32Array(width));` chạy đúng một lần trước traversal. Nếu đặt nó trong `row-major qua mọi cell.`, state nào sẽ bị reset?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
}
```

**Bước 4 — transition và movement**

> **Prediction checkpoint — transition của Đường đến trường:** Nhìn các brace đang mở để quyết định `dp[0][0] = 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.`, và movement `column tăng rồi row tăng để dependencies đã tính.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
}
```

**Bước 5 — main loop và scope**

> **Prediction checkpoint — loop của Đường đến trường:** Header `for (let row = 0; row < height; row++) {` lặp per item, per candidate hay tới khi state rỗng? Current `cell [row,column].` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
  for (let row = 0; row < height; row++) {
  }
}
```

**Bước 6 — condition/inner branch**

> **Prediction checkpoint — condition của Đường đến trường:** Trước khi chạy `for (let column = 0; column < width; column++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `start cell thì continue; blocked thì giữ 0 và continue.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
  for (let row = 0; row < height; row++) {
    for (let column = 0; column < width; column++) {
    }
  }
}
```

**Bước 7 — transition và movement**

> **Prediction checkpoint — transition của Đường đến trường:** Nhìn các brace đang mở để quyết định `for (let column = 0; column < width; column++) {`, `if (row === 0 && column === 0) continue;`, `if (blocked.has('${row},${column}')) {`, `dp[row][column] = 0;`, `continue;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.`, và movement `column tăng rồi row tăng để dependencies đã tính.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
  for (let row = 0; row < height; row++) {
    for (let column = 0; column < width; column++) {
      if (row === 0 && column === 0) continue;
      if (blocked.has(`${row},${column}`)) {
        dp[row][column] = 0;
        continue;
      }
      const fromTop = row > 0 ? dp[row - 1][column] : 0;
      const fromLeft = column > 0 ? dp[row][column - 1] : 0;
      dp[row][column] = (fromTop + fromLeft) % ROUTE_MODULO;
    }
  }
}
```

**Bước 8 — transition và movement**

> **Prediction checkpoint — transition của Đường đến trường:** Nhìn các brace đang mở để quyết định `for (let row = 0; row < height; row++) {`, `for (let column = 0; column < width; column++) {`, `if (row === 0 && column === 0) continue;`, `if (blocked.has('${row},${column}')) {`, `dp[row][column] = 0;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.`, và movement `column tăng rồi row tăng để dependencies đã tính.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
  for (let row = 0; row < height; row++) {
    for (let column = 0; column < width; column++) {
      if (row === 0 && column === 0) continue;
      if (blocked.has(`${row},${column}`)) {
        dp[row][column] = 0;
        continue;
      }
      const fromTop = row > 0 ? dp[row - 1][column] : 0;
      const fromLeft = column > 0 ? dp[row][column - 1] : 0;
      dp[row][column] = (fromTop + fromLeft) % ROUTE_MODULO;
    }
  }
}
```

**Bước 9 — cleanup/return**

> **Prediction checkpoint — finalization của Đường đến trường:** Dòng `return dp[height - 1][width - 1];` chạy per current hay đúng một lần sau khi `row-major qua mọi cell.` kết thúc? Trước return, cleanup `blocked cell không carry giá trị cũ; modulo áp ngay mỗi update.` và output `số đường từ start tới đích modulo MOD.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function schoolRouteCount(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1},${x - 1}`));
  const dp = Array.from({ length: height }, () => new Uint32Array(width));
  dp[0][0] = 1;
  for (let row = 0; row < height; row++) {
    for (let column = 0; column < width; column++) {
      if (row === 0 && column === 0) continue;
      if (blocked.has(`${row},${column}`)) {
        dp[row][column] = 0;
        continue;
      }
      const fromTop = row > 0 ? dp[row - 1][column] : 0;
      const fromLeft = column > 0 ? dp[row][column - 1] : 0;
      dp[row][column] = (fromTop + fromLeft) % ROUTE_MODULO;
    }
  }
  return dp[height - 1][width - 1];
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const blocked = new Set(puddles.map(([x, y]) => '${y - 1},${x - 1}'));` | Trước traversal trong main function | blocked Set và dp matrix. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Đường đến trường |
| `cell [row,column].` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (row === 0 && column === 0) continue;` | Sau khi state guard/current đã sẵn | Condition đọc fromTop và fromLeft, mặc định 0 ngoài biên. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `for (let row = 0; row < height; row++) {` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi start cell thì continue; blocked thì giữ 0 và continue. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (let row = 0; row < height; row++) {` | column tăng rồi row tăng để dependencies đã tính. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: blocked cell không carry giá trị cũ; modulo áp ngay mỗi update. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return dp[height - 1][width - 1];` | Sau finalization của main function | Output phải đạt số đường từ start tới đích modulo MOD. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Đường đến trường** dùng `1+1=2`: dừng ngay khi `blocked Set và dp matrix.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `schoolRouteCount` với `1+1=2`; theo dõi đúng output `số đường từ start tới đích modulo MOD.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | dp[0][0]=1 nếu start không blocked; cell khác 0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | blocked Set và dp matrix. | cell [row,column]. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | fromTop và fromLeft, mặc định 0 ngoài biên. | dp[row][column] = (fromTop + fromLeft) % MOD. | Đổi đúng như từng dòng dry run gốc | Condition start cell thì continue; blocked thì giữ 0 và continue. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | column tăng rồi row tăng để dependencies đã tính. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | blocked cell không carry giá trị cũ; modulo áp ngay mỗi update. | hết matrix return dp[height-1][width-1]. | số đường từ start tới đích modulo MOD. | Không còn transition bắt buộc nào chưa chạy |

Trong **Đường đến trường**, chỉ dòng thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.` được đổi output; state còn pending theo `blocked Set và dp matrix.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng '[x][y]' thay '[row=y][column=x]'. | `R,D` | Lượt đầu của `cell [row,column].` mà `start cell thì continue; blocked thì giữ 0 và continue.` quyết định branch | Dùng '[x][y]' thay '[row=y][column=x]'. | `for (let row = 0; row < height; row++) {` ở đúng scope |
| Khởi tạo toàn first row/column là 1 bất chấp puddle. | `D,R` | Ngay transition đầu tiên thực hiện `dp[row][column] = (fromTop + fromLeft) % MOD.` trên boundary/duplicate của input | Khởi tạo toàn first row/column là 1 bất chấp puddle. | `for (let row = 0; row < height; row++) {` với init/strictness của Đường đến trường |
| Quên modulo mỗi transition. | `[2,1]` | Lượt đầu movement `column tăng rồi row tăng để dependencies đã tính.` xảy ra trước/sau sai assignment | Quên modulo mỗi transition. | `for (let row = 0; row < height; row++) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(mn+p)`, space `O(mn)`. Mỗi sum dưới khoảng `2*MOD`, an toàn trong Number trước modulo.

**Proof từ operation của Đường đến trường:** Đếm số DP state được tính và số transition đọc cho mỗi state. Mỗi state chỉ được chốt theo recurrence một lần; tổng thời gian bằng số state nhân số lựa chọn/dependency thực sự xét. Main loop được mô tả là `row-major qua mọi cell.`; movement là `column tăng rồi row tăng để dependencies đã tính.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `blocked Set và dp matrix.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Đường đến trường; test `1+1=2` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `blocked Set và dp matrix.`, condition `start cell thì continue; blocked thì giữ 0 và continue.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Đường đến trường. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Grid 2×2 không puddle có hai đường `R,D` và `D,R`. Nếu puddle tại `[2,1]` (x=2,y=1), nhánh đi
phải bị chặn và chỉ còn một đường. Tọa độ input phải đổi thành row 0, column 1.

```js
function schoolRouteCountBrute(width, height, puddles) {
  const blocked = new Set(puddles.map(([x, y]) => `${y - 1}:${x - 1}`));

  function dfs(row, column) {
    if (row >= height || column >= width) return 0;
    if (blocked.has(`${row}:${column}`)) return 0;
    if (row === height - 1 && column === width - 1) return 1;
    return dfs(row + 1, column) + dfs(row, column + 1);
  }

  return dfs(0, 0) % 1_000_000_007;
}
```

Brute recursion tính lại số cách từ cùng cell; DP row-major gộp mọi prefix tại cell đó.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `blocked` | cell không đi được theo key row,column | đổi từng `[x,y]` sang zero-based |
| `dp[row][column]` | số path từ start tới cell | toàn 0, start bằng 1 |
| `fromTop`, `fromLeft` | hai tập path có bước cuối khác nhau | 0 nếu ngoài boundary |

#### 21. Blueprint từ đề đến code

1. Chuyển tọa độ `[x,y]` thành `[row=y-1,column=x-1]` ngay khi dựng blocked.
2. State là số cách tới cell; start có một empty path.
3. Duyệt row-major để top và left đã sẵn sàng.
4. Puddle giữ count 0 và skip transition.
5. Cell thường nhận tổng top + left, modulo ngay mỗi bước.

#### 22. Invariant, lỗi lộ diện và recall

Trước mỗi cell row-major, hai predecessor của nó đã đúng. Mọi route tới cell có bước cuối duy nhất
từ top hoặc left, nên hai tập rời nhau và được cộng. Puddle không có route hợp lệ nên state bằng 0.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| đảo x/y | grid 3×2, puddle `[2,1]` | 1 |
| fill first row/column bằng 1 | puddle chặn corridor | các cell sau bằng 0 |
| overwrite start | grid 1×1 | 1 |
| alias rows | update một cell đổi nhiều row | rows độc lập |

Recall: **count monotone grid = top + left; obstacle bằng 0; normalize x/y trước DP.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số đường từ start tới đích modulo MOD.
PREPARE: đổi puddle [x,y] thành key row=y-1,column=x-1.
GLOBAL STATE: blocked Set và dp matrix.
INIT: dp[0][0]=1 nếu start không blocked; cell khác 0.
MAIN LOOP: row-major qua mọi cell.
CURRENT ITEM: cell [row,column].
PER-ITERATION STATE: fromTop và fromLeft, mặc định 0 ngoài biên.
CHECK: start cell thì continue; blocked thì giữ 0 và continue.
BRANCH: puddle bỏ transition; cell thường nhận top+left.
UPDATE: dp[row][column] = (fromTop + fromLeft) % MOD.
POINTER MOVEMENT: column tăng rồi row tăng để dependencies đã tính.
STOP / RETURN: hết matrix return dp[height-1][width-1].
CLEANUP: blocked cell không carry giá trị cũ; modulo áp ngay mỗi update.
```

- Recall 1: Normalize x/y; seed start một cách; row-major cộng top+left, puddle luôn bằng zero.
- Recall 2: STATE blocked/dp → LOOP row-major → CHECK start/puddle → UPDATE modulo sum → RETURN destination.
- Recall 3: Điền puddle key `${'${'}y - ___},${'${'}x - ___}` và transition `(top + left) % ___`.
<!-- dry-run-reconstruction:end -->
