# OF039 — Chuyển đổi từ

- Nguồn đề: [Programmers lesson 43163](https://school.programmers.co.kr/learn/courses/30/lessons/43163?language=javascript)
- Pattern chính: BFS trên implicit graph của string
- Mức trong bank: CORE / transfer check

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Từ `begin`, mỗi bước đổi đúng một ký tự và từ mới phải thuộc `words`. Tìm số bước ít nhất để đạt `target`; không thể trả `0`. Mọi từ có cùng độ dài.

## 02. Bound

Mỗi word là node; hai word nối cạnh nếu khác đúng một vị trí. Không cần dựng trước toàn bộ graph: khi dequeue một word, scan words để sinh neighbor. `O(N²L)` phù hợp.

## 03. Brute force

DFS mọi chuỗi biến đổi không lặp rồi lấy path ngắn nhất. Số path tăng mạnh và DFS không đảm bảo path đầu là shortest.

## 04. Bottleneck

Mọi transition cost một bước; lần đầu BFS discover word là minimum distance. Nhiều path hội tụ cùng word nên visited loại công việc tương lai trùng nhau.

## 05. Pattern

Implicit BFS: queue `{word,steps}`, với mỗi word chưa visited kiểm Hamming distance đúng 1; mark khi enqueue.

## 06. State

- queue word + steps, head;
- `visited[index]` theo words;
- helper `differsByOneCharacter`.

## 07. Transition

Neighbor hợp lệ nếu chưa visited và khác current đúng một ký tự. Gán visited, enqueue steps+1. Nếu target không có trong words, return 0 sớm.

## 08. Invariant

Queue có steps không giảm. Khi word được mark lần đầu, steps của nó là shortest từ begin. Helper chỉ tạo cạnh đúng contract, không cho zero hoặc hơn một difference.

## 09. Complexity

Mỗi node dequeue tối đa một lần, mỗi lần scan N word và so L ký tự: `O(N²L)` time, `O(N)` space.

## 10. Pseudocode

```text
if target absent return 0
enqueue begin distance 0
while queue:
  pop word
  if target return steps
  for each unseen candidate:
    if differs exactly one: mark; enqueue steps+1
return 0
```

## 11. Code bước 1 — dựng state

```js
function differenceCount(a,b) {
  let count=0;
  for (let i=0;i<a.length;i++) if (a[i]!==b[i]) count++;
  return count;
}
```

## 12. Code bước 2 — viết transition

```js
function differsByOneCharacter(a,b) {
  let differences=0;
  for (let i=0;i<a.length;i++) {
    if (a[i] !== b[i]) {
      differences += 1;
      if (differences > 1) return false;
    }
  }
  return differences===1;
}
```

Cutoff sớm khi quá một difference.

## 13. Code bước 3 — ghép vòng lặp

```js
function enqueueWord(queue,visited,index,word,steps) {
  visited[index]=true;
  queue.push({word,steps:steps+1});
}
```

Mark trước enqueue.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF039.js](../../../solutions/official/OF039.js).

```js
function differsByOneCharacter(first, second) {
  let differences = 0;
  for (let index = 0; index < first.length; index++) {
    if (first[index] !== second[index]) {
      differences += 1;
      if (differences > 1) {
        return false;
      }
    }
  }
  return differences === 1;
}

function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;

  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;

  while (head < queue.length) {
    const { word, steps } = queue[head];
    head += 1;
    if (word === target) {
      return steps;
    }

    for (let index = 0; index < words.length; index++) {
      if (visited[index] || !differsByOneCharacter(word, words[index])) continue;
      visited[index] = true;
      queue.push({ word: words[index], steps: steps + 1 });
    }
  }

  return 0;
}
```

## 15. Dry-run

`hit→hot→dot→dog→cog` có 4 edge. BFS có thể đồng thời khám phá nhánh `hot→lot`, nhưng target lần đầu dequeue ở layer 4 vẫn shortest.

## 16. Edge cases

1. Target không trong list.
2. Candidate giống hệt current: không phải một change.
3. Nhiều shortest paths.
4. Có word unreachable.

## 17. Lỗi JavaScript thường gặp

- Chấp nhận difference `<=1`.
- DFS và return path đầu.
- Mark lúc dequeue.
- Dùng Set value khi words có thể cần identity; bài này unique nhưng index đơn giản.
- Return `-1` thay vì 0.

## 18. Bài transfer cùng pattern

- OF038: neighbor grid trực tiếp.
- OF056: neighbor số generate bằng phép toán.
- OF045: graph đã cho adjacency list.

Tín hiệu: node là object/string và cạnh được xác định bằng rule ⇒ implicit graph; shortest unweighted vẫn là BFS.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF039 Chuyển đổi từ

Phần này dùng chính contract, dry run, Blueprint và code executable của **Chuyển đổi từ** để dựng lại lời giải; pattern kiểm soát là `BFS-implicit-graph`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Từ `begin`, mỗi bước đổi đúng một ký tự và từ mới phải thuộc `words`. Tìm số bước ít nhất để đạt `target`; không thể trả `0`. Mọi từ có cùng độ dài.

**Giới hạn quyết định implementation:** Mỗi word là node; hai word nối cạnh nếu khác đúng một vị trí. Không cần dựng trước toàn bộ graph: khi dequeue một word, scan words để sinh neighbor. `O(N²L)` phù hợp.

### Concrete dry run

**Concrete input:** `hit→hot→dot→dog→cog`

`hit→hot→dot→dog→cog` có 4 edge. BFS có thể đồng thời khám phá nhánh `hot→lot`, nhưng target lần đầu dequeue ở layer 4 vẫn shortest.



Decision ledger của **Chuyển đổi từ** nối `visited occurrence, BFS queue và head.` với đúng block code; mỗi inner action trong input `hit→hot→dot→dog→cog` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `hit→hot→dot→dog→cog` | Chưa có state của Chuyển đổi từ | Contract và input đã hợp lệ | queue [{word:begin,steps:0}]; visited false; head=0. | visited occurrence, BFS queue và head. | Chưa chốt output |
| {word,steps} và candidate words[index]. đi vào | differences trong helper; next steps=steps+1. | Với `hit→hot→dot→dog→cog`, hỏi: current là target; candidate unseen và Hamming distance đúng 1. | target thì return; invalid candidate continue; valid mark/enqueue.; head tăng riêng; candidate index tăng qua words. | visited occurrence, BFS queue và head. sau branch vừa chọn | số lần biến đổi ít nhất từ begin tới target, hoặc 0. chỉ đổi ở branch ghi answer |
| Transition của current | visited occurrence, BFS queue và head. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | visited[index]=true trước push occurrence với steps+1. | visited occurrence, BFS queue và head. đã commit current; invariant của Chuyển đổi từ được phục hồi | số lần biến đổi ít nhất từ begin tới target, hoặc 0. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | visited theo index giữ duplicate occurrence tách biệt.; target absent/queue cạn trả 0; gặp target trả steps. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Chuyển đổi từ**, đỉnh hoặc frontier entry hiện tại đi vào → đọc `differences trong helper; next steps=steps+1.` từ state đang sống → hỏi đúng câu `current là target; candidate unseen và Hamming distance đúng 1.` → đi nhánh `target thì return; invalid candidate continue; valid mark/enqueue.` → thực hiện `visited[index]=true trước push occurrence với steps+1.` → chuyển sang neighbor hợp lệ sau guard bounds/visited hoặc push candidate mới vào frontier → chỉ chốt `số lần biến đổi ít nhất từ begin tới target, hoặc 0.` ở vị trí mà invariant đã đúng → visited theo index giữ duplicate occurrence tách biệt. → target absent/queue cạn trả 0; gặp target trả steps..

Với **Chuyển đổi từ**, chuỗi “đọc {word,steps} và candidate words[index]. → hỏi current là target; candidate unseen và Hamming distance đúng 1. → đổi visited[index]=true trước push occurrence với steps+1. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Chuyển đổi từ | `visited occurrence, BFS queue và head.` | State chung mà mọi current đọc hoặc mutate | `queue [{word:begin,steps:0}]; visited false; head=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `{word,steps} và candidate words[index].` | Current của đúng iteration/candidate hiện tại | Do `dequeue word; scan mọi words để sinh implicit neighbor.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `differences trong helper; next steps=steps+1.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số lần biến đổi ít nhất từ begin tới target, hoặc 0.` | Contract output của Chuyển đổi từ | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const visited = Array(words.length).fill(false);`; lựa chọn này giữ đúng thông tin mà check `current là target; candidate unseen và Hamming distance đúng 1.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `dequeue word; scan mọi words để sinh implicit neighbor.`. Đây là lý do code dùng block bắt đầu bởi `while (head < queue.length) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “current là target; candidate unseen và Hamming distance đúng 1.”. Trong dry run `hit→hot→dot→dog→cog`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (!words.includes(target)) return 0;`.
- **Tiến triển:** head tăng riêng; candidate index tăng qua words.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `visited[index]=true trước push occurrence với steps+1.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `head += 1;`.
- **Cleanup:** visited theo index giữ duplicate occurrence tách biệt.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Chuyển đổi từ:** Guard visited, bounds hoặc stale distance phải chạy trước transition tương ứng. Với BFS, mark visited lúc enqueue để một đỉnh không xếp hàng nhiều lần; với Dijkstra, bỏ entry stale trước khi relax cạnh.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Chuyển đổi từ | visited occurrence, BFS queue và head. | `const visited = Array(words.length).fill(false);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | dequeue word; scan mọi words để sinh implicit neighbor. | `while (head < queue.length) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | current là target; candidate unseen và Hamming distance đúng 1. | `if (!words.includes(target)) return 0;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | visited[index]=true trước push occurrence với steps+1. | `queue.push({ word: words[index], steps: steps + 1 });` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | head tăng riêng; candidate index tăng qua words. | `head += 1;` | Sau read/update theo invariant của Chuyển đổi từ |
| Hoàn tất và trả contract output | visited theo index giữ duplicate occurrence tách biệt. | `return 0;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Chuyển đổi từ** là **toàn bộ `minimumWordTransformations` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Chuyển đổi từ:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function minimumWordTransformations(begin, target, words) {
}
```

**Bước 2 — cleanup/return**

> **Prediction checkpoint — finalization của Chuyển đổi từ:** Dòng `if (!words.includes(target)) return 0;` chạy per current hay đúng một lần sau khi `dequeue word; scan mọi words để sinh implicit neighbor.` kết thúc? Trước return, cleanup `visited theo index giữ duplicate occurrence tách biệt.` và output `số lần biến đổi ít nhất từ begin tới target, hoặc 0.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Chuyển đổi từ:** Những dữ kiện nào trong `visited occurrence, BFS queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const visited = Array(words.length).fill(false);` chạy đúng một lần trước traversal. Nếu đặt nó trong `dequeue word; scan mọi words để sinh implicit neighbor.`, state nào sẽ bị reset?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Chuyển đổi từ:** Những dữ kiện nào trong `visited occurrence, BFS queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const queue = [{ word: begin, steps: 0 }];` chạy đúng một lần trước traversal. Nếu đặt nó trong `dequeue word; scan mọi words để sinh implicit neighbor.`, state nào sẽ bị reset?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Chuyển đổi từ:** Những dữ kiện nào trong `visited occurrence, BFS queue và head.` phải còn nguyên khi current kế tiếp tới? Block `let head = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `dequeue word; scan mọi words để sinh implicit neighbor.`, state nào sẽ bị reset?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
}
```

**Bước 6 — main loop và scope**

> **Prediction checkpoint — loop của Chuyển đổi từ:** Header `while (head < queue.length) {` lặp per item, per candidate hay tới khi state rỗng? Current `{word,steps} và candidate words[index].` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
  while (head < queue.length) {
  }
}
```

**Bước 7 — condition/inner branch**

> **Prediction checkpoint — condition của Chuyển đổi từ:** Trước khi chạy `if (word === target) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `current là target; candidate unseen và Hamming distance đúng 1.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
  while (head < queue.length) {
    const { word, steps } = queue[head];
    head += 1;
    if (word === target) {
    }
  }
}
```

**Bước 8 — transition và movement**

> **Prediction checkpoint — transition của Chuyển đổi từ:** Nhìn các brace đang mở để quyết định `if (word === target) {`, `return steps;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `visited[index]=true trước push occurrence với steps+1.`, và movement `head tăng riêng; candidate index tăng qua words.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
  while (head < queue.length) {
    const { word, steps } = queue[head];
    head += 1;
    if (word === target) {
      return steps;
    }
  }
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Chuyển đổi từ:** Nhìn các brace đang mở để quyết định `while (head < queue.length) {`, `const { word, steps } = queue[head];`, `head += 1;`, `if (word === target) {`, `return steps;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `visited[index]=true trước push occurrence với steps+1.`, và movement `head tăng riêng; candidate index tăng qua words.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
  while (head < queue.length) {
    const { word, steps } = queue[head];
    head += 1;
    if (word === target) {
      return steps;
    }

    for (let index = 0; index < words.length; index++) {
      if (visited[index] || !differsByOneCharacter(word, words[index])) continue;
      visited[index] = true;
      queue.push({ word: words[index], steps: steps + 1 });
    }
  }
}
```

**Bước 10 — cleanup/return**

> **Prediction checkpoint — finalization của Chuyển đổi từ:** Dòng `return 0;` chạy per current hay đúng một lần sau khi `dequeue word; scan mọi words để sinh implicit neighbor.` kết thúc? Trước return, cleanup `visited theo index giữ duplicate occurrence tách biệt.` và output `số lần biến đổi ít nhất từ begin tới target, hoặc 0.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function minimumWordTransformations(begin, target, words) {
  if (!words.includes(target)) return 0;
  const visited = Array(words.length).fill(false);
  const queue = [{ word: begin, steps: 0 }];
  let head = 0;
  while (head < queue.length) {
    const { word, steps } = queue[head];
    head += 1;
    if (word === target) {
      return steps;
    }

    for (let index = 0; index < words.length; index++) {
      if (visited[index] || !differsByOneCharacter(word, words[index])) continue;
      visited[index] = true;
      queue.push({ word: words[index], steps: steps + 1 });
    }
  }
  return 0;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const visited = Array(words.length).fill(false);` | Trước traversal trong main function | visited occurrence, BFS queue và head. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Chuyển đổi từ |
| `{word,steps} và candidate words[index].` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (!words.includes(target)) return 0;` | Sau khi state guard/current đã sẵn | Condition đọc differences trong helper; next steps=steps+1. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `queue.push({ word: words[index], steps: steps + 1 });` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi current là target; candidate unseen và Hamming distance đúng 1. | Đưa ra ngoài áp dụng update cho cả case false |
| `head += 1;` | head tăng riêng; candidate index tăng qua words. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: visited theo index giữ duplicate occurrence tách biệt. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return 0;` | Sau finalization của main function | Output phải đạt số lần biến đổi ít nhất từ begin tới target, hoặc 0. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Chuyển đổi từ** dùng `hit→hot→dot→dog→cog`: dừng ngay khi `visited occurrence, BFS queue và head.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `minimumWordTransformations` với `hit→hot→dot→dog→cog`; theo dõi đúng output `số lần biến đổi ít nhất từ begin tới target, hoặc 0.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | queue [{word:begin,steps:0}]; visited false; head=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | visited occurrence, BFS queue và head. | {word,steps} và candidate words[index]. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | differences trong helper; next steps=steps+1. | visited[index]=true trước push occurrence với steps+1. | Đổi đúng như từng dòng dry run gốc | Condition current là target; candidate unseen và Hamming distance đúng 1. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | head tăng riêng; candidate index tăng qua words. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | visited theo index giữ duplicate occurrence tách biệt. | target absent/queue cạn trả 0; gặp target trả steps. | số lần biến đổi ít nhất từ begin tới target, hoặc 0. | Không còn transition bắt buộc nào chưa chạy |

Trong **Chuyển đổi từ**, chỉ dòng thực hiện `visited[index]=true trước push occurrence với steps+1.` được đổi output; state còn pending theo `visited occurrence, BFS queue và head.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Chấp nhận difference '<=1'. | `hit→hot→dot→dog→cog` | Lượt đầu của `{word,steps} và candidate words[index].` mà `current là target; candidate unseen và Hamming distance đúng 1.` quyết định branch | Chấp nhận difference '<=1'. | `if (!words.includes(target)) return 0;` ở đúng scope |
| DFS và return path đầu. | `hit → cog` | Ngay transition đầu tiên thực hiện `visited[index]=true trước push occurrence với steps+1.` trên boundary/duplicate của input | DFS và return path đầu. | `return 0;` với init/strictness của Chuyển đổi từ |
| Mark lúc dequeue. | `dot, lot` | Lượt đầu movement `head tăng riêng; candidate index tăng qua words.` xảy ra trước/sau sai assignment | Mark lúc dequeue. | `queue.push({ word: words[index], steps: steps + 1 });` trước/sau movement đúng như mapping |

### Complexity proof from movements

Mỗi node dequeue tối đa một lần, mỗi lần scan N word và so L ký tự: `O(N²L)` time, `O(N)` space.

**Proof từ operation của Chuyển đổi từ:** Đếm theo representation của graph: traversal thường xử lý mỗi đỉnh hữu hạn lần và xét mỗi cạnh theo adjacency; nếu frontier là heap, mỗi push/pop hợp lệ cộng O(log V). Main loop được mô tả là `dequeue word; scan mọi words để sinh implicit neighbor.`; movement là `head tăng riêng; candidate index tăng qua words.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `visited occurrence, BFS queue và head.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Chuyển đổi từ; test `hit→hot→dot→dog→cog` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `visited occurrence, BFS queue và head.`, condition `current là target; candidate unseen và Hamming distance đúng 1.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Chuyển đổi từ. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với `hit → cog`, layer đầu chỉ có `hot`; layer hai có `dot, lot`; layer ba có `dog, log`; layer
bốn tới `cog`. Ta không cần dựng adjacency trước: rule “khác đúng một ký tự” sinh edge khi cần.

```js
function minimumWordTransformationsBrute(begin, target, words) {
  let best = Infinity;
  const used = Array(words.length).fill(false);

  function differsByOne(first, second) {
    let differences = 0;
    for (let index = 0; index < first.length; index += 1) {
      if (first[index] !== second[index]) differences += 1;
    }
    return differences === 1;
  }

  function dfs(current, steps) {
    if (steps >= best) return;
    if (current === target) {
      best = steps;
      return;
    }
    for (let index = 0; index < words.length; index += 1) {
      if (used[index] || !differsByOne(current, words[index])) continue;
      used[index] = true;
      dfs(words[index], steps + 1);
      used[index] = false;
    }
  }

  dfs(begin, 0);
  return best === Infinity ? 0 : best;
}
```

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `visited[index]` | occurrence trong `words` đã discover | toàn false |
| queue item | word hiện tại và shortest steps | `{word: begin, steps: 0}` |
| `head` | vị trí dequeue | `0` |
| `differences` | Hamming distance đang đếm | `0` cho mỗi cặp |

#### 21. Blueprint từ đề đến code

1. Xem mỗi word là node; hai node nối nếu Hamming distance đúng 1.
2. Không materialize graph vì neighbor có thể kiểm trực tiếp từ `words`.
3. Vì mỗi phép đổi cost 1 và cần minimum, dùng BFS.
4. Loại sớm nếu target không có trong danh sách hợp lệ.
5. Mark occurrence khi enqueue và gắn `steps + 1` vào state.

#### 22. Invariant, lỗi lộ diện và recall

Mỗi queue item có shortest steps tới word của nó. Helper chỉ nhận đúng một ký tự khác, nên mọi
edge sinh ra đều hợp lệ; BFS layer order bảo đảm lần discover đầu là tốt nhất. Dùng visited theo
index khiến identity rõ ràng ngay cả khi dữ liệu mở rộng có duplicate.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| chấp nhận khác 0 ký tự | begin xuất hiện trong words | không tạo self-step |
| chấp nhận khác 2 ký tự | `hit → dot` | không phải neighbor |
| target không có trong list | `cog` absent | 0 |
| DFS trả path đầu | có nhánh dài được xét trước | shortest BFS |

Recall: **implicit graph không cần adjacency; viết rule neighbor chính xác rồi BFS nếu edge cost 1.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số lần biến đổi ít nhất từ begin tới target, hoặc 0.
PREPARE: target phải xuất hiện trong words; neighbor rule khác đúng một ký tự.
GLOBAL STATE: visited occurrence, BFS queue và head.
INIT: queue [{word:begin,steps:0}]; visited false; head=0.
MAIN LOOP: dequeue word; scan mọi words để sinh implicit neighbor.
CURRENT ITEM: {word,steps} và candidate words[index].
PER-ITERATION STATE: differences trong helper; next steps=steps+1.
CHECK: current là target; candidate unseen và Hamming distance đúng 1.
BRANCH: target thì return; invalid candidate continue; valid mark/enqueue.
UPDATE: visited[index]=true trước push occurrence với steps+1.
POINTER MOVEMENT: head tăng riêng; candidate index tăng qua words.
STOP / RETURN: target absent/queue cạn trả 0; gặp target trả steps.
CLEANUP: visited theo index giữ duplicate occurrence tách biệt.
```

- Recall 1: Viết neighbor khác đúng một ký tự rồi BFS implicit graph; mark occurrence khi enqueue.
- Recall 2: STATE visited/queue → LOOP dequeue+scan → CHECK target/diff1 → UPDATE mark/enqueue → RETURN steps/0.
- Recall 3: Điền helper chỉ true khi `differences === ___`; enqueue với `steps + ___`.
<!-- dry-run-reconstruction:end -->
