# OF040 — Nhặt vật phẩm

- Nguồn đề: [Programmers lesson 87694](https://school.programmers.co.kr/learn/courses/30/lessons/87694?language=javascript)
- Pattern chính: coordinate scaling + border BFS
- Mức trong bank: STRETCH / special modeling

## 01. Contract

Các rectangle tạo vùng; chỉ đi trên đường biên union từ nhân vật tới vật phẩm. Trả shortest distance.

## 02. Bound

Tọa độ nhỏ cho phép raster grid. Scale ×2 tránh hai cạnh/corner chạm nhau bị coi là đường tắt.

## 03. Brute force

Đi trên grid gốc đánh dấu border trực tiếp; sai topology ở corner và đoạn cạnh sát nhau.

## 04. Bottleneck

Mô hình tọa độ nguyên không biểu diễn midpoint/crossing. Nhân đôi tạo ô trung gian, sau đó fill rectangles và erase strict interiors để giữ đúng border union.

## 05. Pattern

Scale all coordinates; fill toàn rectangle=1; erase interior=0; BFS bốn hướng trên cell1; chia distance2.

## 06. State

Scaled board, distance, queue/head, scaled start/target.

## 07. Transition

Neighbor trong bounds, board=1, unseen; mark/enqueue.

## 08. Invariant

Sau fill-erase, cell1 đúng đường biên có thể đi của union. Scale bảo đảm các điểm giao hình học xuất hiện thành node/cạnh riêng; BFS lần đầu tới target là shortest scaled path.

## 09. Complexity

Với coordinate bound cố định, build/BFS `O(C²)`; space `O(C²)`.

## 10. Pseudocode

```text
scale x2
fill every rectangle including border
erase every strict interior
BFS border from character to item
return scaledDistance/2
```

## 11. Code bước 1 — dựng state

```js
const start=[characterY*2,characterX*2];
const target=[itemY*2,itemX*2];
```

## 12. Code bước 2 — viết transition

```js
for (let row=y1*2+1;row<y2*2;row++)
  for (let column=x1*2+1;column<x2*2;column++) board[row][column]=0;
```

## 13. Code bước 3 — ghép vòng lặp

```js
if (board[nextRow][nextColumn]===1 && distance[nextRow][nextColumn]===-1) {
  distance[nextRow][nextColumn]=distance[row][column]+1;
  queue.push([nextRow,nextColumn]);
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF040.js](../../../solutions/official/OF040.js).

```js
const BORDER_DIRECTIONS = [[-1, 0], [1, 0], [0, -1], [0, 1]];

function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));

  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }

  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;

  while (head < queue.length) {
    const [row, column] = queue[head];
    head += 1;
    if (row === targetRow && column === targetColumn) {
      return distance[row][column] / 2;
    }
    for (const [deltaRow, deltaColumn] of BORDER_DIRECTIONS) {
      const nextRow = row + deltaRow;
      const nextColumn = column + deltaColumn;
      if (
        nextRow < 0 || nextRow >= SIZE || nextColumn < 0 || nextColumn >= SIZE ||
        board[nextRow][nextColumn] !== 1 || distance[nextRow][nextColumn] !== -1
      ) {
        continue;
      }
      distance[nextRow][nextColumn] = distance[row][column] + 1;
      queue.push([nextRow, nextColumn]);
    }
  }
  return -1;
}
```

## 15. Dry-run

Rectangle đơn từ `(1,1)` tới `(2,2)` thành square scaled cạnh2; hai corner đối diện cách4 half-steps, trả2.

## 16. Edge cases

Rectangles overlap; share border/corner; start-target cùng rectangle; inner area phải không đi được.

## 17. Lỗi JavaScript thường gặp

Không scale; erase interior trước khi fill rectangle khác rồi bị fill lại; đảo x/y; quên chia2; đi diagonal.

## 18. Bài transfer cùng pattern

- OF038: grid BFS không modeling.
- OF042: extract/canonicalize components.
- OF047: scale half-step để bắt crossing.

Tín hiệu: geometry trên tọa độ nguyên có corner/crossing mơ hồ ⇒ scale trước khi raster traversal.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF040 Nhặt vật phẩm

Phần này dùng chính contract, dry run, Blueprint và code executable của **Nhặt vật phẩm** để dựng lại lời giải; pattern kiểm soát là `BFS-coordinate-modeling`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Các rectangle tạo vùng; chỉ đi trên đường biên union từ nhân vật tới vật phẩm. Trả shortest distance.

**Giới hạn quyết định implementation:** Tọa độ nhỏ cho phép raster grid. Scale ×2 tránh hai cạnh/corner chạm nhau bị coi là đường tắt.

### Concrete dry run

**Concrete input:** `(1,1)`

Rectangle đơn từ `(1,1)` tới `(2,2)` thành square scaled cạnh2; hai corner đối diện cách4 half-steps, trả2.



Decision ledger của **Nhặt vật phẩm** nối `board border, distance, queue và head.` với đúng block code; mỗi inner action trong input `(1,1)` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `(1,1)` | Chưa có state của Nhặt vật phẩm | Contract và input đã hợp lệ | border cell=1; distance=-1; start distance=0. | board border, distance, queue và head. | Chưa chốt output |
| scaled [row,column] dequeued. đi vào | nextRow,nextColumn. | Với `(1,1)`, hỏi: current là target; next bounds; border; unseen. | invalid continue; valid set distance và enqueue.; head tăng sau read; directions quét bốn neighbor. | board border, distance, queue và head. sau branch vừa chọn | khoảng cách ngắn nhất dọc biên rectangle từ character tới item. chỉ đổi ở branch ghi answer |
| Transition của current | board border, distance, queue và head. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | distance[next]=distance[current]+1 trước push. | board border, distance, queue và head. đã commit current; invariant của Nhặt vật phẩm được phục hồi | khoảng cách ngắn nhất dọc biên rectangle từ character tới item. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | strict interior erase sau toàn bộ fill để overlap union đúng.; tới target trả scaled distance/2; queue cạn -1. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Nhặt vật phẩm**, đỉnh hoặc frontier entry hiện tại đi vào → đọc `nextRow,nextColumn.` từ state đang sống → hỏi đúng câu `current là target; next bounds; border; unseen.` → đi nhánh `invalid continue; valid set distance và enqueue.` → thực hiện `distance[next]=distance[current]+1 trước push.` → chuyển sang neighbor hợp lệ sau guard bounds/visited hoặc push candidate mới vào frontier → chỉ chốt `khoảng cách ngắn nhất dọc biên rectangle từ character tới item.` ở vị trí mà invariant đã đúng → strict interior erase sau toàn bộ fill để overlap union đúng. → tới target trả scaled distance/2; queue cạn -1..

Với **Nhặt vật phẩm**, chuỗi “đọc scaled [row,column] dequeued. → hỏi current là target; next bounds; border; unseen. → đổi distance[next]=distance[current]+1 trước push. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Nhặt vật phẩm | `board border, distance, queue và head.` | State chung mà mọi current đọc hoặc mutate | `border cell=1; distance=-1; start distance=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `scaled [row,column] dequeued.` | Current của đúng iteration/candidate hiện tại | Do `BFS bốn hướng trên cell board===1.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `nextRow,nextColumn.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `khoảng cách ngắn nhất dọc biên rectangle từ character tới item.` | Contract output của Nhặt vật phẩm | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const SIZE = 102;`; lựa chọn này giữ đúng thông tin mà check `current là target; next bounds; border; unseen.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `BFS bốn hướng trên cell board===1.`. Đây là lý do code dùng block bắt đầu bởi `while (head < queue.length) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “current là target; next bounds; border; unseen.”. Trong dry run `(1,1)`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (row === targetRow && column === targetColumn) {`.
- **Tiến triển:** head tăng sau read; directions quét bốn neighbor.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `distance[next]=distance[current]+1 trước push.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (let row = y1 * 2; row <= y2 * 2; row++) {`.
- **Cleanup:** strict interior erase sau toàn bộ fill để overlap union đúng.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Nhặt vật phẩm:** Guard visited, bounds hoặc stale distance phải chạy trước transition tương ứng. Với BFS, mark visited lúc enqueue để một đỉnh không xếp hàng nhiều lần; với Dijkstra, bỏ entry stale trước khi relax cạnh.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Nhặt vật phẩm | board border, distance, queue và head. | `const SIZE = 102;` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | BFS bốn hướng trên cell board===1. | `while (head < queue.length) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | current là target; next bounds; border; unseen. | `if (row === targetRow && column === targetColumn) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | distance[next]=distance[current]+1 trước push. | `queue.push([nextRow, nextColumn]);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | head tăng sau read; directions quét bốn neighbor. | `for (let row = y1 * 2; row <= y2 * 2; row++) {` | Sau read/update theo invariant của Nhặt vật phẩm |
| Hoàn tất và trả contract output | strict interior erase sau toàn bộ fill để overlap union đúng. | `return -1;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Nhặt vật phẩm** là **toàn bộ `itemPickupDistance` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Nhặt vật phẩm:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const SIZE = 102;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
}
```

**Bước 4 — main loop và scope**

> **Prediction checkpoint — loop của Nhặt vật phẩm:** Header `for (const [x1, y1, x2, y2] of rectangles) {` lặp per item, per candidate hay tới khi state rỗng? Current `scaled [row,column] dequeued.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
  }
}
```

**Bước 5 — condition/inner branch**

> **Prediction checkpoint — condition của Nhặt vật phẩm:** Trước khi chạy `for (let row = y1 * 2; row <= y2 * 2; row++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `current là target; next bounds; border; unseen.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
    }
  }
}
```

**Bước 6 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `for (let row = y1 * 2; row <= y2 * 2; row++) {`, `for (let column = x1 * 2; column <= x2 * 2; column++) {`, `board[row][column] = 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
}
```

**Bước 7 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `for (const [x1, y1, x2, y2] of rectangles) {`, `for (let row = y1 * 2; row <= y2 * 2; row++) {`, `for (let column = x1 * 2; column <= x2 * 2; column++) {`, `board[row][column] = 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
}
```

**Bước 8 — main loop và scope**

> **Prediction checkpoint — loop của Nhặt vật phẩm:** Header `for (const [x1, y1, x2, y2] of rectangles) {` lặp per item, per candidate hay tới khi state rỗng? Current `scaled [row,column] dequeued.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
  }
}
```

**Bước 9 — condition/inner branch**

> **Prediction checkpoint — condition của Nhặt vật phẩm:** Trước khi chạy `for (let row = y1 * 2 + 1; row < y2 * 2; row++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `current là target; next bounds; border; unseen.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `for (let row = y1 * 2 + 1; row < y2 * 2; row++) {`, `for (let column = x1 * 2 + 1; column < x2 * 2; column++) {`, `board[row][column] = 0;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
}
```

**Bước 11 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `for (const [x1, y1, x2, y2] of rectangles) {`, `for (let row = y1 * 2 + 1; row < y2 * 2; row++) {`, `for (let column = x1 * 2 + 1; column < x2 * 2; column++) {`, `board[row][column] = 0;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
}
```

**Bước 12 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const start = [characterY * 2, characterX * 2];` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
}
```

**Bước 13 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const targetRow = itemY * 2;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
}
```

**Bước 14 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const targetColumn = itemX * 2;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
}
```

**Bước 15 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
}
```

**Bước 16 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const queue = [start];` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
}
```

**Bước 17 — state/initialization**

> **Prediction checkpoint — state của Nhặt vật phẩm:** Những dữ kiện nào trong `board border, distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `let head = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS bốn hướng trên cell board===1.`, state nào sẽ bị reset?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
}
```

**Bước 18 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `distance[start[0]][start[1]] = 0;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
}
```

**Bước 19 — main loop và scope**

> **Prediction checkpoint — loop của Nhặt vật phẩm:** Header `while (head < queue.length) {` lặp per item, per candidate hay tới khi state rỗng? Current `scaled [row,column] dequeued.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
  while (head < queue.length) {
  }
}
```

**Bước 20 — condition/inner branch**

> **Prediction checkpoint — condition của Nhặt vật phẩm:** Trước khi chạy `if (row === targetRow && column === targetColumn) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `current là target; next bounds; border; unseen.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
  while (head < queue.length) {
    const [row, column] = queue[head];
    head += 1;
    if (row === targetRow && column === targetColumn) {
    }
  }
}
```

**Bước 21 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `if (row === targetRow && column === targetColumn) {`, `return distance[row][column] / 2;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
  while (head < queue.length) {
    const [row, column] = queue[head];
    head += 1;
    if (row === targetRow && column === targetColumn) {
      return distance[row][column] / 2;
    }
  }
}
```

**Bước 22 — transition và movement**

> **Prediction checkpoint — transition của Nhặt vật phẩm:** Nhìn các brace đang mở để quyết định `while (head < queue.length) {`, `const [row, column] = queue[head];`, `head += 1;`, `if (row === targetRow && column === targetColumn) {`, `return distance[row][column] / 2;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[next]=distance[current]+1 trước push.`, và movement `head tăng sau read; directions quét bốn neighbor.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
  while (head < queue.length) {
    const [row, column] = queue[head];
    head += 1;
    if (row === targetRow && column === targetColumn) {
      return distance[row][column] / 2;
    }
    for (const [deltaRow, deltaColumn] of BORDER_DIRECTIONS) {
      const nextRow = row + deltaRow;
      const nextColumn = column + deltaColumn;
      if (
        nextRow < 0 || nextRow >= SIZE || nextColumn < 0 || nextColumn >= SIZE ||
        board[nextRow][nextColumn] !== 1 || distance[nextRow][nextColumn] !== -1
      ) {
        continue;
      }
      distance[nextRow][nextColumn] = distance[row][column] + 1;
      queue.push([nextRow, nextColumn]);
    }
  }
}
```

**Bước 23 — cleanup/return**

> **Prediction checkpoint — finalization của Nhặt vật phẩm:** Dòng `return -1;` chạy per current hay đúng một lần sau khi `BFS bốn hướng trên cell board===1.` kết thúc? Trước return, cleanup `strict interior erase sau toàn bộ fill để overlap union đúng.` và output `khoảng cách ngắn nhất dọc biên rectangle từ character tới item.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function itemPickupDistance(rectangles, characterX, characterY, itemX, itemY) {
  const SIZE = 102;
  const board = Array.from({ length: SIZE }, () => new Uint8Array(SIZE));
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2; row <= y2 * 2; row++) {
      for (let column = x1 * 2; column <= x2 * 2; column++) {
        board[row][column] = 1;
      }
    }
  }
  for (const [x1, y1, x2, y2] of rectangles) {
    for (let row = y1 * 2 + 1; row < y2 * 2; row++) {
      for (let column = x1 * 2 + 1; column < x2 * 2; column++) {
        board[row][column] = 0;
      }
    }
  }
  const start = [characterY * 2, characterX * 2];
  const targetRow = itemY * 2;
  const targetColumn = itemX * 2;
  const distance = Array.from({ length: SIZE }, () => new Int16Array(SIZE).fill(-1));
  const queue = [start];
  let head = 0;
  distance[start[0]][start[1]] = 0;
  while (head < queue.length) {
    const [row, column] = queue[head];
    head += 1;
    if (row === targetRow && column === targetColumn) {
      return distance[row][column] / 2;
    }
    for (const [deltaRow, deltaColumn] of BORDER_DIRECTIONS) {
      const nextRow = row + deltaRow;
      const nextColumn = column + deltaColumn;
      if (
        nextRow < 0 || nextRow >= SIZE || nextColumn < 0 || nextColumn >= SIZE ||
        board[nextRow][nextColumn] !== 1 || distance[nextRow][nextColumn] !== -1
      ) {
        continue;
      }
      distance[nextRow][nextColumn] = distance[row][column] + 1;
      queue.push([nextRow, nextColumn]);
    }
  }
  return -1;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const SIZE = 102;` | Trước traversal trong main function | board border, distance, queue và head. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Nhặt vật phẩm |
| `scaled [row,column] dequeued.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (row === targetRow && column === targetColumn) {` | Sau khi state guard/current đã sẵn | Condition đọc nextRow,nextColumn. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `queue.push([nextRow, nextColumn]);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi current là target; next bounds; border; unseen. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (let row = y1 * 2; row <= y2 * 2; row++) {` | head tăng sau read; directions quét bốn neighbor. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: strict interior erase sau toàn bộ fill để overlap union đúng. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return -1;` | Sau finalization của main function | Output phải đạt khoảng cách ngắn nhất dọc biên rectangle từ character tới item. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Nhặt vật phẩm** dùng `(1,1)`: dừng ngay khi `board border, distance, queue và head.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `itemPickupDistance` với `(1,1)`; theo dõi đúng output `khoảng cách ngắn nhất dọc biên rectangle từ character tới item.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | border cell=1; distance=-1; start distance=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | board border, distance, queue và head. | scaled [row,column] dequeued. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | nextRow,nextColumn. | distance[next]=distance[current]+1 trước push. | Đổi đúng như từng dòng dry run gốc | Condition current là target; next bounds; border; unseen. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | head tăng sau read; directions quét bốn neighbor. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | strict interior erase sau toàn bộ fill để overlap union đúng. | tới target trả scaled distance/2; queue cạn -1. | khoảng cách ngắn nhất dọc biên rectangle từ character tới item. | Không còn transition bắt buộc nào chưa chạy |

Trong **Nhặt vật phẩm**, chỉ dòng thực hiện `distance[next]=distance[current]+1 trước push.` được đổi output; state còn pending theo `board border, distance, queue và head.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Không scale | `(1,1)` | Lượt đầu của `scaled [row,column] dequeued.` mà `current là target; next bounds; border; unseen.` quyết định branch | Không scale | `queue.push([nextRow, nextColumn]);` ở đúng scope |
| erase interior trước khi fill rectangle khác rồi bị fill lại | `(1,1)–(2,2)` | Ngay transition đầu tiên thực hiện `distance[next]=distance[current]+1 trước push.` trên boundary/duplicate của input | erase interior trước khi fill rectangle khác rồi bị fill lại | `queue.push([nextRow, nextColumn]);` với init/strictness của Nhặt vật phẩm |
| quên chia2 | `(2,2)–(4,4)` | Lượt đầu movement `head tăng sau read; directions quét bốn neighbor.` xảy ra trước/sau sai assignment | quên chia2 | `queue.push([nextRow, nextColumn]);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Với coordinate bound cố định, build/BFS `O(C²)`; space `O(C²)`.

**Proof từ operation của Nhặt vật phẩm:** Đếm theo representation của graph: traversal thường xử lý mỗi đỉnh hữu hạn lần và xét mỗi cạnh theo adjacency; nếu frontier là heap, mỗi push/pop hợp lệ cộng O(log V). Main loop được mô tả là `BFS bốn hướng trên cell board===1.`; movement là `head tăng sau read; directions quét bốn neighbor.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `board border, distance, queue và head.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Nhặt vật phẩm; test `(1,1)` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `board border, distance, queue và head.`, condition `current là target; next bounds; border; unseen.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Nhặt vật phẩm. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với rectangle `(1,1)–(2,2)`, sau scale nó thành `(2,2)–(4,4)`. Hai corner đối diện cách 4
half-step trên border, nên đáp án thật là `4 / 2 = 2`. Nếu không scale, những rectangle chỉ chạm
corner có thể tạo một cạnh giả.

```js
function itemPickupDistanceUnscaledBrute(rectangle, start, target) {
  const [x1, y1, x2, y2] = rectangle;
  const border = new Set();
  for (let x = x1; x <= x2; x += 1) {
    border.add(`${y1}:${x}`);
    border.add(`${y2}:${x}`);
  }
  for (let y = y1; y <= y2; y += 1) {
    border.add(`${y}:${x1}`);
    border.add(`${y}:${x2}`);
  }

  const queue = [[start[1], start[0], 0]];
  const visited = new Set([`${start[1]}:${start[0]}`]);
  for (let head = 0; head < queue.length; head += 1) {
    const [row, column, steps] = queue[head];
    if (column === target[0] && row === target[1]) return steps;
    for (const [dr, dc] of [[-1, 0], [1, 0], [0, -1], [0, 1]]) {
      const key = `${row + dr}:${column + dc}`;
      if (!border.has(key) || visited.has(key)) continue;
      visited.add(key);
      queue.push([row + dr, column + dc, steps + 1]);
    }
  }
  return -1;
}
```

Brute trên chỉ đúng cho một rectangle đơn; counterexample nhiều rectangle cho thấy modeling, không
phải BFS, mới là phần khó của bài.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `board` | raster của border union đã scale | fill rectangle rồi erase strict interior |
| `start` | `[row,column]` đã scale | `[characterY*2, characterX*2]` |
| `distance` | half-step shortest; `-1` là unseen | start bằng 0 |
| `queue`, `head` | BFS trên cell border | `[start]`, `0` |

#### 21. Blueprint từ đề đến code

1. Nhận ra đường đi nằm trên biên hình học, không phải toàn cell trong rectangle.
2. Scale mọi tọa độ ×2 để giữ topology tại corner và cạnh sát nhau.
3. Fill toàn bộ rectangle trước; chỉ sau đó erase strict interior của mọi rectangle.
4. Đổi `(x,y)` sang `[row=y,column=x]` nhất quán.
5. BFS bốn hướng trên cell giá trị 1, rồi chia distance scaled cho 2.

#### 22. Invariant, lỗi lộ diện và recall

Sau hai pass fill-erase, cell 1 biểu diễn đúng border đi được của union. Không được erase rồi fill
từng rectangle xen kẽ vì rectangle sau có thể tô lại interior cần xóa. Trên graph border đã đúng,
invariant shortest của BFS giống OF038.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| không scale ×2 | rectangles chạm corner | không đi tắt |
| erase trước khi fill xong | rectangles overlap | chỉ giữ union border |
| đảo x/y | rectangle không đối xứng | path đúng hình |
| quên chia 2 | square cạnh 1 | 2 thay vì 4 half-step |

Recall: **bài geometry thường khó ở graph model: scale ×2 → fill union → erase interior → BFS border.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: khoảng cách ngắn nhất dọc biên rectangle từ character tới item.
PREPARE: scale tọa độ ×2; fill union rectangles rồi erase strict interiors.
GLOBAL STATE: board border, distance, queue và head.
INIT: border cell=1; distance=-1; start distance=0.
MAIN LOOP: BFS bốn hướng trên cell board===1.
CURRENT ITEM: scaled [row,column] dequeued.
PER-ITERATION STATE: nextRow,nextColumn.
CHECK: current là target; next bounds; border; unseen.
BRANCH: invalid continue; valid set distance và enqueue.
UPDATE: distance[next]=distance[current]+1 trước push.
POINTER MOVEMENT: head tăng sau read; directions quét bốn neighbor.
STOP / RETURN: tới target trả scaled distance/2; queue cạn -1.
CLEANUP: strict interior erase sau toàn bộ fill để overlap union đúng.
```

- Recall 1: Scale x2 giữ topology, dựng đúng border union rồi BFS và chia khoảng cách cho 2.
- Recall 2: STATE board/distance/queue → LOOP BFS → CHECK border/unseen/target → UPDATE → RETURN /2.
- Recall 3: Điền fill theo `coordinate * ___`; strict interior bắt đầu `x1*2 + ___`; return distance `/ ___`.
<!-- dry-run-reconstruction:end -->
