# OF045 — Node xa nhất

- Nguồn đề: [Programmers lesson 49189](https://school.programmers.co.kr/learn/courses/30/lessons/49189?language=javascript)
- Pattern chính: adjacency-list BFS distance
- Mức trong bank: CORE / graph distance

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Graph vô hướng không trọng số, bắt đầu node 1. Khoảng cách là số edge trên shortest path. Trả số node có khoảng cách lớn nhất từ node 1.

## 02. Bound

Adjacency list + BFS chạy `O(N+E)`. Không dùng all-pairs shortest path.

## 03. Brute force

Với mỗi node đích chạy một BFS riêng hoặc enumerate path; lặp lại traversal graph N lần.

## 04. Bottleneck

Một BFS từ source đã cho shortest distance tới mọi node cùng lúc. Sau đó chỉ cần argmax count.

## 05. Pattern

Build undirected adjacency; BFS từ 1 với `distance=-1`; scan distance để giữ maximum và count.

## 06. State

- adjacency list;
- distance `n+1`;
- queue/head;
- `maximumDistance`, `count`.

## 07. Transition

Neighbor unseen nhận `distance[node]+1`, mark/enqueue. Khi scan: distance lớn hơn reset count=1; bằng maximum thì increment.

## 08. Invariant

BFS lần đầu gán distance là shortest vì edge cost 1. Trong scan node 1..i, maximum/count mô tả chính xác prefix đã xét.

## 09. Complexity

Time `O(N+E)`, space `O(N+E)`.

## 10. Pseudocode

```text
build undirected adjacency
distance=-1; distance[1]=0; BFS
max=0,count=0
for node:
  if dist>max: max=dist,count=1
  else if dist==max: count++
return count
```

## 11. Code bước 1 — dựng state

```js
function graph(n,edges) {
  const adj=Array.from({length:n+1},()=>[]);
  for (const [a,b] of edges) { adj[a].push(b); adj[b].push(a); }
  return adj;
}
```

## 12. Code bước 2 — viết transition

```js
function discover(distance,node,neighbor,queue) {
  distance[neighbor]=distance[node]+1;
  queue.push(neighbor);
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function countMaximum(values) {
  const maximum=Math.max(...values);
  return values.filter(value=>value===maximum).length;
}
```

Code cuối scan không spread để ổn với array lớn.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF045.js](../../../solutions/official/OF045.js).

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }

  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;

  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }

  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
    if (distance[node] > maximumDistance) {
      maximumDistance = distance[node];
      count = 1;
    } else if (distance[node] === maximumDistance) {
      count += 1;
    }
  }
  return count;
}
```

## 15. Dry-run

Star từ node 1 tới 2,3,4: ba node cùng distance 1, maximum 1 và count 3.

## 16. Edge cases

1. n=1: node 1 xa nhất với distance 0, count 1.
2. Chain: chỉ node cuối xa nhất.
3. Nhiều node tie ở layer cuối.
4. Duplicate edge không làm sai visited.

## 17. Lỗi JavaScript thường gặp

- Chỉ thêm adjacency một chiều.
- Dùng DFS depth làm shortest distance trong graph có cycle.
- Count maximum trước khi BFS xong.
- Dùng `Math.max(...distance)` với input quá lớn.

## 18. Bài transfer cùng pattern

- OF037: đếm components, outer loop.
- OF038: grid BFS.
- OF059: weighted edge bắt buộc Dijkstra.

Tín hiệu: một source + graph unweighted + shortest distance tới mọi node ⇒ một BFS.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF045 Node xa nhất

Phần này dùng chính contract, dry run, Blueprint và code executable của **Node xa nhất** để dựng lại lời giải; pattern kiểm soát là `Graph-BFS-distance`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Graph vô hướng không trọng số, bắt đầu node 1. Khoảng cách là số edge trên shortest path. Trả số node có khoảng cách lớn nhất từ node 1.

**Giới hạn quyết định implementation:** Adjacency list + BFS chạy `O(N+E)`. Không dùng all-pairs shortest path.

### Concrete dry run

**Concrete input:** `n=4, edge=[[1,2],[1,3],[1,4]]`

Star từ node 1 tới 2,3,4: ba node cùng distance 1, maximum 1 và count 3.



Decision ledger của **Node xa nhất** nối `distance, queue và head.` với đúng block code; mỗi inner action trong input `n=4, edge=[[1,2],[1,3],[1,4]]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `n=4, edge=[[1,2],[1,3],[1,4]]` | Chưa có state của Node xa nhất | Contract và input đã hợp lệ | distance=-1; distance[1]=0; queue=[1]; head=0. | distance, queue và head. | Chưa chốt output |
| node và neighbor; phase cuối là distance[node]. đi vào | maximumDistance và count ở phase scan. | Với `n=4, edge=[[1,2],[1,3],[1,4]]`, hỏi: neighbor unseen; distance lớn hơn hay bằng current maximum. | unseen mark/enqueue; scan lớn hơn reset count, bằng thì increment.; head tăng riêng; node scan tăng 1. | distance, queue và head. sau branch vừa chọn | số node có shortest distance lớn nhất từ node 1. chỉ đổi ở branch ghi answer |
| Transition của current | distance, queue và head. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | distance[neighbor]=distance[node]+1; update max/count. | distance, queue và head. đã commit current; invariant của Node xa nhất được phục hồi | số node có shortest distance lớn nhất từ node 1. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không spread distance vào Math.max; index 0 bị bỏ.; BFS queue cạn, scan hết n thì return count. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Node xa nhất**, đỉnh hoặc frontier entry hiện tại đi vào → đọc `maximumDistance và count ở phase scan.` từ state đang sống → hỏi đúng câu `neighbor unseen; distance lớn hơn hay bằng current maximum.` → đi nhánh `unseen mark/enqueue; scan lớn hơn reset count, bằng thì increment.` → thực hiện `distance[neighbor]=distance[node]+1; update max/count.` → chuyển sang neighbor hợp lệ sau guard bounds/visited hoặc push candidate mới vào frontier → chỉ chốt `số node có shortest distance lớn nhất từ node 1.` ở vị trí mà invariant đã đúng → không spread distance vào Math.max; index 0 bị bỏ. → BFS queue cạn, scan hết n thì return count..

Với **Node xa nhất**, chuỗi “đọc node và neighbor; phase cuối là distance[node]. → hỏi neighbor unseen; distance lớn hơn hay bằng current maximum. → đổi distance[neighbor]=distance[node]+1; update max/count. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Node xa nhất | `distance, queue và head.` | State chung mà mọi current đọc hoặc mutate | `distance=-1; distance[1]=0; queue=[1]; head=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `node và neighbor; phase cuối là distance[node].` | Current của đúng iteration/candidate hiện tại | Do `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `maximumDistance và count ở phase scan.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số node có shortest distance lớn nhất từ node 1.` | Contract output của Node xa nhất | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const adjacency = Array.from({ length: n + 1 }, () => []);`; lựa chọn này giữ đúng thông tin mà check `neighbor unseen; distance lớn hơn hay bằng current maximum.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`. Đây là lý do code dùng block bắt đầu bởi `for (const [from, to] of edges) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “neighbor unseen; distance lớn hơn hay bằng current maximum.”. Trong dry run `n=4, edge=[[1,2],[1,3],[1,4]]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (distance[neighbor] !== -1) {`.
- **Tiến triển:** head tăng riêng; node scan tăng 1.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `distance[neighbor]=distance[node]+1; update max/count.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `head += 1;`.
- **Cleanup:** không spread distance vào Math.max; index 0 bị bỏ.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Node xa nhất:** Guard visited, bounds hoặc stale distance phải chạy trước transition tương ứng. Với BFS, mark visited lúc enqueue để một đỉnh không xếp hàng nhiều lần; với Dijkstra, bỏ entry stale trước khi relax cạnh.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Node xa nhất | distance, queue và head. | `const adjacency = Array.from({ length: n + 1 }, () => []);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n. | `for (const [from, to] of edges) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | neighbor unseen; distance lớn hơn hay bằng current maximum. | `if (distance[neighbor] !== -1) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | distance[neighbor]=distance[node]+1; update max/count. | `adjacency[from].push(to);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | head tăng riêng; node scan tăng 1. | `head += 1;` | Sau read/update theo invariant của Node xa nhất |
| Hoàn tất và trả contract output | không spread distance vào Math.max; index 0 bị bỏ. | `return count;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Node xa nhất** là **toàn bộ `farthestNodeCount` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Node xa nhất:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function farthestNodeCount(n, edges) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const adjacency = Array.from({ length: n + 1 }, () => []);` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
}
```

**Bước 3 — main loop và scope**

> **Prediction checkpoint — loop của Node xa nhất:** Header `for (const [from, to] of edges) {` lặp per item, per candidate hay tới khi state rỗng? Current `node và neighbor; phase cuối là distance[node].` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
  }
}
```

**Bước 4 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `for (const [from, to] of edges) {`, `adjacency[from].push(to);`, `adjacency[to].push(from);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const distance = new Int32Array(n + 1);` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
}
```

**Bước 6 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `distance.fill(-1);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
}
```

**Bước 7 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `const queue = [1];` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
}
```

**Bước 8 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `let head = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `distance[1] = 0;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
}
```

**Bước 10 — main loop và scope**

> **Prediction checkpoint — loop của Node xa nhất:** Header `while (head < queue.length) {` lặp per item, per candidate hay tới khi state rỗng? Current `node và neighbor; phase cuối là distance[node].` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
  }
}
```

**Bước 11 — condition/inner branch**

> **Prediction checkpoint — condition của Node xa nhất:** Trước khi chạy `for (const neighbor of adjacency[node]) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `neighbor unseen; distance lớn hơn hay bằng current maximum.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
    }
  }
}
```

**Bước 12 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `for (const neighbor of adjacency[node]) {`, `if (distance[neighbor] !== -1) {`, `continue;`, `distance[neighbor] = distance[node] + 1;`, `queue.push(neighbor);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
}
```

**Bước 13 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `while (head < queue.length) {`, `const node = queue[head];`, `head += 1;`, `for (const neighbor of adjacency[node]) {`, `if (distance[neighbor] !== -1) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
}
```

**Bước 14 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `let maximumDistance = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
}
```

**Bước 15 — state/initialization**

> **Prediction checkpoint — state của Node xa nhất:** Những dữ kiện nào trong `distance, queue và head.` phải còn nguyên khi current kế tiếp tới? Block `let count = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`, state nào sẽ bị reset?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
}
```

**Bước 16 — main loop và scope**

> **Prediction checkpoint — loop của Node xa nhất:** Header `for (let node = 1; node <= n; node++) {` lặp per item, per candidate hay tới khi state rỗng? Current `node và neighbor; phase cuối là distance[node].` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
  }
}
```

**Bước 17 — condition/inner branch**

> **Prediction checkpoint — condition của Node xa nhất:** Trước khi chạy `if (distance[node] > maximumDistance) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `neighbor unseen; distance lớn hơn hay bằng current maximum.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
    if (distance[node] > maximumDistance) {
    }
  }
}
```

**Bước 18 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `if (distance[node] > maximumDistance) {`, `maximumDistance = distance[node];`, `count = 1;`, `} else if (distance[node] === maximumDistance) {`, `count += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
    if (distance[node] > maximumDistance) {
      maximumDistance = distance[node];
      count = 1;
    } else if (distance[node] === maximumDistance) {
      count += 1;
    }
  }
}
```

**Bước 19 — transition và movement**

> **Prediction checkpoint — transition của Node xa nhất:** Nhìn các brace đang mở để quyết định `for (let node = 1; node <= n; node++) {`, `if (distance[node] > maximumDistance) {`, `maximumDistance = distance[node];`, `count = 1;`, `} else if (distance[node] === maximumDistance) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `distance[neighbor]=distance[node]+1; update max/count.`, và movement `head tăng riêng; node scan tăng 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
    if (distance[node] > maximumDistance) {
      maximumDistance = distance[node];
      count = 1;
    } else if (distance[node] === maximumDistance) {
      count += 1;
    }
  }
}
```

**Bước 20 — cleanup/return**

> **Prediction checkpoint — finalization của Node xa nhất:** Dòng `return count;` chạy per current hay đúng một lần sau khi `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.` kết thúc? Trước return, cleanup `không spread distance vào Math.max; index 0 bị bỏ.` và output `số node có shortest distance lớn nhất từ node 1.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function farthestNodeCount(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }
  const distance = new Int32Array(n + 1);
  distance.fill(-1);
  const queue = [1];
  let head = 0;
  distance[1] = 0;
  while (head < queue.length) {
    const node = queue[head];
    head += 1;
    for (const neighbor of adjacency[node]) {
      if (distance[neighbor] !== -1) {
        continue;
      }
      distance[neighbor] = distance[node] + 1;
      queue.push(neighbor);
    }
  }
  let maximumDistance = 0;
  let count = 0;
  for (let node = 1; node <= n; node++) {
    if (distance[node] > maximumDistance) {
      maximumDistance = distance[node];
      count = 1;
    } else if (distance[node] === maximumDistance) {
      count += 1;
    }
  }
  return count;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const adjacency = Array.from({ length: n + 1 }, () => []);` | Trước traversal trong main function | distance, queue và head. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Node xa nhất |
| `node và neighbor; phase cuối là distance[node].` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (distance[neighbor] !== -1) {` | Sau khi state guard/current đã sẵn | Condition đọc maximumDistance và count ở phase scan. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `adjacency[from].push(to);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi neighbor unseen; distance lớn hơn hay bằng current maximum. | Đưa ra ngoài áp dụng update cho cả case false |
| `head += 1;` | head tăng riêng; node scan tăng 1. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không spread distance vào Math.max; index 0 bị bỏ. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return count;` | Sau finalization của main function | Output phải đạt số node có shortest distance lớn nhất từ node 1. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Node xa nhất** dùng `n=4, edge=[[1,2],[1,3],[1,4]]`: dừng ngay khi `distance, queue và head.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `farthestNodeCount` với `n=4, edge=[[1,2],[1,3],[1,4]]`; theo dõi đúng output `số node có shortest distance lớn nhất từ node 1.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | distance=-1; distance[1]=0; queue=[1]; head=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | distance, queue và head. | node và neighbor; phase cuối là distance[node]. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | maximumDistance và count ở phase scan. | distance[neighbor]=distance[node]+1; update max/count. | Đổi đúng như từng dòng dry run gốc | Condition neighbor unseen; distance lớn hơn hay bằng current maximum. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | head tăng riêng; node scan tăng 1. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không spread distance vào Math.max; index 0 bị bỏ. | BFS queue cạn, scan hết n thì return count. | số node có shortest distance lớn nhất từ node 1. | Không còn transition bắt buộc nào chưa chạy |

Trong **Node xa nhất**, chỉ dòng thực hiện `distance[neighbor]=distance[node]+1; update max/count.` được đổi output; state còn pending theo `distance, queue và head.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Chỉ thêm adjacency một chiều. | `n=4, edge=[[1,2],[1,3],[1,4]]` | Lượt đầu của `node và neighbor; phase cuối là distance[node].` mà `neighbor unseen; distance lớn hơn hay bằng current maximum.` quyết định branch | Chỉ thêm adjacency một chiều. | `adjacency[from].push(to);` ở đúng scope |
| Dùng DFS depth làm shortest distance trong graph có cycle. | `1—{2,3,4}` | Ngay transition đầu tiên thực hiện `distance[neighbor]=distance[node]+1; update max/count.` trên boundary/duplicate của input | Dùng DFS depth làm shortest distance trong graph có cycle. | `adjacency[from].push(to);` với init/strictness của Node xa nhất |
| Count maximum trước khi BFS xong. | `[1]` | Lượt đầu movement `head tăng riêng; node scan tăng 1.` xảy ra trước/sau sai assignment | Count maximum trước khi BFS xong. | `for (const [from, to] of edges) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(N+E)`, space `O(N+E)`.

**Proof từ operation của Node xa nhất:** Đếm theo representation của graph: traversal thường xử lý mỗi đỉnh hữu hạn lần và xét mỗi cạnh theo adjacency; nếu frontier là heap, mỗi push/pop hợp lệ cộng O(log V). Main loop được mô tả là `BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.`; movement là `head tăng riêng; node scan tăng 1.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `distance, queue và head.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Node xa nhất; test `n=4, edge=[[1,2],[1,3],[1,4]]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `distance, queue và head.`, condition `neighbor unseen; distance lớn hơn hay bằng current maximum.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Node xa nhất. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Trong star `1—{2,3,4}`, BFS layer 0 có node 1 và layer 1 có ba node. Maximum distance 1 xuất
hiện ba lần, nên answer 3.

```js
function farthestNodeCountBrute(n, edges) {
  const adjacency = Array.from({ length: n + 1 }, () => []);
  for (const [from, to] of edges) {
    adjacency[from].push(to);
    adjacency[to].push(from);
  }

  function distanceTo(target) {
    const queue = [[1, 0]];
    const visited = new Set([1]);
    for (let head = 0; head < queue.length; head += 1) {
      const [node, distance] = queue[head];
      if (node === target) return distance;
      for (const neighbor of adjacency[node]) {
        if (visited.has(neighbor)) continue;
        visited.add(neighbor);
        queue.push([neighbor, distance + 1]);
      }
    }
    return -1;
  }

  const distances = Array.from({ length: n }, (_, index) => distanceTo(index + 1));
  const maximum = Math.max(...distances);
  return distances.filter((distance) => distance === maximum).length;
}
```

Brute chạy BFS cho từng target. Một BFS từ source đã tính mọi distance cùng lúc.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `adjacency` | graph vô hướng | thêm cả hai chiều |
| `distance[node]` | shortest số edge từ 1 | `-1`, riêng node 1 bằng 0 |
| `queue`, `head` | BFS layer order | `[1]`, `0` |
| `maximumDistance`, `count` | argmax và multiplicity | `0`, `0` |

#### 21. Blueprint từ đề đến code

1. Graph unweighted và một source nên chọn BFS.
2. Dựng adjacency hai chiều.
3. Distance `-1` đồng thời là unseen; mark khi enqueue.
4. Sau BFS, scan node 1..n: lớn hơn thì reset count, bằng thì tăng count.
5. Không spread toàn distance vào `Math.max` khi n lớn.

#### 22. Invariant, lỗi lộ diện và recall

BFS gán distance lần đầu là shortest. Trong scan prefix node 1..i, cặp
`(maximumDistance,count)` luôn mô tả đúng maximum và số lần xuất hiện trong prefix.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| adjacency một chiều | edge input đảo orientation | graph vẫn vô hướng |
| DFS depth | graph có cycle/shortcut | shortest BFS |
| quên count node 1 | n=1 | 1 |
| chỉ giữ một farthest node | star | 3 |

Recall: **one-source unweighted distance ⇒ một BFS; rồi scan argmax + count.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số node có shortest distance lớn nhất từ node 1.
PREPARE: dựng adjacency hai chiều.
GLOBAL STATE: distance, queue và head.
INIT: distance=-1; distance[1]=0; queue=[1]; head=0.
MAIN LOOP: BFS dequeue node; inner quét neighbors; sau BFS scan node 1..n.
CURRENT ITEM: node và neighbor; phase cuối là distance[node].
PER-ITERATION STATE: maximumDistance và count ở phase scan.
CHECK: neighbor unseen; distance lớn hơn hay bằng current maximum.
BRANCH: unseen mark/enqueue; scan lớn hơn reset count, bằng thì increment.
UPDATE: distance[neighbor]=distance[node]+1; update max/count.
POINTER MOVEMENT: head tăng riêng; node scan tăng 1.
STOP / RETURN: BFS queue cạn, scan hết n thì return count.
CLEANUP: không spread distance vào Math.max; index 0 bị bỏ.
```

- Recall 1: Một BFS lấy mọi distance; scan sau để vừa tìm maximum vừa đếm tie.
- Recall 2: STATE distance/queue/max/count → LOOP BFS+scan → CHECK unseen/compare → UPDATE → RETURN count.
- Recall 3: Điền neighbor distance `distance[node] + ___`; lớn hơn max thì `count = ___`.
<!-- dry-run-reconstruction:end -->
