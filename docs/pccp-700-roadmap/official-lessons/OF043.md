# OF043 — Kiểm tra nhập cảnh

- Nguồn đề: [Programmers lesson 43238](https://school.programmers.co.kr/learn/courses/30/lessons/43238?language=javascript)
- Pattern chính: binary search on answer / first feasible
- Mức trong bank: CORE / template

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Mỗi nhân viên mất `times[i]` phút cho một người và làm liên tục. Tìm thời gian nhỏ nhất để xử lý ít nhất `n` người.

## 02. Bound

Mô phỏng từng người bằng heap là `O(n log m)` với n rất lớn. Answer nằm trên trục thời gian và predicate “xử lý đủ trong T phút” đơn điệu, nên binary search `O(m log answer)`.

## 03. Brute force

Tăng time từng phút rồi tính tổng `floor(time/times[i])` đến khi đủ. Upper bound có thể tới `10^18`, không thể scan.

## 04. Bottleneck

Ta không cần biết ai vào quầy nào; tại time T, quầy i xử lý `floor(T/times[i])`. Predicate false ở thời gian nhỏ rồi true mãi về sau.

## 05. Pattern

First feasible trên đoạn đóng:

```text
feasible(T) = sum floor(T/time_i) >= n
false ... false | true ... true
```

Nếu mid feasible giữ mid bằng `high=mid`; nếu false bỏ mid bằng `low=mid+1`.

## 06. State

- `low`, `high` BigInt;
- `middle`;
- `processed` có cutoff khi đạt target;
- durations BigInt.

## 07. Transition

Midpoint `(low+high)/2n`. Feasible ⇒ high=middle; infeasible ⇒ low=middle+1n. Loop khi `low<high`; kết thúc low là first true.

## 08. Invariant

Đáp án luôn nằm trong `[low,high]`. High ban đầu `min(times)*n` chắc chắn feasible vì quầy nhanh nhất tự xử lý đủ. Transition không loại answer: feasible mid có thể chính là answer; false mid chắc chắn nhỏ hơn answer.

## 09. Complexity

Time `O(m log(minTime*n))`, space `O(m)` do convert BigInt. BigInt tránh precision loss khi tích tới `10^18`.

## 10. Pseudocode

```text
low=0; high=min(times)*n
while low<high:
  mid=(low+high)//2
  processed=sum(mid//time)
  if processed>=n: high=mid
  else: low=mid+1
return low
```

## 11. Code bước 1 — dựng state

```js
function bounds(people, durations) {
  const target=BigInt(people);
  const fastest=durations.reduce((a,b)=>a<b?a:b);
  return [0n, fastest*target];
}
```

## 12. Code bước 2 — viết transition

```js
function canProcess(time, durations, target) {
  let count=0n;
  for (const d of durations) {
    count += time/d;
    if (count >= target) return true;
  }
  return false;
}
```

Cutoff tránh tổng tăng không cần thiết.

## 13. Code bước 3 — ghép vòng lặp

```js
function firstFeasible(low, high, predicate) {
  while (low<high) {
    const mid=(low+high)/2n;
    if (predicate(mid)) high=mid;
    else low=mid+1n;
  }
  return low;
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF043.js](../../../solutions/official/OF043.js).

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }

  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }

  let low = 0n;
  let high = fastest * target;

  while (low < high) {
    const middle = (low + high) / 2n;
    let processed = 0n;

    for (const duration of durations) {
      processed += middle / duration;
      if (processed >= target) {
        break;
      }
    }

    if (processed >= target) {
      high = middle;
    } else {
      low = middle + 1n;
    }
  }

  return low;
}

function minimumImmigrationTime(people, times) {
  return Number(minimumImmigrationTimeBigInt(people, times));
}
```

Adapter `minimumImmigrationTime` đổi kết quả sang Number cho giao diện judge; core BigInt giữ arithmetic chính xác.

## 15. Dry-run

`n=6,times=[7,10]`: T=27 xử lý `3+2=5` false; T=28 xử lý `4+2=6` true. First feasible là 28.

## 16. Edge cases

1. Một người.
2. Một quầy.
3. Nhiều quầy cùng duration.
4. Answer vượt `Number.MAX_SAFE_INTEGER` trong core arithmetic.
5. Mid feasible đúng bằng answer.

## 17. Lỗi JavaScript thường gặp

- Dùng bitwise midpoint, ép về 32-bit.
- Trộn BigInt và Number trong cùng phép toán.
- Khi feasible đặt `high=mid-1`, rồi trả sai convention.
- Upper bound không chắc feasible.
- Không floor division; BigInt `/` tự chia nguyên.

## 18. Bài transfer cùng pattern

- OF044: binary search answer với predicate remove rocks.
- OF056: shortest unweighted state dùng BFS, không binary search.

Tín hiệu: hỏi minimum/maximum số và có predicate đơn điệu kiểm được nhanh ⇒ binary search trên answer.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF043 Kiểm tra nhập cảnh

Phần này dùng chính contract, dry run, Blueprint và code executable của **Kiểm tra nhập cảnh** để dựng lại lời giải; pattern kiểm soát là `Binary-search-answer`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mỗi nhân viên mất `times[i]` phút cho một người và làm liên tục. Tìm thời gian nhỏ nhất để xử lý ít nhất `n` người.

**Giới hạn quyết định implementation:** Mô phỏng từng người bằng heap là `O(n log m)` với n rất lớn. Answer nằm trên trục thời gian và predicate “xử lý đủ trong T phút” đơn điệu, nên binary search `O(m log answer)`.

### Concrete dry run

**Concrete input:** `n=6,times=[7,10]`

`n=6,times=[7,10]`: T=27 xử lý `3+2=5` false; T=28 xử lý `4+2=6` true. First feasible là 28.



Decision ledger của **Kiểm tra nhập cảnh** nối `low và high của first-feasible closed search.` với đúng block code; mỗi inner action trong input `n=6,times=[7,10]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `n=6,times=[7,10]` | Chưa có state của Kiểm tra nhập cảnh | Contract và input đã hợp lệ | low=0n; high=minDuration*target. | low và high của first-feasible closed search. | Chưa chốt output |
| middle=(low+high)/2 và processed tại middle. đi vào | processed cộng floor(middle/duration), có thể dừng khi đủ. | Với `n=6,times=[7,10]`, hỏi: processed >= target hay predicate còn false. | feasible giữ middle bằng high=middle; false bỏ middle bằng low=middle+1n.; interval co; không mô phỏng từng người. | low và high của first-feasible closed search. sau branch vừa chọn | thời gian nhỏ nhất xử lý đủ target người. chỉ đổi ở branch ghi answer |
| Transition của current | low và high của first-feasible closed search. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | chỉ một boundary đổi mỗi iteration, interval luôn chứa answer. | low và high của first-feasible closed search. đã commit current; invariant của Kiểm tra nhập cảnh được phục hồi | thời gian nhỏ nhất xử lý đủ target người. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | mọi arithmetic core dùng BigInt, không trộn Number.; low===high thì return low (adapter đổi Number nếu an toàn). | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Kiểm tra nhập cảnh**, candidate middle của miền tìm kiếm đi vào → đọc `processed cộng floor(middle/duration), có thể dừng khi đủ.` từ state đang sống → hỏi đúng câu `processed >= target hay predicate còn false.` → đi nhánh `feasible giữ middle bằng high=middle; false bỏ middle bằng low=middle+1n.` → thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.` → loại đúng nửa đã được predicate chứng minh không thể chứa boundary → chỉ chốt `thời gian nhỏ nhất xử lý đủ target người.` ở vị trí mà invariant đã đúng → mọi arithmetic core dùng BigInt, không trộn Number. → low===high thì return low (adapter đổi Number nếu an toàn)..

Với **Kiểm tra nhập cảnh**, chuỗi “đọc middle=(low+high)/2 và processed tại middle. → hỏi processed >= target hay predicate còn false. → đổi chỉ một boundary đổi mỗi iteration, interval luôn chứa answer. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Kiểm tra nhập cảnh | `low và high của first-feasible closed search.` | State chung mà mọi current đọc hoặc mutate | `low=0n; high=minDuration*target.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `middle=(low+high)/2 và processed tại middle.` | Current của đúng iteration/candidate hiện tại | Do `while low < high.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `processed cộng floor(middle/duration), có thể dừng khi đủ.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `thời gian nhỏ nhất xử lý đủ target người.` | Contract output của Kiểm tra nhập cảnh | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const target = BigInt(people);`; lựa chọn này giữ đúng thông tin mà check `processed >= target hay predicate còn false.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `while low < high.`. Đây là lý do code dùng block bắt đầu bởi `while (low < high) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “processed >= target hay predicate còn false.”. Trong dry run `n=6,times=[7,10]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (duration < fastest) {`.
- **Tiến triển:** interval co; không mô phỏng từng người.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `const target = BigInt(people);`.
- **Cleanup:** mọi arithmetic core dùng BigInt, không trộn Number.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Kiểm tra nhập cảnh:** Predicate phải đơn điệu và interval convention phải nhất quán. Nhánh true có giữ middle hay loại middle phụ thuộc việc tìm first/last feasible; không trộn closed với half-open boundary.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Kiểm tra nhập cảnh | low và high của first-feasible closed search. | `const target = BigInt(people);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | while low < high. | `while (low < high) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | processed >= target hay predicate còn false. | `if (duration < fastest) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | chỉ một boundary đổi mỗi iteration, interval luôn chứa answer. | `durations.push(BigInt(time));` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | interval co; không mô phỏng từng người. | `const target = BigInt(people);` | Sau read/update theo invariant của Kiểm tra nhập cảnh |
| Hoàn tất và trả contract output | mọi arithmetic core dùng BigInt, không trộn Number. | `return low;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Kiểm tra nhập cảnh** là **toàn bộ `minimumImmigrationTimeBigInt` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Kiểm tra nhập cảnh:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function minimumImmigrationTimeBigInt(people, times) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Kiểm tra nhập cảnh:** Những dữ kiện nào trong `low và high của first-feasible closed search.` phải còn nguyên khi current kế tiếp tới? Block `const target = BigInt(people);` chạy đúng một lần trước traversal. Nếu đặt nó trong `while low < high.`, state nào sẽ bị reset?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Kiểm tra nhập cảnh:** Những dữ kiện nào trong `low và high của first-feasible closed search.` phải còn nguyên khi current kế tiếp tới? Block `const durations = [];` chạy đúng một lần trước traversal. Nếu đặt nó trong `while low < high.`, state nào sẽ bị reset?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
}
```

**Bước 4 — main loop và scope**

> **Prediction checkpoint — loop của Kiểm tra nhập cảnh:** Header `for (const time of times) {` lặp per item, per candidate hay tới khi state rỗng? Current `middle=(low+high)/2 và processed tại middle.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
  }
}
```

**Bước 5 — transition và movement**

> **Prediction checkpoint — transition của Kiểm tra nhập cảnh:** Nhìn các brace đang mở để quyết định `for (const time of times) {`, `durations.push(BigInt(time));` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.`, và movement `interval co; không mô phỏng từng người.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
}
```

**Bước 6 — state/initialization**

> **Prediction checkpoint — state của Kiểm tra nhập cảnh:** Những dữ kiện nào trong `low và high của first-feasible closed search.` phải còn nguyên khi current kế tiếp tới? Block `let fastest = durations[0];` chạy đúng một lần trước traversal. Nếu đặt nó trong `while low < high.`, state nào sẽ bị reset?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
}
```

**Bước 7 — main loop và scope**

> **Prediction checkpoint — loop của Kiểm tra nhập cảnh:** Header `for (const duration of durations) {` lặp per item, per candidate hay tới khi state rỗng? Current `middle=(low+high)/2 và processed tại middle.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
  }
}
```

**Bước 8 — condition/inner branch**

> **Prediction checkpoint — condition của Kiểm tra nhập cảnh:** Trước khi chạy `if (duration < fastest) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `processed >= target hay predicate còn false.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
    }
  }
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Kiểm tra nhập cảnh:** Nhìn các brace đang mở để quyết định `if (duration < fastest) {`, `fastest = duration;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.`, và movement `interval co; không mô phỏng từng người.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Kiểm tra nhập cảnh:** Nhìn các brace đang mở để quyết định `for (const duration of durations) {`, `if (duration < fastest) {`, `fastest = duration;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.`, và movement `interval co; không mô phỏng từng người.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
}
```

**Bước 11 — state/initialization**

> **Prediction checkpoint — state của Kiểm tra nhập cảnh:** Những dữ kiện nào trong `low và high của first-feasible closed search.` phải còn nguyên khi current kế tiếp tới? Block `let low = 0n;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while low < high.`, state nào sẽ bị reset?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
}
```

**Bước 12 — state/initialization**

> **Prediction checkpoint — state của Kiểm tra nhập cảnh:** Những dữ kiện nào trong `low và high của first-feasible closed search.` phải còn nguyên khi current kế tiếp tới? Block `let high = fastest * target;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while low < high.`, state nào sẽ bị reset?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
}
```

**Bước 13 — main loop và scope**

> **Prediction checkpoint — loop của Kiểm tra nhập cảnh:** Header `while (low < high) {` lặp per item, per candidate hay tới khi state rỗng? Current `middle=(low+high)/2 và processed tại middle.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
  while (low < high) {
  }
}
```

**Bước 14 — condition/inner branch**

> **Prediction checkpoint — condition của Kiểm tra nhập cảnh:** Trước khi chạy `for (const duration of durations) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `processed >= target hay predicate còn false.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
  while (low < high) {
    const middle = (low + high) / 2n;
    let processed = 0n;

    for (const duration of durations) {
    }
  }
}
```

**Bước 15 — transition và movement**

> **Prediction checkpoint — transition của Kiểm tra nhập cảnh:** Nhìn các brace đang mở để quyết định `for (const duration of durations) {`, `processed += middle / duration;`, `if (processed >= target) {`, `break;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.`, và movement `interval co; không mô phỏng từng người.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
  while (low < high) {
    const middle = (low + high) / 2n;
    let processed = 0n;

    for (const duration of durations) {
      processed += middle / duration;
      if (processed >= target) {
        break;
      }
    }
  }
}
```

**Bước 16 — transition và movement**

> **Prediction checkpoint — transition của Kiểm tra nhập cảnh:** Nhìn các brace đang mở để quyết định `while (low < high) {`, `const middle = (low + high) / 2n;`, `let processed = 0n;`, `for (const duration of durations) {`, `processed += middle / duration;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.`, và movement `interval co; không mô phỏng từng người.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
  while (low < high) {
    const middle = (low + high) / 2n;
    let processed = 0n;

    for (const duration of durations) {
      processed += middle / duration;
      if (processed >= target) {
        break;
      }
    }

    if (processed >= target) {
      high = middle;
    } else {
      low = middle + 1n;
    }
  }
}
```

**Bước 17 — cleanup/return**

> **Prediction checkpoint — finalization của Kiểm tra nhập cảnh:** Dòng `return low;` chạy per current hay đúng một lần sau khi `while low < high.` kết thúc? Trước return, cleanup `mọi arithmetic core dùng BigInt, không trộn Number.` và output `thời gian nhỏ nhất xử lý đủ target người.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function minimumImmigrationTimeBigInt(people, times) {
  const target = BigInt(people);
  const durations = [];
  for (const time of times) {
    durations.push(BigInt(time));
  }
  let fastest = durations[0];
  for (const duration of durations) {
    if (duration < fastest) {
      fastest = duration;
    }
  }
  let low = 0n;
  let high = fastest * target;
  while (low < high) {
    const middle = (low + high) / 2n;
    let processed = 0n;

    for (const duration of durations) {
      processed += middle / duration;
      if (processed >= target) {
        break;
      }
    }

    if (processed >= target) {
      high = middle;
    } else {
      low = middle + 1n;
    }
  }
  return low;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const target = BigInt(people);` | Trước traversal trong main function | low và high của first-feasible closed search. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Kiểm tra nhập cảnh |
| `middle=(low+high)/2 và processed tại middle.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (duration < fastest) {` | Sau khi state guard/current đã sẵn | Condition đọc processed cộng floor(middle/duration), có thể dừng khi đủ. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `durations.push(BigInt(time));` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi processed >= target hay predicate còn false. | Đưa ra ngoài áp dụng update cho cả case false |
| `const target = BigInt(people);` | interval co; không mô phỏng từng người. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: mọi arithmetic core dùng BigInt, không trộn Number. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return low;` | Sau finalization của main function | Output phải đạt thời gian nhỏ nhất xử lý đủ target người. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Kiểm tra nhập cảnh** dùng `n=6,times=[7,10]`: dừng ngay khi `low và high của first-feasible closed search.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `minimumImmigrationTimeBigInt` với `n=6,times=[7,10]`; theo dõi đúng output `thời gian nhỏ nhất xử lý đủ target người.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | low=0n; high=minDuration*target. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | low và high của first-feasible closed search. | middle=(low+high)/2 và processed tại middle. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | processed cộng floor(middle/duration), có thể dừng khi đủ. | chỉ một boundary đổi mỗi iteration, interval luôn chứa answer. | Đổi đúng như từng dòng dry run gốc | Condition processed >= target hay predicate còn false. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | interval co; không mô phỏng từng người. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | mọi arithmetic core dùng BigInt, không trộn Number. | low===high thì return low (adapter đổi Number nếu an toàn). | thời gian nhỏ nhất xử lý đủ target người. | Không còn transition bắt buộc nào chưa chạy |

Trong **Kiểm tra nhập cảnh**, chỉ dòng thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.` được đổi output; state còn pending theo `low và high của first-feasible closed search.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng bitwise midpoint, ép về 32-bit. | `n=6,times=[7,10]` | Lượt đầu của `middle=(low+high)/2 và processed tại middle.` mà `processed >= target hay predicate còn false.` quyết định branch | Dùng bitwise midpoint, ép về 32-bit. | `const target = BigInt(people);` ở đúng scope |
| Trộn BigInt và Number trong cùng phép toán. | `people=6` | Ngay transition đầu tiên thực hiện `chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.` trên boundary/duplicate của input | Trộn BigInt và Number trong cùng phép toán. | `durations.push(BigInt(time));` với init/strictness của Kiểm tra nhập cảnh |
| Khi feasible đặt 'high=mid-1', rồi trả sai convention. | `times=[7,10]` | Lượt đầu movement `interval co; không mô phỏng từng người.` xảy ra trước/sau sai assignment | Khi feasible đặt 'high=mid-1', rồi trả sai convention. | `const target = BigInt(people);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(m log(minTime*n))`, space `O(m)` do convert BigInt. BigInt tránh precision loss khi tích tới `10^18`.

**Proof từ operation của Kiểm tra nhập cảnh:** Mỗi decision giảm miền ứng viên ít nhất gần một nửa, nên số lượt là log₂ của độ rộng ban đầu; chi phí tổng còn nhân với chi phí predicate của một lượt. Main loop được mô tả là `while low < high.`; movement là `interval co; không mô phỏng từng người.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `low và high của first-feasible closed search.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Kiểm tra nhập cảnh; test `n=6,times=[7,10]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `low và high của first-feasible closed search.`, condition `processed >= target hay predicate còn false.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Kiểm tra nhập cảnh. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với `people=6`, `times=[7,10]`, hãy thử quanh ranh giới:

| `time` | Quầy 7 phút | Quầy 10 phút | Tổng | Feasible? |
| ---: | ---: | ---: | ---: | --- |
| 27 | 3 | 2 | 5 | Không |
| 28 | 4 | 2 | 6 | Có |

Chỉ cần chứng minh 27 false và 28 true là đã khóa được first feasible bằng 28. Brute force dưới
đây diễn tả đúng định nghĩa nhưng chỉ dùng cho input nhỏ:

```js
function minimumImmigrationTimeBrute(people, times) {
  for (let time = 0; ; time += 1) {
    let processed = 0;
    for (const duration of times) {
      processed += Math.floor(time / duration);
    }
    if (processed >= people) {
      return time;
    }
  }
}
```

Brute force quét `answer + 1` mốc. Binary search giữ nguyên predicate nhưng giảm số mốc cần thử
từ tuyến tính theo answer xuống logarithm.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo | Vì sao đúng |
| --- | --- | --- | --- |
| `target` | số người phải xử lý | `BigInt(people)` | cùng kiểu với mọi phép tính core |
| `durations` | thời gian mỗi quầy | đổi từng phần tử sang BigInt | không trộn Number và BigInt |
| `low` | đầu trái còn có thể chứa đáp án | `0n` | thời gian không âm |
| `high` | đầu phải chắc chắn feasible | `fastest * target` | quầy nhanh nhất tự xử lý đủ người |
| `processed` | số người xử lý được ở `middle` | `0n` mỗi vòng | predicate độc lập giữa các mid |

#### 21. Blueprint từ đề đến code

1. Nhìn thấy `n` rất lớn nên loại mô phỏng từng người.
2. Đặt câu hỏi quyết định: “Trong `T` phút có xử lý đủ người không?”.
3. Viết `processed = sum(T / duration)` và nhận ra predicate `false → true`.
4. Chọn convention first feasible trên đoạn đóng `[low, high]`.
5. Dựng `high` chắc chắn true bằng `fastest * target`.
6. Giữ `middle` khi true (`high = middle`), loại nó khi false (`low = middle + 1n`).
7. Dùng BigInt xuyên suốt core và chỉ đổi sang Number ở adapter khi contract cần.

#### 22. Chứng minh, lỗi lộ diện và recall

Nếu `middle` feasible thì mọi thời gian lớn hơn cũng feasible, nhưng `middle` có thể là đáp án nên
không được loại nó. Nếu `middle` false thì mọi thời gian nhỏ hơn cũng false, do đó có thể bỏ cả
đoạn tới `middle`. Mỗi transition thu hẹp interval và vẫn giữ first true; khi `low === high`, điểm
duy nhất còn lại chính là đáp án.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| high không chắc feasible | `1, [5,10]` | 5 |
| dùng `< target` sai phía ranh giới | `6, [7,10]` | 28 |
| overflow Number trong core | `1_000_000_000, [1_000_000_000]` | `10^18n` |
| quên cộng năng suất các quầy trùng nhau | `4, [2,2]` | 4 |

Recall: **minimum + predicate false-then-true ⇒ first feasible; high phải true; true giữ mid,
false bỏ mid; answer lớn thì BigInt từ đầu đến cuối.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: thời gian nhỏ nhất xử lý đủ target người.
PREPARE: chuyển target/durations sang BigInt; chọn high=fastest*target chắc chắn feasible.
GLOBAL STATE: low và high của first-feasible closed search.
INIT: low=0n; high=minDuration*target.
MAIN LOOP: while low < high.
CURRENT ITEM: middle=(low+high)/2 và processed tại middle.
PER-ITERATION STATE: processed cộng floor(middle/duration), có thể dừng khi đủ.
CHECK: processed >= target hay predicate còn false.
BRANCH: feasible giữ middle bằng high=middle; false bỏ middle bằng low=middle+1n.
UPDATE: chỉ một boundary đổi mỗi iteration, interval luôn chứa answer.
POINTER MOVEMENT: interval co; không mô phỏng từng người.
STOP / RETURN: low===high thì return low (adapter đổi Number nếu an toàn).
CLEANUP: mọi arithmetic core dùng BigInt, không trộn Number.
```

- Recall 1: Predicate đủ người false→true; high phải feasible; true giữ mid, false bỏ mid.
- Recall 2: STATE low/high → LOOP binary → CHECK processed>=target → UPDATE boundary → RETURN low.
- Recall 3: Điền `if (processed >= target) high = ___; else low = middle + ___`.
<!-- dry-run-reconstruction:end -->
