# OF006 — Không thích số giống nhau

- Nguồn đề: [Programmers lesson 12906](https://school.programmers.co.kr/learn/courses/30/lessons/12906?language=javascript)
- Pattern chính: prefix reduction / output-as-stack
- Mức trong bank: TRANSFER / warmup

## 01. Contract

Từ mảng số, xóa phần tử trùng **liên tiếp**, giữ một đại diện mỗi run và giữ thứ tự. Duplicate cách nhau phải được giữ.

## 02. Bound

Một scan `O(n)` là tối ưu vì phải đọc mọi phần tử.

## 03. Brute force

Lặp tìm cặp adjacent bằng nhau rồi splice đến khi ổn định; có thể `O(n²)` do scan/dịch mảng.

## 04. Bottleneck

Quyết định giữ current chỉ phụ thuộc phần tử cuối đã giữ, không phụ thuộc toàn output.

## 05. Pattern

Output đóng vai trò stack: top khác current hoặc output rỗng thì push; giống thì skip.

## 06. State

`reduced` là kết quả của prefix đã đọc; `reduced.at(-1)` là đại diện run cuối.

## 07. Transition

Với mỗi value: `empty || top !== value` ⇒ push; ngược lại không đổi.

## 08. Invariant

Sau mỗi phần tử, reduced bằng chính xác mảng prefix sau khi nén từng run liên tiếp thành một value.

## 09. Complexity

Time `O(n)`, output space `O(n)` worst case.

## 10. Pseudocode

```text
result=[]
for value:
  if result empty or top != value: push value
return result
```

## 11. Code bước 1 — dựng state

```js
const reduced=[];
```

## 12. Code bước 2 — viết transition

```js
function beginsNewRun(reduced,value) {
  return reduced.length===0 || reduced.at(-1)!==value;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
for (const value of array) if (beginsNewRun(reduced,value)) reduced.push(value);
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF006.js](../../../solutions/official/OF006.js).

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];

  for (const currentValue of array) {
    const outputIsEmpty = reduced.length === 0;
    if (outputIsEmpty) {
      reduced.push(currentValue);
      continue;
    }

    const previousKeptValue = reduced.at(-1);
    const beginsNewRun = previousKeptValue !== currentValue;
    if (beginsNewRun) {
      reduced.push(currentValue);
    }
  }

  return reduced;
}
```

## 15. Dry-run

`[1,1,2,1]`: push1, skip1, push2, push1 ⇒ `[1,2,1]`; Set sẽ sai thành `[1,2]`.

## 16. Edge cases

Một run dài; không duplicate; duplicate cách nhau; một phần tử.

## 17. Lỗi JavaScript thường gặp

Dùng Set; so current với input trước thay vì output top trong biến thể reduction; splice input khi iterate; quên return array.

## 18. Bài transfer cùng pattern

- OF051: top giống thì pop thay vì skip.
- OF008: prefix state là balance/opening.
- OF027: monotonic reduction có deletion budget.

Tín hiệu: output prefix chỉ cần tương tác với phần tử sống cuối cùng ⇒ output-as-stack.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF006 Không thích số giống nhau

Phần này dùng chính contract, dry run, Blueprint và code executable của **Không thích số giống nhau** để dựng lại lời giải; pattern kiểm soát là `Reduction-stack`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Từ mảng số, xóa phần tử trùng **liên tiếp**, giữ một đại diện mỗi run và giữ thứ tự. Duplicate cách nhau phải được giữ.

**Giới hạn quyết định implementation:** Một scan `O(n)` là tối ưu vì phải đọc mọi phần tử.

### Concrete dry run

**Concrete input:** `[1,1,2,1]`

`[1,1,2,1]`: push1, skip1, push2, push1 ⇒ `[1,2,1]`; Set sẽ sai thành `[1,2]`.

**Dry run chi tiết bổ sung đã có trong lesson:**

| Current | `reduced` trước | Check | Update | `reduced` sau |
| --- | --- | --- | --- | --- |
| 1 | `[]` | rỗng | push 1 | `[1]` |
| 1 | `[1]` | top bằng current | skip | `[1]` |
| 2 | `[1]` | top khác current | push 2 | `[1,2]` |
| 1 | `[1,2]` | top khác current | push 1 | `[1,2,1]` |

Mỗi input được đọc một lần và push tối đa một lần: `O(n)` time. Output lớn nhất `n` khi không
có hai phần tử kề nhau giống nhau, nên space `O(n)` tính cả output.


Decision ledger của **Không thích số giống nhau** nối `reduced` với đúng block code; mỗi inner action trong input `[1,1,2,1]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[1,1,2,1]` | Chưa có state của Không thích số giống nhau | Contract và input đã hợp lệ | reduced = [] | reduced | Chưa chốt output |
| currentValue đi vào | outputIsEmpty, previousKeptValue, beginsNewRun | Với `[1,1,2,1]`, hỏi: output rỗng? nếu không, top có khác current không? | rỗng → push+continue; khác → push; giống → skip; for...of tự tiến | reduced sau branch vừa chọn | reduced chỉ đổi ở branch ghi answer |
| Transition của current | reduced trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | reduced.push(currentValue) chỉ khi bắt đầu run | reduced đã commit current; invariant của Không thích số giống nhau được phục hồi | reduced sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không có state tồn đọng; hết input thì return reduced | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Không thích số giống nhau**, phần tử ở top/frontier hiện tại đi vào → đọc `outputIsEmpty, previousKeptValue, beginsNewRun` từ state đang sống → hỏi đúng câu `output rỗng? nếu không, top có khác current không?` → đi nhánh `rỗng → push+continue; khác → push; giống → skip` → thực hiện `reduced.push(currentValue) chỉ khi bắt đầu run` → pop/dequeue đúng phía, rồi mới push/enqueue state mới theo invariant → chỉ chốt `reduced` ở vị trí mà invariant đã đúng → không có state tồn đọng → hết input thì return reduced.

Với **Không thích số giống nhau**, chuỗi “đọc currentValue → hỏi output rỗng? nếu không, top có khác current không? → đổi reduced.push(currentValue) chỉ khi bắt đầu run → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Không thích số giống nhau | `reduced` | State chung mà mọi current đọc hoặc mutate | `reduced = []` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `currentValue` | Current của đúng iteration/candidate hiện tại | Do `for từng currentValue vì mỗi input được xét đúng một lần` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `outputIsEmpty, previousKeptValue, beginsNewRun` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `reduced` | Contract output của Không thích số giống nhau | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const reduced = [];`; lựa chọn này giữ đúng thông tin mà check `output rỗng? nếu không, top có khác current không?` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `for từng currentValue vì mỗi input được xét đúng một lần`. Đây là lý do code dùng block bắt đầu bởi `for (const currentValue of array) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “output rỗng? nếu không, top có khác current không?”. Trong dry run `[1,1,2,1]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (outputIsEmpty) {`.
- **Tiến triển:** for...of tự tiến. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `reduced.push(currentValue) chỉ khi bắt đầu run` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (const currentValue of array) {`.
- **Cleanup:** không có state tồn đọng. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Không thích số giống nhau:** Guard cấu trúc không rỗng phải đứng trước mọi lần đọc top/front. Equality có pop hay không, và visited được mark lúc enqueue hay dequeue, lấy đúng từ invariant của bài này.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Không thích số giống nhau | reduced | `const reduced = [];` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | for từng currentValue vì mỗi input được xét đúng một lần | `for (const currentValue of array) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | output rỗng? nếu không, top có khác current không? | `if (outputIsEmpty) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | reduced.push(currentValue) chỉ khi bắt đầu run | `reduced.push(currentValue);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | for...of tự tiến | `for (const currentValue of array) {` | Sau read/update theo invariant của Không thích số giống nhau |
| Hoàn tất và trả contract output | không có state tồn đọng | `return reduced;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Không thích số giống nhau** là **toàn bộ `removeConsecutiveDuplicates` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Không thích số giống nhau:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function removeConsecutiveDuplicates(array) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Không thích số giống nhau:** Những dữ kiện nào trong `reduced` phải còn nguyên khi current kế tiếp tới? Block `const reduced = [];` chạy đúng một lần trước traversal. Nếu đặt nó trong `for từng currentValue vì mỗi input được xét đúng một lần`, state nào sẽ bị reset?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
}
```

**Bước 3 — main loop và scope**

> **Prediction checkpoint — loop của Không thích số giống nhau:** Header `for (const currentValue of array) {` lặp per item, per candidate hay tới khi state rỗng? Current `currentValue` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
  for (const currentValue of array) {
  }
}
```

**Bước 4 — condition/inner branch**

> **Prediction checkpoint — condition của Không thích số giống nhau:** Trước khi chạy `if (outputIsEmpty) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `output rỗng? nếu không, top có khác current không?` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
  for (const currentValue of array) {
    const outputIsEmpty = reduced.length === 0;
    if (outputIsEmpty) {
    }
  }
}
```

**Bước 5 — transition và movement**

> **Prediction checkpoint — transition của Không thích số giống nhau:** Nhìn các brace đang mở để quyết định `if (outputIsEmpty) {`, `reduced.push(currentValue);`, `continue;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `reduced.push(currentValue) chỉ khi bắt đầu run`, và movement `for...of tự tiến` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
  for (const currentValue of array) {
    const outputIsEmpty = reduced.length === 0;
    if (outputIsEmpty) {
      reduced.push(currentValue);
      continue;
    }
  }
}
```

**Bước 6 — transition và movement**

> **Prediction checkpoint — transition của Không thích số giống nhau:** Nhìn các brace đang mở để quyết định `for (const currentValue of array) {`, `const outputIsEmpty = reduced.length === 0;`, `if (outputIsEmpty) {`, `reduced.push(currentValue);`, `continue;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `reduced.push(currentValue) chỉ khi bắt đầu run`, và movement `for...of tự tiến` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
  for (const currentValue of array) {
    const outputIsEmpty = reduced.length === 0;
    if (outputIsEmpty) {
      reduced.push(currentValue);
      continue;
    }

    const previousKeptValue = reduced.at(-1);
    const beginsNewRun = previousKeptValue !== currentValue;
    if (beginsNewRun) {
      reduced.push(currentValue);
    }
  }
}
```

**Bước 7 — cleanup/return**

> **Prediction checkpoint — finalization của Không thích số giống nhau:** Dòng `return reduced;` chạy per current hay đúng một lần sau khi `for từng currentValue vì mỗi input được xét đúng một lần` kết thúc? Trước return, cleanup `không có state tồn đọng` và output `reduced` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function removeConsecutiveDuplicates(array) {
  const reduced = [];
  for (const currentValue of array) {
    const outputIsEmpty = reduced.length === 0;
    if (outputIsEmpty) {
      reduced.push(currentValue);
      continue;
    }

    const previousKeptValue = reduced.at(-1);
    const beginsNewRun = previousKeptValue !== currentValue;
    if (beginsNewRun) {
      reduced.push(currentValue);
    }
  }
  return reduced;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const reduced = [];` | Trước traversal trong main function | reduced phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Không thích số giống nhau |
| `currentValue` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (outputIsEmpty) {` | Sau khi state guard/current đã sẵn | Condition đọc outputIsEmpty, previousKeptValue, beginsNewRun | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `reduced.push(currentValue);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi output rỗng? nếu không, top có khác current không? | Đưa ra ngoài áp dụng update cho cả case false |
| `for (const currentValue of array) {` | for...of tự tiến | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không có state tồn đọng | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return reduced;` | Sau finalization của main function | Output phải đạt reduced | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Không thích số giống nhau** dùng `[1,1,2,1]`: dừng ngay khi `reduced` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `removeConsecutiveDuplicates` với `[1,1,2,1]`; theo dõi đúng output `reduced` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | reduced = [] | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | reduced | currentValue | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | outputIsEmpty, previousKeptValue, beginsNewRun | reduced.push(currentValue) chỉ khi bắt đầu run | Đổi đúng như từng dòng dry run gốc | Condition output rỗng? nếu không, top có khác current không? vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | for...of tự tiến | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không có state tồn đọng | hết input thì return reduced | reduced | Không còn transition bắt buộc nào chưa chạy |

Trong **Không thích số giống nhau**, chỉ dòng thực hiện `reduced.push(currentValue) chỉ khi bắt đầu run` được đổi output; state còn pending theo `reduced` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng Set | `[1,1,2,1]` | Lượt đầu của `currentValue` mà `output rỗng? nếu không, top có khác current không?` quyết định branch | Dùng Set | `const reduced = [];` ở đúng scope |
| so current với input trước thay vì output top trong biến thể reduction | `[1,1,2,2,1]` | Ngay transition đầu tiên thực hiện `reduced.push(currentValue) chỉ khi bắt đầu run` trên boundary/duplicate của input | so current với input trước thay vì output top trong biến thể reduction | `reduced.push(currentValue);` với init/strictness của Không thích số giống nhau |
| splice input khi iterate | `[1,2,1]` | Lượt đầu movement `for...of tự tiến` xảy ra trước/sau sai assignment | splice input khi iterate | `reduced.push(currentValue);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n)`, output space `O(n)` worst case.

**Proof từ operation của Không thích số giống nhau:** Đếm số lần một item vào và rời cấu trúc: mỗi item chỉ được push/enqueue theo transition của bài và chỉ được pop/dequeue khi đã hoàn tất vai trò. Nested while không tự động là bình phương nếu item đã rời không quay lại. Main loop được mô tả là `for từng currentValue vì mỗi input được xét đúng một lần`; movement là `for...of tự tiến`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `reduced`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Không thích số giống nhau; test `[1,1,2,1]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `reduced`, condition `output rỗng? nếu không, top có khác current không?`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Không thích số giống nhau. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Tự đi từ đề đến code

##### Làm bằng tay

Với `[1,1,2,2,1]`, đừng nghĩ “xóa mọi số trùng”. Hãy đọc lần lượt:

1. output đang rỗng nên giữ số `1` đầu tiên;
2. số `1` tiếp theo giống số vừa giữ nên bỏ qua;
3. số `2` khác số vừa giữ nên bắt đầu một run mới;
4. số `2` tiếp theo thuộc cùng run nên bỏ qua;
5. số `1` cuối khác top `2`, vì vậy **phải giữ**. Kết quả là `[1,2,1]`.

Điểm quan trọng: duplicate cách nhau không bị xóa. Vì vậy `Set` không đúng contract.

##### Brute force đầy đủ

```js
function removeConsecutiveDuplicatesBruteForce(array) {
  const copied = array.slice();
  let index = 1;

  while (index < copied.length) {
    const previousValue = copied[index - 1];
    const currentValue = copied[index];

    if (previousValue === currentValue) {
      copied.splice(index, 1);
      // Không tăng index: sau splice, phần tử mới vừa trượt vào index này
      // vẫn phải được so với đại diện của run.
      continue;
    }

    index++;
  }

  return copied;
}
```

Brute force đúng vì mỗi lần splice chỉ xóa một phần tử có đại diện bằng nó ngay bên trái.
Nhưng `splice` phải dịch suffix; một run dài có thể làm tổng chi phí thành `O(n²)`. Thông tin
cần giữ thực ra chỉ là phần tử cuối đã đưa vào output.

##### State, init và scope

| State | Kiểu dữ liệu | Lưu chính xác cái gì | Tại sao cần | Init | Vì sao init như vậy | Scope |
| --- | --- | --- | --- | --- | --- | --- |
| `reduced` | number[] | kết quả đúng của prefix đã đọc | vừa là output, vừa cho biết run cuối | `[]` | chưa đọc phần tử nào | xuyên loop |
| `currentValue` | number | phần tử input đang xét | quyết định giữ hay bỏ | từ `for...of` | mỗi lượt đọc lại | iteration |
| `previousKeptValue` | number | đại diện run cuối | so với current | `reduced.at(-1)` sau empty check | không được đọc khi rỗng | iteration |
| `beginsNewRun` | boolean | current có mở run mới không | làm branch dễ đọc | phép so sánh | tính lại mỗi lượt | iteration |

Quy tắc scope hiện rõ: `reduced` phải sống sang lượt sau nên nằm ngoài loop; ba biến mô tả
current/check chỉ dùng trong một lượt nên nằm trong loop.

##### Một iteration và invariant

1. lấy `currentValue`;
2. nếu output rỗng, push current rồi `continue`;
3. đọc `previousKeptValue` từ top output;
4. nếu hai value khác nhau, push current;
5. nếu bằng nhau, không cập nhật gì;
6. `for...of` tự chuyển sang phần tử tiếp theo.

Sau mỗi iteration, `reduced` bằng chính xác prefix đã đọc sau khi nén mỗi run còn một đại diện.
Push khi rỗng tạo đại diện đầu; push khi khác mở run mới; skip khi giống không làm mất run nào.

##### Code Blueprint

```text
OUTPUT: reduced
PREPARE: không mutate input
GLOBAL STATE: reduced
INIT: reduced = []
MAIN LOOP: for từng currentValue vì mỗi input được xét đúng một lần
CURRENT ITEM: currentValue
PER-ITERATION STATE: outputIsEmpty, previousKeptValue, beginsNewRun
CHECK: output rỗng? nếu không, top có khác current không?
BRANCH: rỗng → push+continue; khác → push; giống → skip
UPDATE: reduced.push(currentValue) chỉ khi bắt đầu run
POINTER MOVEMENT: for...of tự tiến
STOP / RETURN: hết input thì return reduced
CLEANUP: không có state tồn đọng
```

`Contract → output array`; `State → reduced`; `một lượt → for...of`; `Invariant → output là
prefix đã nén`; `Transition → hai branch push/skip`; `Stop → hết input`; `Cleanup → không có`.

##### Dry run nhìn thấy state

| Current | `reduced` trước | Check | Update | `reduced` sau |
| --- | --- | --- | --- | --- |
| 1 | `[]` | rỗng | push 1 | `[1]` |
| 1 | `[1]` | top bằng current | skip | `[1]` |
| 2 | `[1]` | top khác current | push 2 | `[1,2]` |
| 1 | `[1,2]` | top khác current | push 1 | `[1,2,1]` |

Mỗi input được đọc một lần và push tối đa một lần: `O(n)` time. Output lớn nhất `n` khi không
có hai phần tử kề nhau giống nhau, nên space `O(n)` tính cả output.

##### Lỗi thường gặp và test lộ lỗi

- Logic — dùng `Set`: `[1,1,2,1]` phải ra `[1,2,1]`, không phải `[1,2]`.
- Off-by-one — bắt đầu loop ở phần tử thứ hai nhưng quên seed output: `[7]` bị trả `[]`.
- Check/update — đọc top rồi push trước khi so: `[1,1]` bị giữ cả hai.
- Init — seed bằng `array[0]` mà không xử lý empty: `[]` tạo `[undefined]`.
- Hidden — một run dài: `[3,3,3,3] → [3]`.
- Performance — `splice` trong loop trên hàng chục nghìn phần tử giống nhau có thể quadratic.

##### Recall và template 30 phút

```text
Recall 1: output rỗng hoặc current khác top → push; ngược lại skip.
Recall 2: STATE reduced; LOOP values; CHECK empty/top; UPDATE push; RETURN reduced.
Recall 3: if (reduced.length === ____ || reduced.at(-1) ____ currentValue) { ____; }

OUT: array nén run; LIMIT: phải đọc n phần tử; HAND: giữ đại diện đầu mỗi run
BRUTE: splice duplicate adjacent; SLOW: dịch suffix; PATTERN: output-as-stack
STATE + INIT: reduced=[]; LOOP + INVARIANT: output là prefix đã nén
UPDATE + STOP: push khi run mới, hết input return
```
<!-- dry-run-reconstruction:end -->
