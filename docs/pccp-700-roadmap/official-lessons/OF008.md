# OF008 — Dấu ngoặc đúng

- Nguồn đề: [Programmers lesson 12909](https://school.programmers.co.kr/learn/courses/30/lessons/12909?language=javascript)
- Pattern chính: prefix balance (stack compression)
- Mức trong bank: CORE / template

## 01. Contract

String chỉ gồm `(` và `)`. Hợp lệ khi mọi dấu đóng ghép được với một dấu mở trước nó và cuối cùng không còn dấu mở.

## 02. Bound

Một scan `O(n)`; chỉ một loại ngoặc nên stack có thể nén thành counter.

## 03. Brute force

Lặp replace `()` đến khi không đổi rồi kiểm rỗng; tạo string mới nhiều lần, có thể quadratic.

## 04. Bottleneck

Ta chỉ cần số opening chưa đóng, không cần vị trí/type vì chỉ có một loại.

## 05. Pattern

`(` tăng balance, `)` giảm. Prefix balance âm ⇒ close không có opening; cuối balance phải 0.

## 06. State

`balance` = số opening chưa ghép trong prefix.

## 07. Transition

Open `+1`, close `-1`; return false ngay khi âm; cuối `balance===0`.

## 08. Invariant

Sau prefix, balance = opens - closes của prefix và nếu chưa return thì không prefix nào âm. Đây chính là số opening chưa đóng.

## 09. Complexity

Time `O(n)`, space `O(1)`.

## 10. Pseudocode

```text
balance=0
for char:
  open? balance++ : balance--
  if balance<0 return false
return balance==0
```

## 11. Code bước 1 — dựng state

```js
let balance=0;
```

## 12. Code bước 2 — viết transition

```js
balance += character==="(" ? 1 : -1;
```

## 13. Code bước 3 — ghép vòng lặp

```js
function prefixIsStillValid(balance) {
  return balance >= 0;
}
```

Trong vòng lặp, nếu helper trả `false` thì return `false` khỏi function chính. Đây là fail sớm ngay prefix invalid.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF008.js](../../../solutions/official/OF008.js).

```js
function isValidParentheses(string) {
  let balance=0;
  for (const ch of string) {
    balance += ch==="(" ? 1 : -1;
    if (balance<0) return false;
  }
  return balance===0;
}
```

## 15. Dry-run

`)()(`: ký tự đầu làm balance -1, false ngay; dù tổng cuối có thể zero ở string khác, prefix rule vẫn bắt buộc.

## 16. Edge cases

Close đầu; opens dư cuối; nested; alternating; chuỗi ngắn nhất theo constraint.

## 17. Lỗi JavaScript thường gặp

Chỉ so tổng count; return true khi balance chưa âm nhưng cuối >0; dùng stack nhưng pop empty không check; regex replace loop.

## 18. Bài transfer cùng pattern

- SR003: ba loại bracket cần stack thật.
- OF051: stack reduction theo type doll.
- OF006: output top để nén run.

Tín hiệu: một loại ngoặc ⇒ stack content đồng nhất, nén được thành balance nhưng vẫn giữ prefix invariant.
