# OF020 — Tìm số nguyên tố

- Nguồn đề: [Programmers lesson 42839](https://school.programmers.co.kr/learn/courses/30/lessons/42839?language=javascript)
- Pattern chính: permutation backtracking + Set + primality
- Mức trong bank: TRANSFER / variant

## 01. Contract

Từ các chữ số trong string, chọn một hoặc nhiều occurrence và sắp thứ tự để tạo số. Đếm bao nhiêu giá trị số nguyên tố khác nhau. Leading zero có thể tạo cùng numeric value.

## 02. Bound

Số digit rất nhỏ nên tổng partial permutations đủ để DFS. Mỗi candidate kiểm prime tới căn bậc hai.

## 03. Brute force

Sinh mọi permutation cho mọi length rồi kiểm prime. Đây chính là hướng phù hợp theo bound, nhưng phải tránh dùng lại cùng index và deduplicate value.

## 04. Bottleneck

Duplicate digit và leading zero tạo cùng number nhiều lần. Backtracking theo index bảo vệ occurrence; Set numeric value loại kết quả trùng.

## 05. Pattern

Ở mọi prefix, thử từng digit chưa dùng; thêm `Number(next)` vào Set; recurse; restore visited. Sau enumeration, filter prime.

## 06. State

`current` string, `used[index]`, `candidates:Set<number>`; primality helper.

## 07. Transition

Mark index, nối digit, emit candidate, recurse, unmark. Prime check: number≥2 và không có divisor ≤sqrt(number).

## 08. Invariant

Path DFS dùng đúng các index marked một lần. Set chứa mọi numeric value của prefix đã emit, không duplicate. Restore làm sibling nhận state sạch.

## 09. Complexity

Enumeration `O(Σ P(n,k))`; mỗi unique candidate prime test `O(sqrt(value))`; recursion/visited `O(n)`, Set theo số candidate.

## 10. Pseudocode

```text
dfs(current):
  for each unused digit index:
    mark; next=current+digit
    add Number(next)
    dfs(next)
    unmark
count candidates that are prime
```

## 11. Code bước 1 — dựng state

```js
const used=Array(digits.length).fill(false);
const candidates=new Set();
```

## 12. Code bước 2 — viết transition

```js
function isPrime(number) {
  if (number<2) return false;
  for (let divisor=2;divisor*divisor<=number;divisor++)
    if (number%divisor===0) return false;
  return true;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
used[index]=true;
const next=current+digits[index];
candidates.add(Number(next));
dfs(next);
used[index]=false;
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF020.js](../../../solutions/official/OF020.js).

```js
function isPrime(number) {
  if (number < 2) {
    return false;
  }
  if (number % 2 === 0) {
    return number === 2;
  }
  for (let divisor = 3; divisor * divisor <= number; divisor += 2) {
    if (number % divisor === 0) {
      return false;
    }
  }
  return true;
}

function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();

  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }

  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
    if (isPrime(candidate)) {
      answer += 1;
    }
  }
  return answer;
}
```

## 15. Dry-run

`"011"` sinh string `"011"`, `"11"`, ...; `Number("011")` và `Number("11")` cùng 11 nên Set đếm một. Prime distinct là 11 và 101, answer 2.

## 16. Edge cases

Toàn zero; duplicate digits; number 1; leading zero; prime hai chữ số từ đảo order.

## 17. Lỗi JavaScript thường gặp

Visited theo digit value làm mất duplicate occurrence; coi 1 là prime; test divisor tới number; Set string không gộp leading zero; quên restore.

## 18. Bài transfer cùng pattern

- OF022: permutation dungeon với feasibility pruning.
- OF024: preorder enumeration không cần visited.
- OF036: fixed two choices per index.

Tín hiệu: dùng subset occurrence theo mọi order, n nhỏ ⇒ partial-permutation backtracking; deduplicate output riêng.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF020 Tìm số nguyên tố

Phần này dùng chính contract, dry run, Blueprint và code executable của **Tìm số nguyên tố** để dựng lại lời giải; pattern kiểm soát là `Permutation-backtracking`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Từ các chữ số trong string, chọn một hoặc nhiều occurrence và sắp thứ tự để tạo số. Đếm bao nhiêu giá trị số nguyên tố khác nhau. Leading zero có thể tạo cùng numeric value.

**Giới hạn quyết định implementation:** Số digit rất nhỏ nên tổng partial permutations đủ để DFS. Mỗi candidate kiểm prime tới căn bậc hai.

### Concrete dry run

**Concrete input:** `Number("011")`

`"011"` sinh string `"011"`, `"11"`, ...; `Number("011")` và `Number("11")` cùng 11 nên Set đếm một. Prime distinct là 11 và 101, answer 2.



Decision ledger của **Tìm số nguyên tố** nối `usedByIndex và candidates; prime count được tính sau enumeration.` với đúng block code; mỗi inner action trong input `Number("011")` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `Number("011")` | Chưa có state của Tìm số nguyên tố | Contract và input đã hợp lệ | used false; Set rỗng; prefix rỗng. | usedByIndex và candidates; prime count được tính sau enumeration. | Chưa chốt output |
| digit ở candidateIndex và nextText = prefix + digit. đi vào | candidateIndex/nextText của branch hiện tại. | Với `Number("011")`, hỏi: index chưa used; sau enumeration candidate có prime không. | used thì continue; chưa used thì choose→emit→recurse→restore.; for candidateIndex; recursion tăng prefix length. | usedByIndex và candidates; prime count được tính sau enumeration. sau branch vừa chọn | số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit. chỉ đổi ở branch ghi answer |
| Transition của current | usedByIndex và candidates; prime count được tính sau enumeration. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | used true, Set.add(Number(nextText)), recurse, used false. | usedByIndex và candidates; prime count được tính sau enumeration. đã commit current; invariant của Tìm số nguyên tố được phục hồi | số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | restore used ngay sau recursive return; Number gộp leading zero.; DFS dừng khi không còn choice; sau scan Set trả primeCount. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Tìm số nguyên tố**, choice tại depth/index hiện tại đi vào → đọc `candidateIndex/nextText của branch hiện tại.` từ state đang sống → hỏi đúng câu `index chưa used; sau enumeration candidate có prime không.` → đi nhánh `used thì continue; chưa used thì choose→emit→recurse→restore.` → thực hiện `used true, Set.add(Number(nextText)), recurse, used false.` → chọn một candidate, recurse sang depth kế tiếp, rồi hoàn tác đúng mutation trước khi thử candidate tiếp → chỉ chốt `số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit.` ở vị trí mà invariant đã đúng → restore used ngay sau recursive return; Number gộp leading zero. → DFS dừng khi không còn choice; sau scan Set trả primeCount..

Với **Tìm số nguyên tố**, chuỗi “đọc digit ở candidateIndex và nextText = prefix + digit. → hỏi index chưa used; sau enumeration candidate có prime không. → đổi used true, Set.add(Number(nextText)), recurse, used false. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Tìm số nguyên tố | `usedByIndex và candidates; prime count được tính sau enumeration.` | State chung mà mọi current đọc hoặc mutate | `used false; Set rỗng; prefix rỗng.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `digit ở candidateIndex và nextText = prefix + digit.` | Current của đúng iteration/candidate hiện tại | Do `DFS; tại mỗi depth thử mọi digit index chưa dùng.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `candidateIndex/nextText của branch hiện tại.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit.` | Contract output của Tìm số nguyên tố | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const digits = [...numbers];`; lựa chọn này giữ đúng thông tin mà check `index chưa used; sau enumeration candidate có prime không.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `DFS; tại mỗi depth thử mọi digit index chưa dùng.`. Đây là lý do code dùng block bắt đầu bởi `function dfs(current) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “index chưa used; sau enumeration candidate có prime không.”. Trong dry run `Number("011")`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (used[index]) {`.
- **Tiến triển:** for candidateIndex; recursion tăng prefix length.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `used true, Set.add(Number(nextText)), recurse, used false.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (let index = 0; index < digits.length; index++) {`.
- **Cleanup:** restore used ngay sau recursive return; Number gộp leading zero.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Tìm số nguyên tố:** Base case phải hỏi đúng lúc partial solution đã đủ. Guard candidate chạy trước choose; undo phải đối xứng với choose và nằm sau lời gọi đệ quy trong cùng branch.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Tìm số nguyên tố | usedByIndex và candidates; prime count được tính sau enumeration. | `const digits = [...numbers];` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | DFS; tại mỗi depth thử mọi digit index chưa dùng. | `function dfs(current) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | index chưa used; sau enumeration candidate có prime không. | `if (used[index]) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | used true, Set.add(Number(nextText)), recurse, used false. | `for (let index = 0; index < digits.length; index++) {` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | for candidateIndex; recursion tăng prefix length. | `for (let index = 0; index < digits.length; index++) {` | Sau read/update theo invariant của Tìm số nguyên tố |
| Hoàn tất và trả contract output | restore used ngay sau recursive return; Number gộp leading zero. | `return answer;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Tìm số nguyên tố** là **toàn bộ `primePermutationCount` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Tìm số nguyên tố:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function primePermutationCount(numbers) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Tìm số nguyên tố:** Những dữ kiện nào trong `usedByIndex và candidates; prime count được tính sau enumeration.` phải còn nguyên khi current kế tiếp tới? Block `const digits = [...numbers];` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS; tại mỗi depth thử mọi digit index chưa dùng.`, state nào sẽ bị reset?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Tìm số nguyên tố:** Những dữ kiện nào trong `usedByIndex và candidates; prime count được tính sau enumeration.` phải còn nguyên khi current kế tiếp tới? Block `const used = Array(digits.length).fill(false);` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS; tại mỗi depth thử mọi digit index chưa dùng.`, state nào sẽ bị reset?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Tìm số nguyên tố:** Những dữ kiện nào trong `usedByIndex và candidates; prime count được tính sau enumeration.` phải còn nguyên khi current kế tiếp tới? Block `const candidates = new Set();` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS; tại mỗi depth thử mọi digit index chưa dùng.`, state nào sẽ bị reset?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Tìm số nguyên tố:** Những dữ kiện nào trong `usedByIndex và candidates; prime count được tính sau enumeration.` phải còn nguyên khi current kế tiếp tới? Block `function dfs(current) {`, `for (let index = 0; index < digits.length; index++) {`, `if (used[index]) {`, `continue;`, `used[index] = true;` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS; tại mỗi depth thử mọi digit index chưa dùng.`, state nào sẽ bị reset?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
}
```

**Bước 6 — transition và movement**

> **Prediction checkpoint — transition của Tìm số nguyên tố:** Nhìn các brace đang mở để quyết định `dfs("");` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `used true, Set.add(Number(nextText)), recurse, used false.`, và movement `for candidateIndex; recursion tăng prefix length.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
}
```

**Bước 7 — state/initialization**

> **Prediction checkpoint — state của Tìm số nguyên tố:** Những dữ kiện nào trong `usedByIndex và candidates; prime count được tính sau enumeration.` phải còn nguyên khi current kế tiếp tới? Block `let answer = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS; tại mỗi depth thử mọi digit index chưa dùng.`, state nào sẽ bị reset?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
}
```

**Bước 8 — main loop và scope**

> **Prediction checkpoint — loop của Tìm số nguyên tố:** Header `for (const candidate of candidates) {` lặp per item, per candidate hay tới khi state rỗng? Current `digit ở candidateIndex và nextText = prefix + digit.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
  }
}
```

**Bước 9 — condition/inner branch**

> **Prediction checkpoint — condition của Tìm số nguyên tố:** Trước khi chạy `if (isPrime(candidate)) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `index chưa used; sau enumeration candidate có prime không.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
    if (isPrime(candidate)) {
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Tìm số nguyên tố:** Nhìn các brace đang mở để quyết định `if (isPrime(candidate)) {`, `answer += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `used true, Set.add(Number(nextText)), recurse, used false.`, và movement `for candidateIndex; recursion tăng prefix length.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
    if (isPrime(candidate)) {
      answer += 1;
    }
  }
}
```

**Bước 11 — transition và movement**

> **Prediction checkpoint — transition của Tìm số nguyên tố:** Nhìn các brace đang mở để quyết định `for (const candidate of candidates) {`, `if (isPrime(candidate)) {`, `answer += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `used true, Set.add(Number(nextText)), recurse, used false.`, và movement `for candidateIndex; recursion tăng prefix length.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
    if (isPrime(candidate)) {
      answer += 1;
    }
  }
}
```

**Bước 12 — cleanup/return**

> **Prediction checkpoint — finalization của Tìm số nguyên tố:** Dòng `return answer;` chạy per current hay đúng một lần sau khi `DFS; tại mỗi depth thử mọi digit index chưa dùng.` kết thúc? Trước return, cleanup `restore used ngay sau recursive return; Number gộp leading zero.` và output `số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function primePermutationCount(numbers) {
  const digits = [...numbers];
  const used = Array(digits.length).fill(false);
  const candidates = new Set();
  function dfs(current) {
    for (let index = 0; index < digits.length; index++) {
      if (used[index]) {
        continue;
      }
      used[index] = true;
      const next = current + digits[index];
      candidates.add(Number(next));
      dfs(next);
      used[index] = false;
    }
  }
  dfs("");
  let answer = 0;
  for (const candidate of candidates) {
    if (isPrime(candidate)) {
      answer += 1;
    }
  }
  return answer;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const digits = [...numbers];` | Trước traversal trong main function | usedByIndex và candidates; prime count được tính sau enumeration. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Tìm số nguyên tố |
| `digit ở candidateIndex và nextText = prefix + digit.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (used[index]) {` | Sau khi state guard/current đã sẵn | Condition đọc candidateIndex/nextText của branch hiện tại. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `for (let index = 0; index < digits.length; index++) {` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi index chưa used; sau enumeration candidate có prime không. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (let index = 0; index < digits.length; index++) {` | for candidateIndex; recursion tăng prefix length. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: restore used ngay sau recursive return; Number gộp leading zero. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return answer;` | Sau finalization của main function | Output phải đạt số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Tìm số nguyên tố** dùng `Number("011")`: dừng ngay khi `usedByIndex và candidates; prime count được tính sau enumeration.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `primePermutationCount` với `Number("011")`; theo dõi đúng output `số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | used false; Set rỗng; prefix rỗng. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | usedByIndex và candidates; prime count được tính sau enumeration. | digit ở candidateIndex và nextText = prefix + digit. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | candidateIndex/nextText của branch hiện tại. | used true, Set.add(Number(nextText)), recurse, used false. | Đổi đúng như từng dòng dry run gốc | Condition index chưa used; sau enumeration candidate có prime không. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | for candidateIndex; recursion tăng prefix length. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | restore used ngay sau recursive return; Number gộp leading zero. | DFS dừng khi không còn choice; sau scan Set trả primeCount. | số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit. | Không còn transition bắt buộc nào chưa chạy |

Trong **Tìm số nguyên tố**, chỉ dòng thực hiện `used true, Set.add(Number(nextText)), recurse, used false.` được đổi output; state còn pending theo `usedByIndex và candidates; prime count được tính sau enumeration.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Visited theo digit value làm mất duplicate occurrence | `"011"` | Lượt đầu của `digit ở candidateIndex và nextText = prefix + digit.` mà `index chưa used; sau enumeration candidate có prime không.` quyết định branch | Visited theo digit value làm mất duplicate occurrence | `if (used[index]) {` ở đúng scope |
| coi 1 là prime | `"11"` | Ngay transition đầu tiên thực hiện `used true, Set.add(Number(nextText)), recurse, used false.` trên boundary/duplicate của input | coi 1 là prime | `for (let index = 0; index < digits.length; index++) {` với init/strictness của Tìm số nguyên tố |
| test divisor tới number | `""` | Lượt đầu movement `for candidateIndex; recursion tăng prefix length.` xảy ra trước/sau sai assignment | test divisor tới number | `for (let index = 0; index < digits.length; index++) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Enumeration `O(Σ P(n,k))`; mỗi unique candidate prime test `O(sqrt(value))`; recursion/visited `O(n)`, Set theo số candidate.

**Proof từ operation của Tìm số nguyên tố:** Mỗi node của cây lựa chọn tương ứng một partial solution thực sự được tạo. Thời gian là số node/nhánh được duyệt nhân chi phí kiểm tra; space là độ sâu recursion cộng state đang chọn. Main loop được mô tả là `DFS; tại mỗi depth thử mọi digit index chưa dùng.`; movement là `for candidateIndex; recursion tăng prefix length.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `usedByIndex và candidates; prime count được tính sau enumeration.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Tìm số nguyên tố; test `Number("011")` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `usedByIndex và candidates; prime count được tính sau enumeration.`, condition `index chưa used; sau enumeration candidate có prime không.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Tìm số nguyên tố. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với `"011"`, index là ba occurrence riêng. Các prefix numeric gồm 0, 1, 10, 11, 101, 110,
...; string `"011"` và `"11"` cùng thành number 11 nên chỉ được đếm một lần.

```js
function primePermutationCountBrute(numbers) {
  const digits = [...numbers];
  const candidates = new Set();

  function permute(remaining, current) {
    if (current.length > 0) candidates.add(Number(current));
    for (let index = 0; index < remaining.length; index += 1) {
      const nextRemaining = remaining.slice();
      const [digit] = nextRemaining.splice(index, 1);
      permute(nextRemaining, current + digit);
    }
  }

  permute(digits, "");
  let count = 0;
  for (const value of candidates) {
    if (isPrime(value)) count += 1;
  }
  return count;
}
```

Brute copy/splice array ở mỗi nhánh. Bản tối ưu giữ một `used` array và restore tại chỗ.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `current` | chuỗi digit của prefix | `""` |
| `used[index]` | occurrence đã nằm trong prefix | toàn false |
| `candidates` | numeric value distinct đã sinh | Set rỗng |
| `divisor` | ước lẻ đang kiểm tra | 3, tăng 2 tới căn bậc hai |

#### 21. Blueprint từ đề đến code

1. Bound nhỏ cho phép enumerate mọi partial permutation.
2. Identity của choice là index, không phải digit value.
3. Ở mỗi choice: mark → nối digit → emit candidate → recurse → unmark.
4. Chuyển prefix sang Number trước khi đưa vào Set để gộp leading zero.
5. Sau enumeration, kiểm prime từng candidate distinct tới `sqrt(value)`.

#### 22. Invariant, lỗi lộ diện và recall

Trong `dfs(current)`, các index marked chính xác là occurrences tạo nên `current`, mỗi index xuất hiện
tối đa một lần. Restore sau child khiến sibling bắt đầu từ cùng prefix. Set numeric giữ mỗi kết quả
đúng một lần dù có nhiều đường sinh.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| visited theo digit value | `"11"` | vẫn dùng được cả hai occurrence |
| Set string | `"011"` | 2 prime distinct |
| coi 1 là prime | `"1"` | 0 |
| quên emit prefix ngắn | `"17"` | gồm 7, 17, 71 |

Recall: **partial permutation dùng visited theo index; deduplicate output bằng numeric Set; restore sau recurse.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số integer prime khác nhau tạo từ một phần hoặc toàn bộ digit.
PREPARE: giữ digits theo index; tạo used boolean và candidate Set.
GLOBAL STATE: usedByIndex và candidates; prime count được tính sau enumeration.
INIT: used false; Set rỗng; prefix rỗng.
MAIN LOOP: DFS; tại mỗi depth thử mọi digit index chưa dùng.
CURRENT ITEM: digit ở candidateIndex và nextText = prefix + digit.
PER-ITERATION STATE: candidateIndex/nextText của branch hiện tại.
CHECK: index chưa used; sau enumeration candidate có prime không.
BRANCH: used thì continue; chưa used thì choose→emit→recurse→restore.
UPDATE: used true, Set.add(Number(nextText)), recurse, used false.
POINTER MOVEMENT: for candidateIndex; recursion tăng prefix length.
STOP / RETURN: DFS dừng khi không còn choice; sau scan Set trả primeCount.
CLEANUP: restore used ngay sau recursive return; Number gộp leading zero.
```

- Recall 1: DFS partial permutation theo index, emit mọi prefix vào numeric Set, restore rồi kiểm prime distinct.
- Recall 2: STATE used/Set → LOOP DFS choices → CHECK unused/prime → UPDATE mark-add-recurse-unmark → RETURN count.
- Recall 3: Điền `used[candidateIndex] = true; candidates.add(___(nextText)); ... used[candidateIndex] = ___`.
<!-- dry-run-reconstruction:end -->
