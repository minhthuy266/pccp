# OF014 — Hai hàng đợi ưu tiên

- Nguồn đề: [Programmers lesson 42628](https://school.programmers.co.kr/learn/courses/30/lessons/42628?language=javascript)
- Pattern chính: min/max heap + lazy deletion
- Mức trong bank: TRANSFER / variant

## 01. Contract

Xử lý lệnh insert, delete maximum, delete minimum. Delete trên cấu trúc rỗng bị bỏ qua. Cuối trả `[max,min]`, hoặc `[0,0]` nếu rỗng. Duplicate value là các occurrence riêng.

## 02. Bound

Sort array sau mỗi command hoặc splice cực trị là tốn kém. Hai heap cho hai cực trị `O(log n)` nhưng phải đồng bộ deletion.

## 03. Brute force

Giữ array, insert push, delete sort rồi shift/pop. Đúng nhưng sort lặp `O(q² log q)` worst case.

## 04. Bottleneck

Xóa từ min heap không thể xóa occurrence tương ứng trong max heap nhanh. Gán unique id cho mỗi insert và đánh dấu alive; heap kia bỏ entry chết khi nó nổi lên root.

## 05. Pattern

Dual heap lazy deletion:

```text
insert same {value,id} into both heaps; alive[id]=true
delete one root after cleaning; alive[id]=false
clean(heap): pop while root id dead
```

## 06. State

MinHeap, MaxHeap, `alive[id]`, `liveCount`, `nextId`.

## 07. Transition

Insert tăng liveCount. Delete nếu liveCount>0: clean heap tương ứng, pop, mark dead, giảm count. Cuối clean cả hai trước peek.

## 08. Invariant

Mỗi live occurrence có một entry trong cả hai heap; `alive` là nguồn sự thật. Sau clean, root là cực trị trong tập live. Unique id giữ duplicate độc lập.

## 09. Complexity

Mỗi entry push hai lần và pop tối đa một lần khỏi mỗi heap: amortized `O(q log q)`, space `O(q)`.

## 10. Pseudocode

```text
for operation:
  insert: create id, push both, alive true
  delete if nonempty:
    choose heap; clean dead roots; pop; mark dead
if empty return [0,0]
clean both; return [maxRoot,minRoot]
```

## 11. Code bước 1 — dựng state

```js
const minHeap=new NumericHeap((a,b)=>a.value-b.value);
const maxHeap=new NumericHeap((a,b)=>b.value-a.value);
```

## 12. Code bước 2 — viết transition

```js
function clean(heap,alive) {
  while (heap.size && !alive[heap.peek().id]) heap.pop();
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
const entry={value,id:nextId++};
alive[entry.id]=true; minHeap.push(entry); maxHeap.push(entry);
```

## 14. Code hoàn chỉnh

Code executable gồm heap đầy đủ: [solutions/official/OF014.js](../../../solutions/official/OF014.js).

```js
class NumericHeap {
  constructor(compare) {
    this.values = [];
    this.compare = compare;
  }
  get size() {
    return this.values.length;
  }
  peek() {
    return this.values[0];
  }
  push(value) {
    const heap = this.values;
    heap.push(value);
    let child = heap.length - 1;
    while (child > 0) {
      const parent = Math.floor((child - 1) / 2);
      if (this.compare(heap[parent], heap[child]) <= 0) {
        break;
      }
      [heap[parent], heap[child]] = [heap[child], heap[parent]];
      child = parent;
    }
  }
  pop() {
    const heap = this.values;
    if (heap.length === 0) {
      return undefined;
    }
    if (heap.length === 1) {
      return heap.pop();
    }
    const root = heap[0];
    heap[0] = heap.pop();
    let parent = 0;
    while (true) {
      const left = parent * 2 + 1;
      const right = left + 1;
      let best = parent;
      if (left < heap.length && this.compare(heap[left], heap[best]) < 0) {
        best = left;
      }
      if (right < heap.length && this.compare(heap[right], heap[best]) < 0) {
        best = right;
      }
      if (best === parent) {
        break;
      }
      [heap[parent], heap[best]] = [heap[best], heap[parent]];
      parent = best;
    }
    return root;
  }
}

function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;

  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }

  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }

  if (liveCount === 0) {
    return [0, 0];
  }
  clean(minHeap);
  clean(maxHeap);
  return [maxHeap.peek().value, minHeap.peek().value];
}
```

## 15. Dry-run

Insert 5 hai lần tạo id0,id1. Delete max mark chỉ một id dead, liveCount 1. Clean heap còn lại có thể pop dead id nhưng vẫn giữ occurrence 5 sống; kết quả `[5,5]`.

## 16. Edge cases

Delete empty; duplicate values; delete min rồi max; tất cả entries dead nhưng còn nằm vật lý trong heap.

## 17. Lỗi JavaScript thường gặp

Dùng value làm alive key làm mất duplicate; không clean trước delete/peek; giảm liveCount khi delete empty; comparator max sai dấu; heap pop empty.

## 18. Bài transfer cùng pattern

- OF012: một min-heap đủ.
- OF013: heap object với comparator nhiều key.
- OF009: priority domain nhỏ dùng count array.

Tín hiệu: cần đồng thời min/max và không có balanced tree sẵn ⇒ hai heap đồng bộ bằng id + lazy deletion.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF014 Hai hàng đợi ưu tiên

Phần này dùng chính contract, dry run, Blueprint và code executable của **Hai hàng đợi ưu tiên** để dựng lại lời giải; pattern kiểm soát là `Double-priority-queue`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Xử lý lệnh insert, delete maximum, delete minimum. Delete trên cấu trúc rỗng bị bỏ qua. Cuối trả `[max,min]`, hoặc `[0,0]` nếu rỗng. Duplicate value là các occurrence riêng.

**Giới hạn quyết định implementation:** Sort array sau mỗi command hoặc splice cực trị là tốn kém. Hai heap cho hai cực trị `O(log n)` nhưng phải đồng bộ deletion.

### Concrete dry run

**Concrete input:** `[5,5]`

Insert 5 hai lần tạo id0,id1. Delete max mark chỉ một id dead, liveCount 1. Clean heap còn lại có thể pop dead id nhưng vẫn giữ occurrence 5 sống; kết quả `[5,5]`.



Decision ledger của **Hai hàng đợi ưu tiên** nối `two heaps, alive flags, nextId và liveCount.` với đúng block code; mỗi inner action trong input `[5,5]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[5,5]` | Chưa có state của Hai hàng đợi ưu tiên | Contract và input đã hợp lệ | heaps rỗng; alive rỗng; nextId=0; liveCount=0. | two heaps, alive flags, nextId và liveCount. | Chưa chốt output |
| command/value; khi clean là root entry của heap. đi vào | entry {value,id} và deletedEntry. | Với `[5,5]`, hỏi: insert hay delete; liveCount > 0; root id còn alive không. | insert push hai heap; D 1 pop max; D -1 pop min; delete empty thì bỏ qua.; operation loop tiến; clean pop liên tục stale roots. | two heaps, alive flags, nextId và liveCount. sau branch vừa chọn | [maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0]. chỉ đổi ở branch ghi answer |
| Transition của current | two heaps, alive flags, nextId và liveCount. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | insert đánh alive true và tăng liveCount; delete đánh false và giảm count. | two heaps, alive flags, nextId và liveCount. đã commit current; invariant của Hai hàng đợi ưu tiên được phục hồi | [maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0]. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | lazy-clean cả min và max trước final peek để loại entry chết.; sau mọi operation clean hai root; liveCount 0 trả [0,0], khác trả hai value. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Hai hàng đợi ưu tiên**, phần tử ở top/frontier hiện tại đi vào → đọc `entry {value,id} và deletedEntry.` từ state đang sống → hỏi đúng câu `insert hay delete; liveCount > 0; root id còn alive không.` → đi nhánh `insert push hai heap; D 1 pop max; D -1 pop min; delete empty thì bỏ qua.` → thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.` → pop/dequeue đúng phía, rồi mới push/enqueue state mới theo invariant → chỉ chốt `[maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0].` ở vị trí mà invariant đã đúng → lazy-clean cả min và max trước final peek để loại entry chết. → sau mọi operation clean hai root; liveCount 0 trả [0,0], khác trả hai value..

Với **Hai hàng đợi ưu tiên**, chuỗi “đọc command/value; khi clean là root entry của heap. → hỏi insert hay delete; liveCount > 0; root id còn alive không. → đổi insert đánh alive true và tăng liveCount; delete đánh false và giảm count. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Hai hàng đợi ưu tiên | `two heaps, alive flags, nextId và liveCount.` | State chung mà mọi current đọc hoặc mutate | `heaps rỗng; alive rỗng; nextId=0; liveCount=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `command/value; khi clean là root entry của heap.` | Current của đúng iteration/candidate hiện tại | Do `parse từng operation theo input order.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `entry {value,id} và deletedEntry.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `[maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0].` | Contract output của Hai hàng đợi ưu tiên | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const minHeap = new NumericHeap((a, b) => a.value - b.value);`; lựa chọn này giữ đúng thông tin mà check `insert hay delete; liveCount > 0; root id còn alive không.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `parse từng operation theo input order.`. Đây là lý do code dùng block bắt đầu bởi `for (const operation of operations) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “insert hay delete; liveCount > 0; root id còn alive không.”. Trong dry run `[5,5]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (command === "I") {`.
- **Tiến triển:** operation loop tiến; clean pop liên tục stale roots.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `nextId += 1;`.
- **Cleanup:** lazy-clean cả min và max trước final peek để loại entry chết.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Hai hàng đợi ưu tiên:** Guard cấu trúc không rỗng phải đứng trước mọi lần đọc top/front. Equality có pop hay không, và visited được mark lúc enqueue hay dequeue, lấy đúng từ invariant của bài này.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Hai hàng đợi ưu tiên | two heaps, alive flags, nextId và liveCount. | `const minHeap = new NumericHeap((a, b) => a.value - b.value);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | parse từng operation theo input order. | `for (const operation of operations) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | insert hay delete; liveCount > 0; root id còn alive không. | `if (command === "I") {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | insert đánh alive true và tăng liveCount; delete đánh false và giảm count. | `minHeap.push(entry);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | operation loop tiến; clean pop liên tục stale roots. | `nextId += 1;` | Sau read/update theo invariant của Hai hàng đợi ưu tiên |
| Hoàn tất và trả contract output | lazy-clean cả min và max trước final peek để loại entry chết. | `return [maxHeap.peek().value, minHeap.peek().value];` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Hai hàng đợi ưu tiên** là **toàn bộ `doublePriorityQueue` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Hai hàng đợi ưu tiên:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function doublePriorityQueue(operations) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Hai hàng đợi ưu tiên:** Những dữ kiện nào trong `two heaps, alive flags, nextId và liveCount.` phải còn nguyên khi current kế tiếp tới? Block `const minHeap = new NumericHeap((a, b) => a.value - b.value);` chạy đúng một lần trước traversal. Nếu đặt nó trong `parse từng operation theo input order.`, state nào sẽ bị reset?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Hai hàng đợi ưu tiên:** Những dữ kiện nào trong `two heaps, alive flags, nextId và liveCount.` phải còn nguyên khi current kế tiếp tới? Block `const maxHeap = new NumericHeap((a, b) => b.value - a.value);` chạy đúng một lần trước traversal. Nếu đặt nó trong `parse từng operation theo input order.`, state nào sẽ bị reset?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Hai hàng đợi ưu tiên:** Những dữ kiện nào trong `two heaps, alive flags, nextId và liveCount.` phải còn nguyên khi current kế tiếp tới? Block `const alive = [];` chạy đúng một lần trước traversal. Nếu đặt nó trong `parse từng operation theo input order.`, state nào sẽ bị reset?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Hai hàng đợi ưu tiên:** Những dữ kiện nào trong `two heaps, alive flags, nextId và liveCount.` phải còn nguyên khi current kế tiếp tới? Block `let liveCount = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `parse từng operation theo input order.`, state nào sẽ bị reset?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
}
```

**Bước 6 — state/initialization**

> **Prediction checkpoint — state của Hai hàng đợi ưu tiên:** Những dữ kiện nào trong `two heaps, alive flags, nextId và liveCount.` phải còn nguyên khi current kế tiếp tới? Block `let nextId = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `parse từng operation theo input order.`, state nào sẽ bị reset?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
}
```

**Bước 7 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `function clean(heap) {`, `while (heap.size > 0 && !alive[heap.peek().id]) {`, `heap.pop();` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
}
```

**Bước 8 — main loop và scope**

> **Prediction checkpoint — loop của Hai hàng đợi ưu tiên:** Header `for (const operation of operations) {` lặp per item, per candidate hay tới khi state rỗng? Current `command/value; khi clean là root entry của heap.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
  }
}
```

**Bước 9 — condition/inner branch**

> **Prediction checkpoint — condition của Hai hàng đợi ưu tiên:** Trước khi chạy `if (command === "I") {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `insert hay delete; liveCount > 0; root id còn alive không.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `if (command === "I") {`, `const entry = { value, id: nextId };`, `nextId += 1;`, `alive[entry.id] = true;`, `minHeap.push(entry);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }
  }
}
```

**Bước 11 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `for (const operation of operations) {`, `const [command, rawValue] = operation.split(" ");`, `const value = Number(rawValue);`, `if (command === "I") {`, `const entry = { value, id: nextId };` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
}
```

**Bước 12 — main loop và scope**

> **Prediction checkpoint — loop của Hai hàng đợi ưu tiên:** Header `if (liveCount === 0) {` lặp per item, per candidate hay tới khi state rỗng? Current `command/value; khi clean là root entry của heap.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
  if (liveCount === 0) {
  }
}
```

**Bước 13 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `if (liveCount === 0) {`, `return [0, 0];` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
  if (liveCount === 0) {
    return [0, 0];
  }
}
```

**Bước 14 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `clean(minHeap);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
  if (liveCount === 0) {
    return [0, 0];
  }
  clean(minHeap);
}
```

**Bước 15 — transition và movement**

> **Prediction checkpoint — transition của Hai hàng đợi ưu tiên:** Nhìn các brace đang mở để quyết định `clean(maxHeap);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.`, và movement `operation loop tiến; clean pop liên tục stale roots.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
  if (liveCount === 0) {
    return [0, 0];
  }
  clean(minHeap);
  clean(maxHeap);
}
```

**Bước 16 — cleanup/return**

> **Prediction checkpoint — finalization của Hai hàng đợi ưu tiên:** Dòng `return [maxHeap.peek().value, minHeap.peek().value];` chạy per current hay đúng một lần sau khi `parse từng operation theo input order.` kết thúc? Trước return, cleanup `lazy-clean cả min và max trước final peek để loại entry chết.` và output `[maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0].` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function doublePriorityQueue(operations) {
  const minHeap = new NumericHeap((a, b) => a.value - b.value);
  const maxHeap = new NumericHeap((a, b) => b.value - a.value);
  const alive = [];
  let liveCount = 0;
  let nextId = 0;
  function clean(heap) {
    while (heap.size > 0 && !alive[heap.peek().id]) {
      heap.pop();
    }
  }
  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);

    if (command === "I") {
      const entry = { value, id: nextId };
      nextId += 1;
      alive[entry.id] = true;
      minHeap.push(entry);
      maxHeap.push(entry);
      liveCount += 1;
      continue;
    }

    if (liveCount === 0) {
      continue;
    }
    const heap = value === 1 ? maxHeap : minHeap;
    clean(heap);
    const removed = heap.pop();
    alive[removed.id] = false;
    liveCount -= 1;
  }
  if (liveCount === 0) {
    return [0, 0];
  }
  clean(minHeap);
  clean(maxHeap);
  return [maxHeap.peek().value, minHeap.peek().value];
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const minHeap = new NumericHeap((a, b) => a.value - b.value);` | Trước traversal trong main function | two heaps, alive flags, nextId và liveCount. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Hai hàng đợi ưu tiên |
| `command/value; khi clean là root entry của heap.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (command === "I") {` | Sau khi state guard/current đã sẵn | Condition đọc entry {value,id} và deletedEntry. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `minHeap.push(entry);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi insert hay delete; liveCount > 0; root id còn alive không. | Đưa ra ngoài áp dụng update cho cả case false |
| `nextId += 1;` | operation loop tiến; clean pop liên tục stale roots. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: lazy-clean cả min và max trước final peek để loại entry chết. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return [maxHeap.peek().value, minHeap.peek().value];` | Sau finalization của main function | Output phải đạt [maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0]. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Hai hàng đợi ưu tiên** dùng `[5,5]`: dừng ngay khi `two heaps, alive flags, nextId và liveCount.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `doublePriorityQueue` với `[5,5]`; theo dõi đúng output `[maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0].` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | heaps rỗng; alive rỗng; nextId=0; liveCount=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | two heaps, alive flags, nextId và liveCount. | command/value; khi clean là root entry của heap. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | entry {value,id} và deletedEntry. | insert đánh alive true và tăng liveCount; delete đánh false và giảm count. | Đổi đúng như từng dòng dry run gốc | Condition insert hay delete; liveCount > 0; root id còn alive không. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | operation loop tiến; clean pop liên tục stale roots. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | lazy-clean cả min và max trước final peek để loại entry chết. | sau mọi operation clean hai root; liveCount 0 trả [0,0], khác trả hai value. | [maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0]. | Không còn transition bắt buộc nào chưa chạy |

Trong **Hai hàng đợi ưu tiên**, chỉ dòng thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.` được đổi output; state còn pending theo `two heaps, alive flags, nextId và liveCount.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng value làm alive key làm mất duplicate | `[5,5]` | Lượt đầu của `command/value; khi clean là root entry của heap.` mà `insert hay delete; liveCount > 0; root id còn alive không.` quyết định branch | Dùng value làm alive key làm mất duplicate | `const minHeap = new NumericHeap((a, b) => a.value - b.value);` ở đúng scope |
| không clean trước delete/peek | `{value,id}` | Ngay transition đầu tiên thực hiện `insert đánh alive true và tăng liveCount; delete đánh false và giảm count.` trên boundary/duplicate của input | không clean trước delete/peek | `minHeap.push(entry);` với init/strictness của Hai hàng đợi ưu tiên |
| giảm liveCount khi delete empty | `I 5, I 5, D 1` | Lượt đầu movement `operation loop tiến; clean pop liên tục stale roots.` xảy ra trước/sau sai assignment | giảm liveCount khi delete empty | `const minHeap = new NumericHeap((a, b) => a.value - b.value);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Mỗi entry push hai lần và pop tối đa một lần khỏi mỗi heap: amortized `O(q log q)`, space `O(q)`.

**Proof từ operation của Hai hàng đợi ưu tiên:** Đếm số lần một item vào và rời cấu trúc: mỗi item chỉ được push/enqueue theo transition của bài và chỉ được pop/dequeue khi đã hoàn tất vai trò. Nested while không tự động là bình phương nếu item đã rời không quay lại. Main loop được mô tả là `parse từng operation theo input order.`; movement là `operation loop tiến; clean pop liên tục stale roots.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `two heaps, alive flags, nextId và liveCount.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Hai hàng đợi ưu tiên; test `[5,5]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `two heaps, alive flags, nextId và liveCount.`, condition `insert hay delete; liveCount > 0; root id còn alive không.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Hai hàng đợi ưu tiên. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Insert 5 hai lần phải tạo hai occurrence khác nhau. Delete max chỉ xóa một occurrence; cấu trúc vẫn
còn `[5,5]` dưới dạng max/min. Nếu alive keyed bằng value, cả hai bản sao sẽ bị coi là đã chết.

```js
function doublePriorityQueueBrute(operations) {
  const values = [];

  for (const operation of operations) {
    const [command, rawValue] = operation.split(" ");
    const value = Number(rawValue);
    if (command === "I") {
      values.push(value);
      continue;
    }
    if (values.length === 0) continue;
    values.sort((a, b) => a - b);
    if (value === 1) values.pop();
    else values.shift();
  }

  if (values.length === 0) return [0, 0];
  values.sort((a, b) => a - b);
  return [values[values.length - 1], values[0]];
}
```

Brute force đúng nhưng sort lặp. Hai heap không xóa đồng bộ vật lý; `alive` đồng bộ logic.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `minHeap` | candidate minimum theo value tăng | rỗng |
| `maxHeap` | candidate maximum theo value giảm | rỗng |
| `alive[id]` | occurrence còn sống về logic | true khi insert |
| `nextId` | identity mới, kể cả duplicate value | `0` |
| `liveCount` | số occurrence sống | `0` |

#### 21. Blueprint từ đề đến code

1. Một heap chỉ hỗ trợ nhanh một đầu; bài cần cả min và max.
2. Push cùng object `{value,id}` vào hai heap đối nghịch.
3. Xóa ở một heap bằng cách mark `alive[id]=false`, không tìm trong heap kia.
4. Trước pop hoặc final peek, clean liên tục các root đã chết.
5. Dùng `liveCount` để nhận biết logical empty dù heap vật lý còn entry.
6. Identity theo id, không theo value, để duplicate độc lập.

#### 22. Invariant, lỗi lộ diện và recall

Mỗi occurrence sống có một entry trong cả hai heap; `alive` là nguồn sự thật duy nhất. `clean(heap)`
không cần xóa dead entry nằm sâu vì nó chưa ảnh hưởng cực trị. Sau clean, root là cực trị trong tập
sống. Mỗi stale entry bị pop nhiều nhất một lần nên lazy deletion vẫn amortized `O(log q)`.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | --- |
| alive keyed bằng value | `I 5, I 5, D 1` | `[5,5]` |
| không clean trước final peek | xóa min rồi còn stale root bên max | cực trị live |
| giảm count khi delete empty | `D -1, I 3` | `[3,3]` |
| max comparator cùng chiều min | `I 1, I 9, D 1` | `[1,1]` |

Recall: **cần hai cực trị ⇒ dual heap; cùng id ở hai heap; delete logic bằng alive, clean khi root lộ ra.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: [maximum, minimum] còn sống sau toàn bộ operations, hoặc [0,0].
PREPARE: tạo minHeap/maxHeap dùng chung object id và mảng alive.
GLOBAL STATE: two heaps, alive flags, nextId và liveCount.
INIT: heaps rỗng; alive rỗng; nextId=0; liveCount=0.
MAIN LOOP: parse từng operation theo input order.
CURRENT ITEM: command/value; khi clean là root entry của heap.
PER-ITERATION STATE: entry {value,id} và deletedEntry.
CHECK: insert hay delete; liveCount > 0; root id còn alive không.
BRANCH: insert push hai heap; D 1 pop max; D -1 pop min; delete empty thì bỏ qua.
UPDATE: insert đánh alive true và tăng liveCount; delete đánh false và giảm count.
POINTER MOVEMENT: operation loop tiến; clean pop liên tục stale roots.
STOP / RETURN: sau mọi operation clean hai root; liveCount 0 trả [0,0], khác trả hai value.
CLEANUP: lazy-clean cả min và max trước final peek để loại entry chết.
```

- Recall 1: Mỗi occurrence có id riêng trong hai heap; xóa chỉ mark dead, root stale mới bị clean.
- Recall 2: STATE min/max/alive/liveCount → LOOP ops → CHECK command/live → UPDATE mark → CLEANUP roots → RETURN.
- Recall 3: Điền `alive[entry.id] = ___` khi insert và `alive[deleted.id] = ___` khi delete.
<!-- dry-run-reconstruction:end -->
