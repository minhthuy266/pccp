# OF054 — Làm hai queue có tổng bằng nhau

- Nguồn đề: [Programmers lesson 118667](https://school.programmers.co.kr/learn/courses/30/lessons/118667?language=javascript)
- Pattern chính: two queue pointers + conserved sum
- Mức trong bank: TRANSFER / invariant

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Một operation lấy phần tử front của queue này và đưa vào back queue kia. Tìm số operation ít nhất để tổng hai queue bằng nhau; không thể thì `-1`. Các phần tử dương và hai queue ban đầu cùng độ dài.

## 02. Bound

Dùng `shift()` làm mỗi operation `O(n)`. Có thể biểu diễn chuỗi phần tử mà queue1 nhận/mất bằng một window trên `queue1 + queue2`, hai pointer chỉ tiến.

## 03. Brute force

Mô phỏng hai array bằng `shift/push`, thử chuyển từ queue có tổng lớn hơn. Logic choice đúng với số dương nhưng implementation dịch array và cần bound chống cycle.

## 04. Bottleneck

Tổng toàn hệ thống bảo toàn. Nếu total lẻ, impossible. Queue1 tại mọi thời điểm là một đoạn liên tiếp trong chuỗi ghép theo thứ tự chuyển; sum dương cho biết: thấp thì mở right, cao thì tăng left.

## 05. Pattern

Window target `total/2`:

```text
current < target → đọc combined[right] → cộng vào sum → tăng right
current > target → đọc combined[left] → trừ khỏi sum → tăng left
```

Mỗi pointer tối đa đi qua combined một lần.

## 06. State

- `combined=[...queue1,...queue2]`;
- window queue1 `[left,right)`;
- `current` sum window;
- `target`, `operations`.

## 07. Transition

Nhận phần tử: add trước vị trí right rồi right++. Bỏ phần tử: subtract vị trí left rồi left++. Mỗi transition tương ứng đúng một queue operation.

## 08. Invariant

`current` bằng tổng đoạn `[left,right)` đại diện queue1 sau đúng `operations` moves. Vì value dương, khi current thấp chỉ mở rộng mới có thể tăng; khi cao chỉ thu trái mới có thể giảm. Do đó greedy pointer không bỏ qua target window hợp lệ sớm hơn.

## 09. Complexity

Time `O(n)`, space `O(n)` cho combined. Mỗi pointer bị chặn bởi `2n`; không cần magic loop vô hạn.

## 10. Pseudocode

```text
total=sum both; if odd return -1
target=total/2; combined=q1+q2
left=0; right=len(q1); current=sum(q1); ops=0
while left<combined.length:
  if current==target return ops
  if current<target:
    if right==combined.length: break
    entering = combined[right]
    current += entering
    right += 1
  else:
    leaving = combined[left]
    current -= leaving
    left += 1
  ops += 1
return -1
```

## 11. Code bước 1 — dựng state

```js
function queueWindow(queue1, queue2) {
  return { combined:[...queue1,...queue2], left:0, right:queue1.length };
}
```

## 12. Code bước 2 — viết transition

```js
function receive(state) {
  const enteringValue = state.combined[state.right];
  state.current += enteringValue;
  state.right += 1;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function release(state) {
  const leavingValue = state.combined[state.left];
  state.current -= leavingValue;
  state.left += 1;
}
```

Hai transition đều tiến pointer, nên không cycle.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF054.js](../../../solutions/official/OF054.js).

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;

  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }

  let left = 0;
  let right = queue1.length;
  let operationCount = 0;

  while (left < combined.length) {
    if (queue1Sum === targetSum) return operationCount;

    if (queue1Sum < targetSum) {
      if (right >= combined.length) break;
      const enteringValue = combined[right];
      queue1Sum += enteringValue;
      right += 1;
    } else {
      const leavingValue = combined[left];
      queue1Sum -= leavingValue;
      left += 1;
    }

    operationCount += 1;
  }

  return -1;
}
```

## 15. Dry-run

Q1 sum 14, Q2 sum 16, target 15: current thấp nên nhận front Q2; nếu value làm current cao, bước sau bỏ front Q1. Mỗi thay đổi chính xác một operation và window dịch theo FIFO.

## 16. Edge cases

1. Sum đã bằng: 0.
2. Total lẻ: -1 ngay.
3. Một value lớn hơn target có thể làm impossible.
4. Cần nhiều lần nhận rồi nhiều lần bỏ.

## 17. Lỗi JavaScript thường gặp

- `shift()` lặp.
- Chỉ giới hạn operations bằng số đoán tùy ý.
- Không check total lẻ.
- Pointer right vượt combined.
- Dùng Number nếu constraints tổng vượt safe integer; bài này nằm trong miền Number an toàn theo contract hiện hành.

## 18. Bài transfer cùng pattern

- OF053: positive window đạt target sum trên một sequence.
- OF009: re-enqueue queue item với identity.
- SR004: hai front độc lập nhưng không có invariant sum.

Tín hiệu: move front→back bảo toàn thứ tự vòng và value dương ⇒ biểu diễn queue bằng window trên array nối.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF054 Làm hai queue có tổng bằng nhau

Phần này dùng chính contract, dry run, Blueprint và code executable của **Làm hai queue có tổng bằng nhau** để dựng lại lời giải; pattern kiểm soát là `Queue-pointers-invariant`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Một operation lấy phần tử front của queue này và đưa vào back queue kia. Tìm số operation ít nhất để tổng hai queue bằng nhau; không thể thì `-1`. Các phần tử dương và hai queue ban đầu cùng độ dài.

**Giới hạn quyết định implementation:** Dùng `shift()` làm mỗi operation `O(n)`. Có thể biểu diễn chuỗi phần tử mà queue1 nhận/mất bằng một window trên `queue1 + queue2`, hai pointer chỉ tiến.

### Concrete dry run

**Concrete input:** `queue1=[3,2,7,2], queue2=[4,6,5,1]`

Q1 sum 14, Q2 sum 16, target 15: current thấp nên nhận front Q2; nếu value làm current cao, bước sau bỏ front Q1. Mỗi thay đổi chính xác một operation và window dịch theo FIFO.



Decision ledger của **Làm hai queue có tổng bằng nhau** nối `left,right,queue1Sum,operationCount` với đúng block code; mỗi inner action trong input `queue1=[3,2,7,2], queue2=[4,6,5,1]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `queue1=[3,2,7,2], queue2=[4,6,5,1]` | Chưa có state của Làm hai queue có tổng bằng nhau | Contract và input đã hợp lệ | 0,queue1.length,sum(queue1),0 | left,right,queue1Sum,operationCount | Chưa chốt output |
| entering at right hoặc leaving at left đi vào | enteringValue/leavingValue | Với `queue1=[3,2,7,2], queue2=[4,6,5,1]`, hỏi: sum==target? sum<target? right còn trong bound? | thấp → add rồi right++; cao → subtract rồi left++; pointer tách riêng khỏi array access | left,right,queue1Sum,operationCount sau branch vừa chọn | minimum operation count hoặc -1 chỉ đổi ở branch ghi answer |
| Transition của current | left,right,queue1Sum,operationCount trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | queue1Sum và đúng một pointer; operationCount++ sau move | left,right,queue1Sum,operationCount đã commit current; invariant của Làm hai queue có tổng bằng nhau được phục hồi | minimum operation count hoặc -1 sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không; equal → count; không thể mở rộng → break; cuối -1 | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Làm hai queue có tổng bằng nhau**, phần tử ở top/frontier hiện tại đi vào → đọc `enteringValue/leavingValue` từ state đang sống → hỏi đúng câu `sum==target? sum<target? right còn trong bound?` → đi nhánh `thấp → add rồi right++; cao → subtract rồi left++` → thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move` → pop/dequeue đúng phía, rồi mới push/enqueue state mới theo invariant → chỉ chốt `minimum operation count hoặc -1` ở vị trí mà invariant đã đúng → không → equal → count; không thể mở rộng → break; cuối -1.

Với **Làm hai queue có tổng bằng nhau**, chuỗi “đọc entering at right hoặc leaving at left → hỏi sum==target? sum<target? right còn trong bound? → đổi queue1Sum và đúng một pointer; operationCount++ sau move → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Làm hai queue có tổng bằng nhau | `left,right,queue1Sum,operationCount` | State chung mà mọi current đọc hoặc mutate | `0,queue1.length,sum(queue1),0` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `entering at right hoặc leaving at left` | Current của đúng iteration/candidate hiện tại | Do `while left<combined.length` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `enteringValue/leavingValue` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `minimum operation count hoặc -1` | Contract output của Làm hai queue có tổng bằng nhau | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const combined = [...queue1, ...queue2];`; lựa chọn này giữ đúng thông tin mà check `sum==target? sum<target? right còn trong bound?` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `while left<combined.length`. Đây là lý do code dùng block bắt đầu bởi `while (left < combined.length) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “sum==target? sum<target? right còn trong bound?”. Trong dry run `queue1=[3,2,7,2], queue2=[4,6,5,1]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (totalSum % 2 !== 0) return -1;`.
- **Tiến triển:** pointer tách riêng khỏi array access. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `queue1Sum và đúng một pointer; operationCount++ sau move` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `right += 1;`.
- **Cleanup:** không. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Làm hai queue có tổng bằng nhau:** Guard cấu trúc không rỗng phải đứng trước mọi lần đọc top/front. Equality có pop hay không, và visited được mark lúc enqueue hay dequeue, lấy đúng từ invariant của bài này.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Làm hai queue có tổng bằng nhau | left,right,queue1Sum,operationCount | `const combined = [...queue1, ...queue2];` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | while left<combined.length | `while (left < combined.length) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | sum==target? sum<target? right còn trong bound? | `if (totalSum % 2 !== 0) return -1;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | queue1Sum và đúng một pointer; operationCount++ sau move | `totalSum += value;` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | pointer tách riêng khỏi array access | `right += 1;` | Sau read/update theo invariant của Làm hai queue có tổng bằng nhau |
| Hoàn tất và trả contract output | không | `return -1;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Làm hai queue có tổng bằng nhau** là **toàn bộ `equalQueueSumOperations` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Làm hai queue có tổng bằng nhau:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function equalQueueSumOperations(queue1, queue2) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `const combined = [...queue1, ...queue2];` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `let totalSum = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
}
```

**Bước 4 — main loop và scope**

> **Prediction checkpoint — loop của Làm hai queue có tổng bằng nhau:** Header `for (const value of combined) {` lặp per item, per candidate hay tới khi state rỗng? Current `entering at right hoặc leaving at left` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
  }
}
```

**Bước 5 — transition và movement**

> **Prediction checkpoint — transition của Làm hai queue có tổng bằng nhau:** Nhìn các brace đang mở để quyết định `for (const value of combined) {`, `totalSum += value;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move`, và movement `pointer tách riêng khỏi array access` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
}
```

**Bước 6 — cleanup/return**

> **Prediction checkpoint — finalization của Làm hai queue có tổng bằng nhau:** Dòng `if (totalSum % 2 !== 0) return -1;` chạy per current hay đúng một lần sau khi `while left<combined.length` kết thúc? Trước return, cleanup `không` và output `minimum operation count hoặc -1` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
}
```

**Bước 7 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `const targetSum = totalSum / 2;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
}
```

**Bước 8 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `let queue1Sum = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
}
```

**Bước 9 — main loop và scope**

> **Prediction checkpoint — loop của Làm hai queue có tổng bằng nhau:** Header `for (const value of queue1) {` lặp per item, per candidate hay tới khi state rỗng? Current `entering at right hoặc leaving at left` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Làm hai queue có tổng bằng nhau:** Nhìn các brace đang mở để quyết định `for (const value of queue1) {`, `queue1Sum += value;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move`, và movement `pointer tách riêng khỏi array access` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
}
```

**Bước 11 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `let left = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
}
```

**Bước 12 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `let right = queue1.length;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
  let right = queue1.length;
}
```

**Bước 13 — state/initialization**

> **Prediction checkpoint — state của Làm hai queue có tổng bằng nhau:** Những dữ kiện nào trong `left,right,queue1Sum,operationCount` phải còn nguyên khi current kế tiếp tới? Block `let operationCount = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `while left<combined.length`, state nào sẽ bị reset?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
  let right = queue1.length;
  let operationCount = 0;
}
```

**Bước 14 — main loop và scope**

> **Prediction checkpoint — loop của Làm hai queue có tổng bằng nhau:** Header `while (left < combined.length) {` lặp per item, per candidate hay tới khi state rỗng? Current `entering at right hoặc leaving at left` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
  let right = queue1.length;
  let operationCount = 0;
  while (left < combined.length) {
  }
}
```

**Bước 15 — transition và movement**

> **Prediction checkpoint — transition của Làm hai queue có tổng bằng nhau:** Nhìn các brace đang mở để quyết định `while (left < combined.length) {`, `if (queue1Sum === targetSum) return operationCount;`, `if (queue1Sum < targetSum) {`, `if (right >= combined.length) break;`, `const enteringValue = combined[right];` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move`, và movement `pointer tách riêng khỏi array access` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
  let right = queue1.length;
  let operationCount = 0;
  while (left < combined.length) {
    if (queue1Sum === targetSum) return operationCount;

    if (queue1Sum < targetSum) {
      if (right >= combined.length) break;
      const enteringValue = combined[right];
      queue1Sum += enteringValue;
      right += 1;
    } else {
      const leavingValue = combined[left];
      queue1Sum -= leavingValue;
      left += 1;
    }

    operationCount += 1;
  }
}
```

**Bước 16 — cleanup/return**

> **Prediction checkpoint — finalization của Làm hai queue có tổng bằng nhau:** Dòng `return -1;` chạy per current hay đúng một lần sau khi `while left<combined.length` kết thúc? Trước return, cleanup `không` và output `minimum operation count hoặc -1` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function equalQueueSumOperations(queue1, queue2) {
  const combined = [...queue1, ...queue2];
  let totalSum = 0;
  for (const value of combined) {
    totalSum += value;
  }
  if (totalSum % 2 !== 0) return -1;
  const targetSum = totalSum / 2;
  let queue1Sum = 0;
  for (const value of queue1) {
    queue1Sum += value;
  }
  let left = 0;
  let right = queue1.length;
  let operationCount = 0;
  while (left < combined.length) {
    if (queue1Sum === targetSum) return operationCount;

    if (queue1Sum < targetSum) {
      if (right >= combined.length) break;
      const enteringValue = combined[right];
      queue1Sum += enteringValue;
      right += 1;
    } else {
      const leavingValue = combined[left];
      queue1Sum -= leavingValue;
      left += 1;
    }

    operationCount += 1;
  }
  return -1;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const combined = [...queue1, ...queue2];` | Trước traversal trong main function | left,right,queue1Sum,operationCount phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Làm hai queue có tổng bằng nhau |
| `entering at right hoặc leaving at left` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (totalSum % 2 !== 0) return -1;` | Sau khi state guard/current đã sẵn | Condition đọc enteringValue/leavingValue | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `totalSum += value;` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi sum==target? sum<target? right còn trong bound? | Đưa ra ngoài áp dụng update cho cả case false |
| `right += 1;` | pointer tách riêng khỏi array access | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return -1;` | Sau finalization của main function | Output phải đạt minimum operation count hoặc -1 | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Làm hai queue có tổng bằng nhau** dùng `queue1=[3,2,7,2], queue2=[4,6,5,1]`: dừng ngay khi `left,right,queue1Sum,operationCount` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `equalQueueSumOperations` với `queue1=[3,2,7,2], queue2=[4,6,5,1]`; theo dõi đúng output `minimum operation count hoặc -1` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | 0,queue1.length,sum(queue1),0 | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | left,right,queue1Sum,operationCount | entering at right hoặc leaving at left | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | enteringValue/leavingValue | queue1Sum và đúng một pointer; operationCount++ sau move | Đổi đúng như từng dòng dry run gốc | Condition sum==target? sum<target? right còn trong bound? vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | pointer tách riêng khỏi array access | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không | equal → count; không thể mở rộng → break; cuối -1 | minimum operation count hoặc -1 | Không còn transition bắt buộc nào chưa chạy |

Trong **Làm hai queue có tổng bằng nhau**, chỉ dòng thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move` được đổi output; state còn pending theo `left,right,queue1Sum,operationCount` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| 'shift()' lặp. | `queue1=[3,2,7,2], queue2=[4,6,5,1]` | Lượt đầu của `entering at right hoặc leaving at left` mà `sum==target? sum<target? right còn trong bound?` quyết định branch | 'shift()' lặp. | `totalSum += value;` ở đúng scope |
| Chỉ giới hạn operations bằng số đoán tùy ý. | `[left,right)` | Ngay transition đầu tiên thực hiện `queue1Sum và đúng một pointer; operationCount++ sau move` trên boundary/duplicate của input | Chỉ giới hạn operations bằng số đoán tùy ý. | `totalSum += value;` với init/strictness của Làm hai queue có tổng bằng nhau |
| Không check total lẻ. | `[1],[2]` | Lượt đầu movement `pointer tách riêng khỏi array access` xảy ra trước/sau sai assignment | Không check total lẻ. | `if (totalSum % 2 !== 0) return -1;` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n)`, space `O(n)` cho combined. Mỗi pointer bị chặn bởi `2n`; không cần magic loop vô hạn.

**Proof từ operation của Làm hai queue có tổng bằng nhau:** Đếm số lần một item vào và rời cấu trúc: mỗi item chỉ được push/enqueue theo transition của bài và chỉ được pop/dequeue khi đã hoàn tất vai trò. Nested while không tự động là bình phương nếu item đã rời không quay lại. Main loop được mô tả là `while left<combined.length`; movement là `pointer tách riêng khỏi array access`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `left,right,queue1Sum,operationCount`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Làm hai queue có tổng bằng nhau; test `queue1=[3,2,7,2], queue2=[4,6,5,1]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `left,right,queue1Sum,operationCount`, condition `sum==target? sum<target? right còn trong bound?`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Làm hai queue có tổng bằng nhau. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Từ hai queue thật đến một cửa sổ dễ code

##### Làm bằng tay và brute force

Mỗi lần chỉ có hai lựa chọn hợp lý vì value dương: queue1 nặng hơn target thì chuyển front của
queue1 sang queue2; queue1 nhẹ hơn thì nhận front queue2. Tổng toàn hệ thống không đổi.

```js
function equalQueueSumOperationsBruteForce(queue1, queue2) {
  const firstQueue = queue1.slice();
  const secondQueue = queue2.slice();
  let firstSum = firstQueue.reduce((sum, value) => sum + value, 0);
  let secondSum = secondQueue.reduce((sum, value) => sum + value, 0);
  const operationLimit = (queue1.length + queue2.length) * 3;

  for (let operationCount = 0; operationCount <= operationLimit; operationCount++) {
    if (firstSum === secondSum) return operationCount;
    if (firstSum > secondSum) {
      const leavingValue = firstQueue.shift();
      if (leavingValue === undefined) return -1;
      firstSum -= leavingValue;
      secondSum += leavingValue;
      secondQueue.push(leavingValue);
    } else {
      const enteringValue = secondQueue.shift();
      if (enteringValue === undefined) return -1;
      secondSum -= enteringValue;
      firstSum += enteringValue;
      firstQueue.push(enteringValue);
    }
  }
  return -1;
}
```

Baseline dễ hiểu nhưng `shift()` dịch array và operation limit là hàng rào chống cycle khó
chứng minh. Thứ tự FIFO cho phép xem queue1 như window `[left,right)` trên `queue1+queue2`.

##### State, init và ý nghĩa pointer

| State | Kiểu | Lưu chính xác gì | Init/lý do | Scope |
| --- | --- | --- | --- | --- |
| `combined` | number[] | thứ tự các value có thể đi qua queue1 | nối hai queue | toàn hàm |
| `left` | number | front logic của queue1 | `0` | xuyên loop |
| `right` | number | vị trí đầu tiên chưa thuộc queue1 | `queue1.length` | xuyên loop |
| `queue1Sum` | number | tổng window `[left,right)` | sum queue1 | xuyên loop |
| `targetSum` | number | nửa tổng bảo toàn | `total/2` | toàn hàm |
| `operationCount` | number | số move đã mô phỏng | `0` | output state |
| `enteringValue`,`leavingValue` | number | value vào/ra window | từ pointer | iteration |

Total lẻ return `-1` trước loop. Positive values là điều kiện cho transition đơn điệu: sum thấp
chỉ có thể tăng bằng mở right; sum cao chỉ có thể giảm bằng tăng left.

```text
OUTPUT: minimum operation count hoặc -1; PREPARE: combined,total,target
GLOBAL STATE: left,right,queue1Sum,operationCount
INIT: 0,queue1.length,sum(queue1),0; MAIN LOOP: while left<combined.length
CURRENT ITEM: entering at right hoặc leaving at left
PER-ITERATION STATE: enteringValue/leavingValue
CHECK: sum==target? sum<target? right còn trong bound?
BRANCH: thấp → add rồi right++; cao → subtract rồi left++
UPDATE: queue1Sum và đúng một pointer; operationCount++ sau move
POINTER MOVEMENT: pointer tách riêng khỏi array access
STOP / RETURN: equal → count; không thể mở rộng → break; cuối -1
CLEANUP: không
```

Invariant đầu vòng: `queue1Sum` đúng bằng tổng `combined[left..right)` sau đúng
`operationCount` moves. Mỗi transition là đúng một front→back operation, nên lần đầu chạm target
là số operation nhỏ nhất trên đường pointer đơn điệu.

| Sum trước | So target | Transition | Pointer | Operations |
| ---: | --- | --- | --- | ---: |
| 14 | `<15` | add front queue2 | right++ | 1 |
| 18 | `>15` | remove front queue1 | left++ | 2 |
| 15 | bằng | return | không đổi | 2 |

Mỗi pointer chỉ tiến tối đa độ dài combined: `O(n)` time, `O(n)` space cho array nối. Không có
nested rescan và không cần magic operation limit.

- Logic: chuyển từ queue nhẹ sang queue nặng; sum càng lệch.
- Off-by-one: right khởi tạo `queue1.length-1`; window thiếu phần tử cuối.
- Check/update: `right++` trước khi đọc làm skip front queue2.
- Init: quên total lẻ; `[1],[2]` không thể bằng nhau.
- Hidden: sum đã bằng phải return 0 trước bất kỳ move nào.
- Performance: dùng `shift()` ở mỗi operation trên input lớn.

```text
Recall 1: total lẻ fail; queue1 là window; thấp add right; cao remove left; bằng return.
Recall 2: STATE combined,left,right,sum,target,ops; LOOP; CHECK; UPDATE pointer; RETURN.
Recall 3: if (sum < target) { sum += combined[____]; ____++; } else { sum -= combined[____]; ____++; }
OUT: min moves/-1; LIMIT: pointers O(n); HAND: theo dõi sum queue1
BRUTE: shift/push hai queue; SLOW: shift và cycle bound; PATTERN: positive window
STATE + INIT: [left,right)=[0,n),sum=q1; LOOP + INVARIANT: window là queue1
UPDATE + STOP: add/right hoặc subtract/left, equal return
```
<!-- dry-run-reconstruction:end -->
