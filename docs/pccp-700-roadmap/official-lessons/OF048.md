# OF048 — Hạn lưu trữ dữ liệu cá nhân

- Nguồn đề: [Programmers lesson 150370](https://school.programmers.co.kr/learn/courses/30/lessons/150370?language=javascript)
- Pattern chính: parsing + scalar date + Hash lookup
- Mức trong bank: CORE / implementation

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Mọi tháng được quy ước đúng 28 ngày. `terms` ánh xạ mã điều khoản sang số tháng lưu trữ. Mỗi privacy có ngày bắt đầu và mã. Tại `today`, trả index 1-based của các privacy đã hết hạn theo thứ tự tăng dần.

Nếu ngày hết hạn exclusive bằng today thì dữ liệu đã phải hủy.

## 02. Bound

Dữ liệu không lớn, nhưng so date bằng string hoặc xử lý mượn ngày/tháng thủ công dễ lỗi. Quy ước 28 ngày cho phép đổi mọi date thành một số ngày tuyến tính và so `O(1)`.

## 03. Brute force

Cho từng privacy, cộng term từng tháng/ngày trên ba field year-month-day, xử lý overflow và trừ một ngày cuối hiệu lực. Đúng nếu cực kỳ cẩn thận nhưng nhiều boundary.

## 04. Bottleneck

Date ba chiều gây mượn/carry. Đổi sang serial day làm phép cộng term thành `start + months*28`. Dùng mốc hết hạn exclusive tránh phép “trừ một ngày” riêng.

## 05. Pattern

Normalize then compare:

```text
date → scalar day
term code → months Map
expiryExclusive = startSerial + months*28
expired iff expiryExclusive <= todaySerial
```

## 06. State

- `termMonths: Map<code, months>`;
- `todaySerial`;
- từng privacy: `startDate`, `code`, `expiryExclusive`;
- `expired` index output.

## 07. Transition

Parse `YYYY.MM.DD` bằng split và Number. Serial dùng `(month-1)`, `(day-1)` để mốc đầu năm có offset zero. Với mỗi privacy, lookup months, cộng duration và so.

## 08. Invariant

`dateToSerial` bảo toàn thứ tự ngày vì mỗi năm có `12*28` ngày và mỗi tháng 28 ngày. Sau khi xử lý `i` privacy, output chứa đúng index hết hạn trong prefix, vốn tăng dần do scan input.

## 09. Complexity

- Build terms `O(t)`.
- Scan privacies `O(p)`.
- Space `O(t+p)` tính cả output.

## 10. Pseudocode

```text
parse terms into map
todayDay = serial(today)
for privacy at index i:
    parse start, code
    expiry = serial(start) + terms[code]*28
    if expiry <= todayDay: append i+1
return output
```

## 11. Code bước 1 — dựng state

```js
function dateToSerial(date) {
  const [year, month, day] = date.split(".").map(Number);
  return year * 12 * 28 + (month - 1) * 28 + (day - 1);
}
```

Mốc tuyệt đối tùy ý; chỉ hiệu và thứ tự quan trọng.

## 12. Code bước 2 — viết transition

```js
function parseTerms(terms) {
  const map = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    map.set(code, Number(rawMonths));
  }
  return map;
}
```

Không so mã term bằng nhiều `if`; Map phản ánh đúng lookup contract.

## 13. Code bước 3 — ghép vòng lặp

```js
function isExpired(startDate, months, todaySerial) {
  const expiryExclusive = dateToSerial(startDate) + months * 28;
  return expiryExclusive <= todaySerial;
}
```

Dấu `<=` là boundary quan trọng.

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF048.js](../../../solutions/official/OF048.js).

```js
const DAYS_PER_MONTH = 28;
const MONTHS_PER_YEAR = 12;

function dateToSerial(date) {
  const [year, month, day] = date.split(".").map(Number);
  return (
    year * MONTHS_PER_YEAR * DAYS_PER_MONTH +
    (month - 1) * DAYS_PER_MONTH +
    (day - 1)
  );
}

function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }

  const todaySerial = dateToSerial(today);
  const expired = [];

  for (let index = 0; index < privacies.length; index++) {
    const [startDate, code] = privacies[index].split(" ");
    const expiryExclusive =
      dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;
    if (expiryExclusive <= todaySerial) {
      expired.push(index + 1);
    }
  }

  return expired;
}
```

## 15. Dry-run

Start `2022.01.01`, term 1 tháng: expiry exclusive là `2022.02.01`. Today đúng `2022.02.01` ⇒ `expiry <= today` ⇒ đã hết hạn. Today `2022.01.28` thì chưa.

## 16. Edge cases

1. Expiry đúng today.
2. Qua tháng 12 sang năm sau.
3. Ngày 28 sang ngày 1 tháng kế.
4. Nhiều privacy cùng term.
5. Không privacy nào hết hạn.

## 17. Lỗi JavaScript thường gặp

- Dùng `new Date()` với lịch thật 30/31 ngày, trái quy ước đề.
- So chuỗi sau khi tự cộng date không normalize.
- Dùng `<` thay `<=`.
- Quên output 1-based.
- Không `Number(rawMonths)`, dựa vào coercion.

## 18. Bài transfer cùng pattern

- OF049: parse command rồi validate transition grid.
- OF050: event lookup bằng Map và mutate state.
- SR005: parse time thành phút scalar trước event sort.

Tín hiệu: domain có đơn vị quy ước cố định ⇒ normalize về scalar rồi làm arithmetic/compare.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF048 Hạn lưu trữ dữ liệu cá nhân

Phần này dùng chính contract, dry run, Blueprint và code executable của **Hạn lưu trữ dữ liệu cá nhân** để dựng lại lời giải; pattern kiểm soát là `Parsing-date-hash`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mọi tháng được quy ước đúng 28 ngày. `terms` ánh xạ mã điều khoản sang số tháng lưu trữ. Mỗi privacy có ngày bắt đầu và mã. Tại `today`, trả index 1-based của các privacy đã hết hạn theo thứ tự tăng dần.

Nếu ngày hết hạn exclusive bằng today thì dữ liệu đã phải hủy.

**Giới hạn quyết định implementation:** Dữ liệu không lớn, nhưng so date bằng string hoặc xử lý mượn ngày/tháng thủ công dễ lỗi. Quy ước 28 ngày cho phép đổi mọi date thành một số ngày tuyến tính và so `O(1)`.

### Concrete dry run

**Concrete input:** `2022.01.01`

Start `2022.01.01`, term 1 tháng: expiry exclusive là `2022.02.01`. Today đúng `2022.02.01` ⇒ `expiry <= today` ⇒ đã hết hạn. Today `2022.01.28` thì chưa.



Decision ledger của **Hạn lưu trữ dữ liệu cá nhân** nối `termMonths, todaySerial, expired.` với đúng block code; mỗi inner action trong input `2022.01.01` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `2022.01.01` | Chưa có state của Hạn lưu trữ dữ liệu cá nhân | Contract và input đã hợp lệ | Map/output rỗng; todaySerial tính một lần. | termMonths, todaySerial, expired. | Chưa chốt output |
| startDate và code của privacy hiện tại. đi vào | expiryExclusive. | Với `2022.01.01`, hỏi: expiryExclusive <= todaySerial. | đúng thì push index+1.; index += 1. | termMonths, todaySerial, expired. sau branch vừa chọn | index 1-based của privacy hết hạn theo input order. chỉ đổi ở branch ghi answer |
| Transition của current | termMonths, todaySerial, expired. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | output chỉ tăng; không sửa input. | termMonths, todaySerial, expired. đã commit current; invariant của Hạn lưu trữ dữ liệu cá nhân được phục hồi | index 1-based của privacy hết hạn theo input order. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không có.; hết input return expired. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Hạn lưu trữ dữ liệu cá nhân**, key/occurrence hiện tại đi vào → đọc `expiryExclusive.` từ state đang sống → hỏi đúng câu `expiryExclusive <= todaySerial.` → đi nhánh `đúng thì push index+1.` → thực hiện `output chỉ tăng; không sửa input.` → sang occurrence kế tiếp sau khi multiplicity của key đã nhất quán → chỉ chốt `index 1-based của privacy hết hạn theo input order.` ở vị trí mà invariant đã đúng → không có. → hết input return expired..

Với **Hạn lưu trữ dữ liệu cá nhân**, chuỗi “đọc startDate và code của privacy hiện tại. → hỏi expiryExclusive <= todaySerial. → đổi output chỉ tăng; không sửa input. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Hạn lưu trữ dữ liệu cá nhân | `termMonths, todaySerial, expired.` | State chung mà mọi current đọc hoặc mutate | `Map/output rỗng; todaySerial tính một lần.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `startDate và code của privacy hiện tại.` | Current của đúng iteration/candidate hiện tại | Do `scan privacies theo index tăng.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `expiryExclusive.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `index 1-based của privacy hết hạn theo input order.` | Contract output của Hạn lưu trữ dữ liệu cá nhân | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const termMonths = new Map();`; lựa chọn này giữ đúng thông tin mà check `expiryExclusive <= todaySerial.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `scan privacies theo index tăng.`. Đây là lý do code dùng block bắt đầu bởi `for (const term of terms) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “expiryExclusive <= todaySerial.”. Trong dry run `2022.01.01`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (expiryExclusive <= todaySerial) {`.
- **Tiến triển:** index += 1.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `output chỉ tăng; không sửa input.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `expired.push(index + 1);`.
- **Cleanup:** không có.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Hạn lưu trữ dữ liệu cá nhân:** Với Map/Set, phải phân biệt key chưa tồn tại với count bằng 0 và membership với multiplicity. Default, increment, decrement hoặc delete chỉ xuất hiện nếu contract của bài cần đúng transition đó.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Hạn lưu trữ dữ liệu cá nhân | termMonths, todaySerial, expired. | `const termMonths = new Map();` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | scan privacies theo index tăng. | `for (const term of terms) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | expiryExclusive <= todaySerial. | `if (expiryExclusive <= todaySerial) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | output chỉ tăng; không sửa input. | `termMonths.set(code, Number(rawMonths));` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | index += 1. | `expired.push(index + 1);` | Sau read/update theo invariant của Hạn lưu trữ dữ liệu cá nhân |
| Hoàn tất và trả contract output | không có. | `return expired;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Hạn lưu trữ dữ liệu cá nhân** là **toàn bộ `expiredPrivacyIndices` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Hạn lưu trữ dữ liệu cá nhân:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function expiredPrivacyIndices(today, terms, privacies) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Hạn lưu trữ dữ liệu cá nhân:** Những dữ kiện nào trong `termMonths, todaySerial, expired.` phải còn nguyên khi current kế tiếp tới? Block `const termMonths = new Map();` chạy đúng một lần trước traversal. Nếu đặt nó trong `scan privacies theo index tăng.`, state nào sẽ bị reset?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
}
```

**Bước 3 — main loop và scope**

> **Prediction checkpoint — loop của Hạn lưu trữ dữ liệu cá nhân:** Header `for (const term of terms) {` lặp per item, per candidate hay tới khi state rỗng? Current `startDate và code của privacy hiện tại.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
  }
}
```

**Bước 4 — transition và movement**

> **Prediction checkpoint — transition của Hạn lưu trữ dữ liệu cá nhân:** Nhìn các brace đang mở để quyết định `for (const term of terms) {`, `const [code, rawMonths] = term.split(" ");`, `termMonths.set(code, Number(rawMonths));` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `output chỉ tăng; không sửa input.`, và movement `index += 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Hạn lưu trữ dữ liệu cá nhân:** Những dữ kiện nào trong `termMonths, todaySerial, expired.` phải còn nguyên khi current kế tiếp tới? Block `const todaySerial = dateToSerial(today);` chạy đúng một lần trước traversal. Nếu đặt nó trong `scan privacies theo index tăng.`, state nào sẽ bị reset?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
}
```

**Bước 6 — state/initialization**

> **Prediction checkpoint — state của Hạn lưu trữ dữ liệu cá nhân:** Những dữ kiện nào trong `termMonths, todaySerial, expired.` phải còn nguyên khi current kế tiếp tới? Block `const expired = [];` chạy đúng một lần trước traversal. Nếu đặt nó trong `scan privacies theo index tăng.`, state nào sẽ bị reset?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
}
```

**Bước 7 — main loop và scope**

> **Prediction checkpoint — loop của Hạn lưu trữ dữ liệu cá nhân:** Header `for (let index = 0; index < privacies.length; index++) {` lặp per item, per candidate hay tới khi state rỗng? Current `startDate và code của privacy hiện tại.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
  for (let index = 0; index < privacies.length; index++) {
  }
}
```

**Bước 8 — condition/inner branch**

> **Prediction checkpoint — condition của Hạn lưu trữ dữ liệu cá nhân:** Trước khi chạy `if (expiryExclusive <= todaySerial) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `expiryExclusive <= todaySerial.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
  for (let index = 0; index < privacies.length; index++) {
    const [startDate, code] = privacies[index].split(" ");
    const expiryExclusive =
      dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;
    if (expiryExclusive <= todaySerial) {
    }
  }
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Hạn lưu trữ dữ liệu cá nhân:** Nhìn các brace đang mở để quyết định `if (expiryExclusive <= todaySerial) {`, `expired.push(index + 1);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `output chỉ tăng; không sửa input.`, và movement `index += 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
  for (let index = 0; index < privacies.length; index++) {
    const [startDate, code] = privacies[index].split(" ");
    const expiryExclusive =
      dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;
    if (expiryExclusive <= todaySerial) {
      expired.push(index + 1);
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Hạn lưu trữ dữ liệu cá nhân:** Nhìn các brace đang mở để quyết định `for (let index = 0; index < privacies.length; index++) {`, `const [startDate, code] = privacies[index].split(" ");`, `const expiryExclusive =`, `dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;`, `if (expiryExclusive <= todaySerial) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `output chỉ tăng; không sửa input.`, và movement `index += 1.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
  for (let index = 0; index < privacies.length; index++) {
    const [startDate, code] = privacies[index].split(" ");
    const expiryExclusive =
      dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;
    if (expiryExclusive <= todaySerial) {
      expired.push(index + 1);
    }
  }
}
```

**Bước 11 — cleanup/return**

> **Prediction checkpoint — finalization của Hạn lưu trữ dữ liệu cá nhân:** Dòng `return expired;` chạy per current hay đúng một lần sau khi `scan privacies theo index tăng.` kết thúc? Trước return, cleanup `không có.` và output `index 1-based của privacy hết hạn theo input order.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function expiredPrivacyIndices(today, terms, privacies) {
  const termMonths = new Map();
  for (const term of terms) {
    const [code, rawMonths] = term.split(" ");
    termMonths.set(code, Number(rawMonths));
  }
  const todaySerial = dateToSerial(today);
  const expired = [];
  for (let index = 0; index < privacies.length; index++) {
    const [startDate, code] = privacies[index].split(" ");
    const expiryExclusive =
      dateToSerial(startDate) + termMonths.get(code) * DAYS_PER_MONTH;
    if (expiryExclusive <= todaySerial) {
      expired.push(index + 1);
    }
  }
  return expired;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const termMonths = new Map();` | Trước traversal trong main function | termMonths, todaySerial, expired. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Hạn lưu trữ dữ liệu cá nhân |
| `startDate và code của privacy hiện tại.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (expiryExclusive <= todaySerial) {` | Sau khi state guard/current đã sẵn | Condition đọc expiryExclusive. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `termMonths.set(code, Number(rawMonths));` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi expiryExclusive <= todaySerial. | Đưa ra ngoài áp dụng update cho cả case false |
| `expired.push(index + 1);` | index += 1. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không có. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return expired;` | Sau finalization của main function | Output phải đạt index 1-based của privacy hết hạn theo input order. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Hạn lưu trữ dữ liệu cá nhân** dùng `2022.01.01`: dừng ngay khi `termMonths, todaySerial, expired.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `expiredPrivacyIndices` với `2022.01.01`; theo dõi đúng output `index 1-based của privacy hết hạn theo input order.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | Map/output rỗng; todaySerial tính một lần. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | termMonths, todaySerial, expired. | startDate và code của privacy hiện tại. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | expiryExclusive. | output chỉ tăng; không sửa input. | Đổi đúng như từng dòng dry run gốc | Condition expiryExclusive <= todaySerial. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | index += 1. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không có. | hết input return expired. | index 1-based của privacy hết hạn theo input order. | Không còn transition bắt buộc nào chưa chạy |

Trong **Hạn lưu trữ dữ liệu cá nhân**, chỉ dòng thực hiện `output chỉ tăng; không sửa input.` được đổi output; state còn pending theo `termMonths, todaySerial, expired.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng 'new Date()' với lịch thật 30/31 ngày, trái quy ước đề. | `Map<string,number>` | Lượt đầu của `startDate và code của privacy hiện tại.` mà `expiryExclusive <= todaySerial.` quyết định branch | Dùng 'new Date()' với lịch thật 30/31 ngày, trái quy ước đề. | `termMonths.set(code, Number(rawMonths));` ở đúng scope |
| So chuỗi sau khi tự cộng date không normalize. | `[]` | Ngay transition đầu tiên thực hiện `output chỉ tăng; không sửa input.` trên boundary/duplicate của input | So chuỗi sau khi tự cộng date không normalize. | `termMonths.set(code, Number(rawMonths));` với init/strictness của Hạn lưu trữ dữ liệu cá nhân |
| Dùng '<' thay '<='. | `[1]` | Lượt đầu movement `index += 1.` xảy ra trước/sau sai assignment | Dùng '<' thay '<='. | `if (expiryExclusive <= todaySerial) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

- Build terms `O(t)`.
- Scan privacies `O(p)`.
- Space `O(t+p)` tính cả output.

**Proof từ operation của Hạn lưu trữ dữ liệu cá nhân:** Mỗi occurrence được đọc đúng theo vòng sở hữu nó; mỗi lần đọc gây số hữu hạn thao tác Map/Set. Nếu có sort, phần sort chi phối; nếu không, các lượt lookup/update cộng lại theo kích thước input. Main loop được mô tả là `scan privacies theo index tăng.`; movement là `index += 1.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `termMonths, todaySerial, expired.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Hạn lưu trữ dữ liệu cá nhân; test `2022.01.01` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `termMonths, todaySerial, expired.`, condition `expiryExclusive <= todaySerial.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Hạn lưu trữ dữ liệu cá nhân. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force đầy đủ

Với today `2022.02.01`, privacy `2022.01.01 A`, term `A 1`, lịch 28 ngày cho mốc hết
hạn exclusive đúng `2022.02.01`; bằng today nghĩa là đã hết hạn. Bản brute force cộng từng
ngày giúp thấy boundary nhưng làm thừa:

```js
function expiresByDailySimulation(startDate, months, today) {
  let currentDay = dateToSerial(startDate);
  const duration = months * 28;

  for (let elapsed = 0; elapsed < duration; elapsed++) {
    currentDay += 1;
  }

  return currentDay <= dateToSerial(today);
}
```

Loop đúng vì tiến đúng số ngày hiệu lực, nhưng lặp theo duration. Scalar arithmetic thay cả
loop bằng `startSerial + months*28`; đây là cùng transition được gộp, không đổi semantics.

#### 20. Bảng state và init

| State | Kiểu | Lưu chính xác | Init | Vì sao |
| --- | --- | --- | --- | --- |
| `termMonths` | `Map<string,number>` | duration tháng của từng code | `new Map()` | chưa parse term |
| `todaySerial` | `number` | today trong lịch 28 ngày | `dateToSerial(today)` | mốc so cố định |
| `expired` | `number[]` | index 1-based hết hạn trong prefix | `[]` | chưa xét privacy |
| `expiryExclusive` | `number` | ngày đầu tiên privacy không còn hiệu lực | start + months×28 | state mỗi record |

#### 21. Code Blueprint

```text
OUTPUT: index 1-based của privacy hết hạn theo input order.
PREPARE: parse terms vào Map; normalize today thành scalar.
GLOBAL STATE: termMonths, todaySerial, expired.
INIT: Map/output rỗng; todaySerial tính một lần.
MAIN LOOP: scan privacies theo index tăng.
CURRENT ITEM: startDate và code của privacy hiện tại.
PER-ITERATION STATE: expiryExclusive.
CHECK: expiryExclusive <= todaySerial.
BRANCH: đúng thì push index+1.
UPDATE: output chỉ tăng; không sửa input.
POINTER MOVEMENT: index += 1.
STOP / RETURN: hết input return expired.
CLEANUP: không có.
```

Invariant: trước index `i`, output chứa đúng và theo thứ tự mọi record hết hạn trong
`privacies[0..i)`. Normalize bảo toàn thứ tự thời gian; dấu `<=` áp đúng exclusive boundary.

#### 22. Ma trận lỗi và recall

| Lỗi | Revealing test | Kết quả đúng |
| --- | --- | --- |
| dùng `<` | today `2022.02.01`, start `2022.01.01`, term 1 | expired |
| lịch thật 30/31 ngày | start `2022.01.28`, term 1 | expiry `2022.02.28` |
| output 0-based | privacy đầu hết hạn | `[1]` |
| không qua năm | start `2022.12.28`, term 1 | expiry `2023.01.28` |

- **Recall 1:** parse → normalize ngày → lookup tháng → expiry exclusive → `<= today`.
- **Recall 2:** `Map terms / todaySerial / scan / push index+1`.
- **Recall 3:** điền `expiry = dateToSerial(start) + ___ * ___`; condition là `___ <= ___`.
<!-- dry-run-reconstruction:end -->
