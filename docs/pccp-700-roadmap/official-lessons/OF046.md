# OF046 — Thứ hạng

- Nguồn đề: [Programmers lesson 49191](https://school.programmers.co.kr/learn/courses/30/lessons/49191?language=javascript)
- Pattern chính: transitive closure / reachability
- Mức trong bank: TRANSFER / graph integration

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Mỗi result `[winner,loser]` cho quan hệ thắng. Quan hệ bắc cầu: nếu A thắng B và B thắng C thì biết A đứng trên C. Đếm player có thứ hạng xác định duy nhất.

Một player xác định hạng khi với mọi player khác, biết họ thắng người kia hoặc thua người kia.

## 02. Bound

n nhỏ cho Floyd-Warshall boolean `O(n³)`. Không cần shortest distance, chỉ reachability.

## 03. Brute force

Thử mọi permutation ranking phù hợp results rồi xem player có cùng vị trí trong mọi permutation; factorial.

## 04. Bottleneck

Ta chỉ cần biết quan hệ có đường đi directed hay không. Tính transitive closure gom toàn bộ suy luận bắc cầu.

## 05. Pattern

Boolean Floyd-Warshall:

```text
if beats[from][middle] and beats[middle][to]
  beats[from][to]=true
```

Sau closure, player comparable với mọi other thì rank xác định.

## 06. State

- matrix `beats[from][to]`;
- loop order middle, from, to;
- `comparable` count từng player.

## 07. Transition

Cho phép node `middle` làm trung gian mới; mọi path dùng intermediates đã xét được ghi vào matrix. Skip row khi from không reach middle.

## 08. Invariant

Sau vòng middle k, matrix đúng cho mọi path có intermediate trong `0..k`. Khi xong, matrix là reachability toàn graph. Player có đúng n-1 quan hệ một chiều với others nên số người chắc chắn trên/dưới xác định vị trí duy nhất.

## 09. Complexity

Time `O(n³)`, space `O(n²)`.

## 10. Pseudocode

```text
beats[winner][loser]=true
for middle,from,to:
  beats[from][to] |= beats[from][middle] && beats[middle][to]
for player:
  comparable=count other where beats[p][o] or beats[o][p]
  if comparable==n-1 answer++
```

## 11. Code bước 1 — dựng state

```js
function reachability(n,results) {
  const beats=Array.from({length:n},()=>new Uint8Array(n));
  for (const [winner,loser] of results) beats[winner-1][loser-1]=1;
  return beats;
}
```

## 12. Code bước 2 — viết transition

```js
function canInfer(beats,from,middle,to) {
  return beats[from][middle] && beats[middle][to];
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function areComparable(beats,a,b) {
  return Boolean(beats[a][b] || beats[b][a]);
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF046.js](../../../solutions/official/OF046.js).

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }

  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }

  let answer = 0;
  for (let player = 0; player < n; player++) {
    let comparable = 0;
    for (let other = 0; other < n; other++) {
      if (player !== other && (beats[player][other] || beats[other][player])) {
        comparable += 1;
      }
    }
    if (comparable === n - 1) {
      answer += 1;
    }
  }
  return answer;
}
```

## 15. Dry-run

Results 1>2, 2>3: closure thêm 1>3. Player1 comparable với 2,3; player2 biết thua1/thắng3; player3 thua1,2. Cả ba có rank xác định.

## 16. Edge cases

1. Không đủ relation: zero rank xác định.
2. Chain đầy đủ qua transitivity.
3. Direct result không đủ nếu bỏ closure.
4. Không đếm self trong n-1.

## 17. Lỗi JavaScript thường gặp

- Treat edge vô hướng.
- Chỉ đếm direct wins/losses.
- Loop order không đặt middle ngoài cùng, phá proof in-place.
- Dùng `&&` value mà quên set matrix.
- Yêu cầu wins+losses bằng n thay n-1.

## 18. Bài transfer cùng pattern

- OF045: shortest unweighted từ một source.
- OF037: connected components vô hướng.
- OF029: MST/DSU, mục tiêu khác reachability all-pairs.

Tín hiệu: cần suy mọi quan hệ bắc cầu giữa mọi cặp, n nhỏ ⇒ transitive closure.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF046 Thứ hạng

Phần này dùng chính contract, dry run, Blueprint và code executable của **Thứ hạng** để dựng lại lời giải; pattern kiểm soát là `Graph-reachability`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mỗi result `[winner,loser]` cho quan hệ thắng. Quan hệ bắc cầu: nếu A thắng B và B thắng C thì biết A đứng trên C. Đếm player có thứ hạng xác định duy nhất.

Một player xác định hạng khi với mọi player khác, biết họ thắng người kia hoặc thua người kia.

**Giới hạn quyết định implementation:** n nhỏ cho Floyd-Warshall boolean `O(n³)`. Không cần shortest distance, chỉ reachability.

### Concrete dry run

**Concrete input:** `n=3, results=[[1,2],[2,3]]`

Results 1>2, 2>3: closure thêm 1>3. Player1 comparable với 2,3; player2 biết thua1/thắng3; player3 thua1,2. Cả ba có rank xác định.



Decision ledger của **Thứ hạng** nối `reachability matrix và determinableCount.` với đúng block code; mỗi inner action trong input `n=3, results=[[1,2],[2,3]]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `n=3, results=[[1,2],[2,3]]` | Chưa có state của Thứ hạng | Contract và input đã hợp lệ | direct wins true; count=0. | reachability matrix và determinableCount. | Chưa chốt output |
| triple (from,middle,to), rồi pair (player,other). đi vào | comparableCount của player. | Với `n=3, results=[[1,2],[2,3]]`, hỏi: from tới middle và middle tới to; player thắng hoặc thua other. | thiếu first leg thì skip row transition; comparable thì tăng count.; middle tăng đảm bảo invariant intermediate prefix. | reachability matrix và determinableCount. sau branch vừa chọn | số player có thứ hạng xác định duy nhất. chỉ đổi ở branch ghi answer |
| Transition của current | reachability matrix và determinableCount. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | beats[from][to]=true; determinable++ nếu comparableCount===n-1. | reachability matrix và determinableCount. đã commit current; invariant của Thứ hạng được phục hồi | số player có thứ hạng xác định duy nhất. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không tính self trong comparableCount.; closure và scan xong return determinableCount. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Thứ hạng**, đỉnh hoặc frontier entry hiện tại đi vào → đọc `comparableCount của player.` từ state đang sống → hỏi đúng câu `from tới middle và middle tới to; player thắng hoặc thua other.` → đi nhánh `thiếu first leg thì skip row transition; comparable thì tăng count.` → thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.` → chuyển sang neighbor hợp lệ sau guard bounds/visited hoặc push candidate mới vào frontier → chỉ chốt `số player có thứ hạng xác định duy nhất.` ở vị trí mà invariant đã đúng → không tính self trong comparableCount. → closure và scan xong return determinableCount..

Với **Thứ hạng**, chuỗi “đọc triple (from,middle,to), rồi pair (player,other). → hỏi from tới middle và middle tới to; player thắng hoặc thua other. → đổi beats[from][to]=true; determinable++ nếu comparableCount===n-1. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Thứ hạng | `reachability matrix và determinableCount.` | State chung mà mọi current đọc hoặc mutate | `direct wins true; count=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `triple (from,middle,to), rồi pair (player,other).` | Current của đúng iteration/candidate hiện tại | Do `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `comparableCount của player.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số player có thứ hạng xác định duy nhất.` | Contract output của Thứ hạng | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const beats = Array.from({ length: n }, () => new Uint8Array(n));`; lựa chọn này giữ đúng thông tin mà check `from tới middle và middle tới to; player thắng hoặc thua other.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.`. Đây là lý do code dùng block bắt đầu bởi `for (const [winner, loser] of results) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “from tới middle và middle tới to; player thắng hoặc thua other.”. Trong dry run `n=3, results=[[1,2],[2,3]]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (beats[from][middle] === 0) {`.
- **Tiến triển:** middle tăng đảm bảo invariant intermediate prefix.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `beats[from][to]=true; determinable++ nếu comparableCount===n-1.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (let middle = 0; middle < n; middle++) {`.
- **Cleanup:** không tính self trong comparableCount.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Thứ hạng:** Guard visited, bounds hoặc stale distance phải chạy trước transition tương ứng. Với BFS, mark visited lúc enqueue để một đỉnh không xếp hàng nhiều lần; với Dijkstra, bỏ entry stale trước khi relax cạnh.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Thứ hạng | reachability matrix và determinableCount. | `const beats = Array.from({ length: n }, () => new Uint8Array(n));` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | Floyd với middle ngoài cùng, from, to; sau closure scan từng player. | `for (const [winner, loser] of results) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | from tới middle và middle tới to; player thắng hoặc thua other. | `if (beats[from][middle] === 0) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | beats[from][to]=true; determinable++ nếu comparableCount===n-1. | `for (let middle = 0; middle < n; middle++) {` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | middle tăng đảm bảo invariant intermediate prefix. | `for (let middle = 0; middle < n; middle++) {` | Sau read/update theo invariant của Thứ hạng |
| Hoàn tất và trả contract output | không tính self trong comparableCount. | `return answer;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Thứ hạng** là **toàn bộ `determinableRankCount` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Thứ hạng:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function determinableRankCount(n, results) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Thứ hạng:** Những dữ kiện nào trong `reachability matrix và determinableCount.` phải còn nguyên khi current kế tiếp tới? Block `const beats = Array.from({ length: n }, () => new Uint8Array(n));` chạy đúng một lần trước traversal. Nếu đặt nó trong `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.`, state nào sẽ bị reset?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
}
```

**Bước 3 — main loop và scope**

> **Prediction checkpoint — loop của Thứ hạng:** Header `for (const [winner, loser] of results) {` lặp per item, per candidate hay tới khi state rỗng? Current `triple (from,middle,to), rồi pair (player,other).` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
  }
}
```

**Bước 4 — transition và movement**

> **Prediction checkpoint — transition của Thứ hạng:** Nhìn các brace đang mở để quyết định `for (const [winner, loser] of results) {`, `beats[winner - 1][loser - 1] = 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.`, và movement `middle tăng đảm bảo invariant intermediate prefix.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
}
```

**Bước 5 — main loop và scope**

> **Prediction checkpoint — loop của Thứ hạng:** Header `for (let middle = 0; middle < n; middle++) {` lặp per item, per candidate hay tới khi state rỗng? Current `triple (from,middle,to), rồi pair (player,other).` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
  }
}
```

**Bước 6 — condition/inner branch**

> **Prediction checkpoint — condition của Thứ hạng:** Trước khi chạy `for (let from = 0; from < n; from++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `from tới middle và middle tới to; player thắng hoặc thua other.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
    }
  }
}
```

**Bước 7 — transition và movement**

> **Prediction checkpoint — transition của Thứ hạng:** Nhìn các brace đang mở để quyết định `for (let from = 0; from < n; from++) {`, `if (beats[from][middle] === 0) {`, `continue;`, `for (let to = 0; to < n; to++) {`, `if (beats[middle][to]) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.`, và movement `middle tăng đảm bảo invariant intermediate prefix.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
}
```

**Bước 8 — transition và movement**

> **Prediction checkpoint — transition của Thứ hạng:** Nhìn các brace đang mở để quyết định `for (let middle = 0; middle < n; middle++) {`, `for (let from = 0; from < n; from++) {`, `if (beats[from][middle] === 0) {`, `continue;`, `for (let to = 0; to < n; to++) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.`, và movement `middle tăng đảm bảo invariant intermediate prefix.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
}
```

**Bước 9 — state/initialization**

> **Prediction checkpoint — state của Thứ hạng:** Những dữ kiện nào trong `reachability matrix và determinableCount.` phải còn nguyên khi current kế tiếp tới? Block `let answer = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.`, state nào sẽ bị reset?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
}
```

**Bước 10 — main loop và scope**

> **Prediction checkpoint — loop của Thứ hạng:** Header `for (let player = 0; player < n; player++) {` lặp per item, per candidate hay tới khi state rỗng? Current `triple (from,middle,to), rồi pair (player,other).` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
  for (let player = 0; player < n; player++) {
  }
}
```

**Bước 11 — condition/inner branch**

> **Prediction checkpoint — condition của Thứ hạng:** Trước khi chạy `for (let other = 0; other < n; other++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `from tới middle và middle tới to; player thắng hoặc thua other.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
  for (let player = 0; player < n; player++) {
    let comparable = 0;
    for (let other = 0; other < n; other++) {
    }
  }
}
```

**Bước 12 — transition và movement**

> **Prediction checkpoint — transition của Thứ hạng:** Nhìn các brace đang mở để quyết định `for (let other = 0; other < n; other++) {`, `if (player !== other && (beats[player][other] \|\| beats[other][player])) {`, `comparable += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.`, và movement `middle tăng đảm bảo invariant intermediate prefix.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
  for (let player = 0; player < n; player++) {
    let comparable = 0;
    for (let other = 0; other < n; other++) {
      if (player !== other && (beats[player][other] || beats[other][player])) {
        comparable += 1;
      }
    }
  }
}
```

**Bước 13 — transition và movement**

> **Prediction checkpoint — transition của Thứ hạng:** Nhìn các brace đang mở để quyết định `for (let player = 0; player < n; player++) {`, `let comparable = 0;`, `for (let other = 0; other < n; other++) {`, `if (player !== other && (beats[player][other] \|\| beats[other][player])) {`, `comparable += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.`, và movement `middle tăng đảm bảo invariant intermediate prefix.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
  for (let player = 0; player < n; player++) {
    let comparable = 0;
    for (let other = 0; other < n; other++) {
      if (player !== other && (beats[player][other] || beats[other][player])) {
        comparable += 1;
      }
    }
    if (comparable === n - 1) {
      answer += 1;
    }
  }
}
```

**Bước 14 — cleanup/return**

> **Prediction checkpoint — finalization của Thứ hạng:** Dòng `return answer;` chạy per current hay đúng một lần sau khi `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.` kết thúc? Trước return, cleanup `không tính self trong comparableCount.` và output `số player có thứ hạng xác định duy nhất.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function determinableRankCount(n, results) {
  const beats = Array.from({ length: n }, () => new Uint8Array(n));
  for (const [winner, loser] of results) {
    beats[winner - 1][loser - 1] = 1;
  }
  for (let middle = 0; middle < n; middle++) {
    for (let from = 0; from < n; from++) {
      if (beats[from][middle] === 0) {
        continue;
      }
      for (let to = 0; to < n; to++) {
        if (beats[middle][to]) {
          beats[from][to] = 1;
        }
      }
    }
  }
  let answer = 0;
  for (let player = 0; player < n; player++) {
    let comparable = 0;
    for (let other = 0; other < n; other++) {
      if (player !== other && (beats[player][other] || beats[other][player])) {
        comparable += 1;
      }
    }
    if (comparable === n - 1) {
      answer += 1;
    }
  }
  return answer;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const beats = Array.from({ length: n }, () => new Uint8Array(n));` | Trước traversal trong main function | reachability matrix và determinableCount. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Thứ hạng |
| `triple (from,middle,to), rồi pair (player,other).` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (beats[from][middle] === 0) {` | Sau khi state guard/current đã sẵn | Condition đọc comparableCount của player. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `for (let middle = 0; middle < n; middle++) {` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi from tới middle và middle tới to; player thắng hoặc thua other. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (let middle = 0; middle < n; middle++) {` | middle tăng đảm bảo invariant intermediate prefix. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không tính self trong comparableCount. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return answer;` | Sau finalization của main function | Output phải đạt số player có thứ hạng xác định duy nhất. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Thứ hạng** dùng `n=3, results=[[1,2],[2,3]]`: dừng ngay khi `reachability matrix và determinableCount.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `determinableRankCount` với `n=3, results=[[1,2],[2,3]]`; theo dõi đúng output `số player có thứ hạng xác định duy nhất.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | direct wins true; count=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | reachability matrix và determinableCount. | triple (from,middle,to), rồi pair (player,other). | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | comparableCount của player. | beats[from][to]=true; determinable++ nếu comparableCount===n-1. | Đổi đúng như từng dòng dry run gốc | Condition from tới middle và middle tới to; player thắng hoặc thua other. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | middle tăng đảm bảo invariant intermediate prefix. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không tính self trong comparableCount. | closure và scan xong return determinableCount. | số player có thứ hạng xác định duy nhất. | Không còn transition bắt buộc nào chưa chạy |

Trong **Thứ hạng**, chỉ dòng thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.` được đổi output; state còn pending theo `reachability matrix và determinableCount.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Treat edge vô hướng. | `n=3, results=[[1,2],[2,3]]` | Lượt đầu của `triple (from,middle,to), rồi pair (player,other).` mà `from tới middle và middle tới to; player thắng hoặc thua other.` quyết định branch | Treat edge vô hướng. | `for (let middle = 0; middle < n; middle++) {` ở đúng scope |
| Chỉ đếm direct wins/losses. | `n=3, results=[[1,2],[2,3]]` | Ngay transition đầu tiên thực hiện `beats[from][to]=true; determinable++ nếu comparableCount===n-1.` trên boundary/duplicate của input | Chỉ đếm direct wins/losses. | `for (let middle = 0; middle < n; middle++) {` với init/strictness của Thứ hạng |
| Loop order không đặt middle ngoài cùng, phá proof in-place. | `n=3, results=[[1,2],[2,3]]` | Lượt đầu movement `middle tăng đảm bảo invariant intermediate prefix.` xảy ra trước/sau sai assignment | Loop order không đặt middle ngoài cùng, phá proof in-place. | `for (let middle = 0; middle < n; middle++) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n³)`, space `O(n²)`.

**Proof từ operation của Thứ hạng:** Đếm theo representation của graph: traversal thường xử lý mỗi đỉnh hữu hạn lần và xét mỗi cạnh theo adjacency; nếu frontier là heap, mỗi push/pop hợp lệ cộng O(log V). Main loop được mô tả là `Floyd với middle ngoài cùng, from, to; sau closure scan từng player.`; movement là `middle tăng đảm bảo invariant intermediate prefix.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `reachability matrix và determinableCount.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Thứ hạng; test `n=3, results=[[1,2],[2,3]]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `reachability matrix và determinableCount.`, condition `from tới middle và middle tới to; player thắng hoặc thua other.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Thứ hạng. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Từ `1>2` và `2>3`, closure suy ra `1>3`. Khi đó mỗi player comparable với hai player còn lại,
nên cả ba hạng đều xác định dù input không có edge trực tiếp 1→3.

```js
function determinableRankCountBrute(n, results) {
  const adjacency = Array.from({ length: n }, () => []);
  for (const [winner, loser] of results) adjacency[winner - 1].push(loser - 1);

  function reaches(start, target) {
    const stack = [start];
    const visited = new Set([start]);
    while (stack.length > 0) {
      for (const next of adjacency[stack.pop()]) {
        if (next === target) return true;
        if (visited.has(next)) continue;
        visited.add(next);
        stack.push(next);
      }
    }
    return false;
  }

  let answer = 0;
  for (let player = 0; player < n; player += 1) {
    let comparable = true;
    for (let other = 0; other < n; other += 1) {
      if (player === other) continue;
      if (!reaches(player, other) && !reaches(other, player)) comparable = false;
    }
    if (comparable) answer += 1;
  }
  return answer;
}
```

Brute chạy reachability lặp cho từng cặp. Boolean Floyd-Warshall tính closure một lần.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `beats[from][to]` | đã biết from đứng trên to | direct results |
| `middle` | intermediate mới được cho phép | `0..n-1` ngoài cùng |
| `comparable` | số other có quan hệ một trong hai chiều | `0` mỗi player |
| `answer` | player comparable với n-1 other | `0` |

#### 21. Blueprint từ đề đến code

1. Dịch quan hệ thắng bắc cầu thành directed reachability.
2. Vì cần mọi cặp và n nhỏ, dùng boolean Floyd-Warshall.
3. Đặt `middle` ngoài cùng để invariant intermediate prefix đúng.
4. Nếu from không tới middle thì skip cả row transition.
5. Sau closure, với mỗi other kiểm `beats[p][o] || beats[o][p]`.
6. Rank xác định khi count đúng `n-1`, không tính self.

#### 22. Invariant, lỗi lộ diện và recall

Sau middle k, matrix chứa đúng các path có intermediate trong `0..k`. Transition nối hai path qua
k, nên induction cho closure đầy đủ. Hạng chỉ xác định nếu player comparable với mọi người khác.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| chỉ đếm direct result | chain 1>2>3 | 3 |
| coi edge vô hướng | một result trong 3 player | 0 |
| đếm self | yêu cầu n thay n-1 | n-1 other |
| middle không ở vòng ngoài | closure chain dài | suy đủ reachability |

Recall: **all-pairs relation bắc cầu, n nhỏ ⇒ boolean closure; rank cần comparable với n-1 người.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số player có thứ hạng xác định duy nhất.
PREPARE: dựng beats[from][to] boolean từ results.
GLOBAL STATE: reachability matrix và determinableCount.
INIT: direct wins true; count=0.
MAIN LOOP: Floyd với middle ngoài cùng, from, to; sau closure scan từng player.
CURRENT ITEM: triple (from,middle,to), rồi pair (player,other).
PER-ITERATION STATE: comparableCount của player.
CHECK: from tới middle và middle tới to; player thắng hoặc thua other.
BRANCH: thiếu first leg thì skip row transition; comparable thì tăng count.
UPDATE: beats[from][to]=true; determinable++ nếu comparableCount===n-1.
POINTER MOVEMENT: middle tăng đảm bảo invariant intermediate prefix.
STOP / RETURN: closure và scan xong return determinableCount.
CLEANUP: không tính self trong comparableCount.
```

- Recall 1: Boolean Floyd tạo thắng bắc cầu; rank xác định khi so được với mọi n-1 người khác.
- Recall 2: STATE beats/count → LOOP middle/from/to+players → CHECK reach/comparable → UPDATE → RETURN.
- Recall 3: Điền `beats[from][to] ||= beats[from][___] && beats[___][to]`; cần count `n - ___`.
<!-- dry-run-reconstruction:end -->
