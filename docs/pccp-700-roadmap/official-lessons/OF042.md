# OF042 — Ghép mảnh puzzle

- Nguồn đề: [Programmers lesson 84021](https://school.programmers.co.kr/learn/courses/30/lessons/84021?language=javascript)
- Pattern chính: component extraction + canonical shape rotation
- Mức trong bank: STRETCH / special modeling

## 01. Contract

Extract lỗ `0` trên game board và mảnh `1` trên table theo bốn hướng. Mỗi mảnh dùng tối đa một lần, được xoay 90°, không phản chiếu; phải khớp chính xác lỗ. Tối đa hóa số ô lấp.

## 02. Bound

Grid nhỏ; BFS mỗi cell một lần, rồi so component/rotation.

## 03. Brute force

Thử mọi placement, rotation và assignment piece-hole trên grid; state explosion.

## 04. Bottleneck

Vị trí tuyệt đối không quan trọng, chỉ hình dạng. Dịch component về origin, sort coordinates thành canonical key; rotate bốn lần.

## 05. Pattern

Extract components; normalize; với mỗi hole tìm unused piece cùng size có một rotation key bằng hole; mark used và cộng size.

## 06. State

Arrays holes/pieces coordinates, `used`, canonical key; BFS visited.

## 07. Transition

Rotation `[r,c]→[c,-r]`, sau đó normalize. Match thành công consume piece và break.

## 08. Invariant

Extract trả maximal connected components đúng target. Normalize loại translation nhưng giữ adjacency. Bốn rotations liệt kê toàn orientation hợp lệ; used bảo đảm mỗi occurrence dùng một lần.

## 09. Complexity

Extract `O(N²)`; matching worst `O(H*P*4*S log S)`, đủ bound; space `O(N²)`.

## 10. Pseudocode

```text
holes=components(board,0); pieces=components(table,1)
for hole:
  for unused same-size piece:
    rotate up to 4; compare canonical key
    if match: use; answer+=size; break
```

## 11. Code bước 1 — dựng state

```js
function shapeKey(cells) {
  return normalizeShape(cells).map(([r,c])=>`${r}:${c}`).join("|");
}
```

## 12. Code bước 2 — viết transition

```js
function rotateShape(cells) {
  return normalizeShape(cells.map(([row,column])=>[column,-row]));
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
for (let turn=0;turn<4;turn++) {
  if (shapeKey(rotated)===holeKey) { matches=true; break; }
  rotated=rotateShape(rotated);
}
```

## 14. Code hoàn chỉnh

Code executable gồm extraction helpers: [solutions/official/OF042.js](../../../solutions/official/OF042.js).

```js
const SHAPE_DIRECTIONS = [[-1, 0], [1, 0], [0, -1], [0, 1]];

function normalizeShape(cells) {
  const minimumRow = Math.min(...cells.map(([row]) => row));
  const minimumColumn = Math.min(...cells.map(([, column]) => column));
  return cells
    .map(([row, column]) => [row - minimumRow, column - minimumColumn])
    .sort((a, b) => a[0] - b[0] || a[1] - b[1]);
}

function shapeKey(cells) {
  return normalizeShape(cells).map(([row, column]) => `${row}:${column}`).join("|");
}

function rotateShape(cells) {
  return normalizeShape(cells.map(([row, column]) => [column, -row]));
}

function extractComponents(board, target) {
  const rows = board.length;
  const columns = board[0].length;
  const visited = Array.from({ length: rows }, () => new Uint8Array(columns));
  const components = [];

  for (let startRow = 0; startRow < rows; startRow++) {
    for (let startColumn = 0; startColumn < columns; startColumn++) {
      if (visited[startRow][startColumn] || board[startRow][startColumn] !== target) continue;
      const queue = [[startRow, startColumn]];
      const cells = [];
      let head = 0;
      visited[startRow][startColumn] = 1;

      while (head < queue.length) {
        const [row, column] = queue[head];
        head += 1;
        cells.push([row, column]);
        for (const [deltaRow, deltaColumn] of SHAPE_DIRECTIONS) {
          const nextRow = row + deltaRow;
          const nextColumn = column + deltaColumn;
          if (
            nextRow < 0 || nextRow >= rows || nextColumn < 0 || nextColumn >= columns ||
            visited[nextRow][nextColumn] || board[nextRow][nextColumn] !== target
          ) continue;
          visited[nextRow][nextColumn] = 1;
          queue.push([nextRow, nextColumn]);
        }
      }
      components.push(normalizeShape(cells));
    }
  }
  return components;
}

function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;

  for (const hole of holes) {
    const holeKey = shapeKey(hole);
    for (let index = 0; index < pieces.length; index++) {
      if (used[index] || pieces[index].length !== hole.length) continue;
      let rotated = pieces[index];
      let matches = false;
      for (let turn = 0; turn < 4; turn++) {
        if (shapeKey(rotated) === holeKey) {
          matches = true;
          break;
        }
        rotated = rotateShape(rotated);
      }
      if (!matches) continue;
      used[index] = true;
      answer += hole.length;
      break;
    }
  }
  return answer;
}
```

## 15. Dry-run

L-piece `[(0,0),(1,0),(1,1)]` rotate/normalize thành `[(0,0),(0,1),(1,0)]`; translation ban đầu không ảnh hưởng key.

## 16. Edge cases

Một cell; duplicate shapes; piece chỉ khớp sau rotation; mirror không hợp lệ; component cùng size khác shape.

## 17. Lỗi JavaScript thường gặp

Không normalize sau rotate; cho phép reflection; dùng Set string unsorted; reuse piece; extract diagonal; alias visited rows.

## 18. Bài transfer cùng pattern

- OF037: component extraction.
- OF040: geometry scale.
- OF047: graph embedding/crossing.

Tín hiệu: object trên grid giống nhau bất kể translation/rotation ⇒ canonical representation trước matching.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF042 Ghép mảnh puzzle

Phần này dùng chính contract, dry run, Blueprint và code executable của **Ghép mảnh puzzle** để dựng lại lời giải; pattern kiểm soát là `Component-shape-canonicalization`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Extract lỗ `0` trên game board và mảnh `1` trên table theo bốn hướng. Mỗi mảnh dùng tối đa một lần, được xoay 90°, không phản chiếu; phải khớp chính xác lỗ. Tối đa hóa số ô lấp.

**Giới hạn quyết định implementation:** Grid nhỏ; BFS mỗi cell một lần, rồi so component/rotation.

### Concrete dry run

**Concrete input:** `[(0,0),(1,0),(1,1)]`

L-piece `[(0,0),(1,0),(1,1)]` rotate/normalize thành `[(0,0),(0,1),(1,0)]`; translation ban đầu không ảnh hưởng key.



Decision ledger của **Ghép mảnh puzzle** nối `holes, pieces, used và answer.` với đúng block code; mỗi inner action trong input `[(0,0),(1,0),(1,1)]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[(0,0),(1,0),(1,1)]` | Chưa có state của Ghép mảnh puzzle | Contract và input đã hợp lệ | used false; answer=0. | holes, pieces, used và answer. | Chưa chốt output |
| holeKey, piece index và rotated shape. đi vào | matches flag và turn. | Với `[(0,0),(1,0),(1,1)]`, hỏi: piece unused/same size; shapeKey(rotated) === holeKey. | mismatch rotate tiếp; match mark used, cộng size và break piece loop.; hole loop, piece index, turn 0..3. | holes, pieces, used và answer. sau branch vừa chọn | tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation. chỉ đổi ở branch ghi answer |
| Transition của current | holes, pieces, used và answer. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | rotated=rotateShape(rotated); used[index]=true; answer += hole.length. | holes, pieces, used và answer. đã commit current; invariant của Ghép mảnh puzzle được phục hồi | tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | normalize sau mỗi rotation; consume theo index để duplicate shape độc lập.; hết holes return answer; mỗi hole nhận tối đa một piece. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Ghép mảnh puzzle**, item/candidate/event hiện tại đi vào → đọc `matches flag và turn.` từ state đang sống → hỏi đúng câu `piece unused/same size; shapeKey(rotated) === holeKey.` → đi nhánh `mismatch rotate tiếp; match mark used, cộng size và break piece loop.` → thực hiện `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.` → chuyển sang candidate kế sau khi áp dụng đủ rule theo đúng thứ tự → chỉ chốt `tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation.` ở vị trí mà invariant đã đúng → normalize sau mỗi rotation; consume theo index để duplicate shape độc lập. → hết holes return answer; mỗi hole nhận tối đa một piece..

Với **Ghép mảnh puzzle**, chuỗi “đọc holeKey, piece index và rotated shape. → hỏi piece unused/same size; shapeKey(rotated) === holeKey. → đổi rotated=rotateShape(rotated); used[index]=true; answer += hole.length. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Ghép mảnh puzzle | `holes, pieces, used và answer.` | State chung mà mọi current đọc hoặc mutate | `used false; answer=0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `holeKey, piece index và rotated shape.` | Current của đúng iteration/candidate hiện tại | Do `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `matches flag và turn.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation.` | Contract output của Ghép mảnh puzzle | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const holes = extractComponents(gameBoard, 0);`; lựa chọn này giữ đúng thông tin mà check `piece unused/same size; shapeKey(rotated) === holeKey.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`. Đây là lý do code dùng block bắt đầu bởi `for (const hole of holes) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “piece unused/same size; shapeKey(rotated) === holeKey.”. Trong dry run `[(0,0),(1,0),(1,1)]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (used[index] \|\| pieces[index].length !== hole.length) continue;`.
- **Tiến triển:** hole loop, piece index, turn 0..3.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (let index = 0; index < pieces.length; index++) {`.
- **Cleanup:** normalize sau mỗi rotation; consume theo index để duplicate shape độc lập.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Ghép mảnh puzzle:** Condition của scan/simulation phải theo đúng tie-break và thứ tự rule của đề. So sánh strict hay non-strict chỉ đổi khi boundary/tie của bài chứng minh, không vì thói quen viết comparator.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Ghép mảnh puzzle | holes, pieces, used và answer. | `const holes = extractComponents(gameBoard, 0);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation. | `for (const hole of holes) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | piece unused/same size; shapeKey(rotated) === holeKey. | `if (used[index] \|\| pieces[index].length !== hole.length) continue;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | rotated=rotateShape(rotated); used[index]=true; answer += hole.length. | `for (let index = 0; index < pieces.length; index++) {` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | hole loop, piece index, turn 0..3. | `for (let index = 0; index < pieces.length; index++) {` | Sau read/update theo invariant của Ghép mảnh puzzle |
| Hoàn tất và trả contract output | normalize sau mỗi rotation; consume theo index để duplicate shape độc lập. | `return answer;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Ghép mảnh puzzle** là **toàn bộ `filledPuzzleCells` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Ghép mảnh puzzle:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function filledPuzzleCells(gameBoard, table) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Ghép mảnh puzzle:** Những dữ kiện nào trong `holes, pieces, used và answer.` phải còn nguyên khi current kế tiếp tới? Block `const holes = extractComponents(gameBoard, 0);` chạy đúng một lần trước traversal. Nếu đặt nó trong `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`, state nào sẽ bị reset?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Ghép mảnh puzzle:** Những dữ kiện nào trong `holes, pieces, used và answer.` phải còn nguyên khi current kế tiếp tới? Block `const pieces = extractComponents(table, 1);` chạy đúng một lần trước traversal. Nếu đặt nó trong `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`, state nào sẽ bị reset?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Ghép mảnh puzzle:** Những dữ kiện nào trong `holes, pieces, used và answer.` phải còn nguyên khi current kế tiếp tới? Block `const used = Array(pieces.length).fill(false);` chạy đúng một lần trước traversal. Nếu đặt nó trong `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`, state nào sẽ bị reset?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Ghép mảnh puzzle:** Những dữ kiện nào trong `holes, pieces, used và answer.` phải còn nguyên khi current kế tiếp tới? Block `let answer = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`, state nào sẽ bị reset?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
}
```

**Bước 6 — main loop và scope**

> **Prediction checkpoint — loop của Ghép mảnh puzzle:** Header `for (const hole of holes) {` lặp per item, per candidate hay tới khi state rỗng? Current `holeKey, piece index và rotated shape.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
  for (const hole of holes) {
  }
}
```

**Bước 7 — condition/inner branch**

> **Prediction checkpoint — condition của Ghép mảnh puzzle:** Trước khi chạy `for (let index = 0; index < pieces.length; index++) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `piece unused/same size; shapeKey(rotated) === holeKey.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
  for (const hole of holes) {
    const holeKey = shapeKey(hole);
    for (let index = 0; index < pieces.length; index++) {
    }
  }
}
```

**Bước 8 — transition và movement**

> **Prediction checkpoint — transition của Ghép mảnh puzzle:** Nhìn các brace đang mở để quyết định `for (let index = 0; index < pieces.length; index++) {`, `if (used[index] \|\| pieces[index].length !== hole.length) continue;`, `let rotated = pieces[index];`, `let matches = false;`, `for (let turn = 0; turn < 4; turn++) {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.`, và movement `hole loop, piece index, turn 0..3.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
  for (const hole of holes) {
    const holeKey = shapeKey(hole);
    for (let index = 0; index < pieces.length; index++) {
      if (used[index] || pieces[index].length !== hole.length) continue;
      let rotated = pieces[index];
      let matches = false;
      for (let turn = 0; turn < 4; turn++) {
        if (shapeKey(rotated) === holeKey) {
          matches = true;
          break;
        }
        rotated = rotateShape(rotated);
      }
      if (!matches) continue;
      used[index] = true;
      answer += hole.length;
      break;
    }
  }
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Ghép mảnh puzzle:** Nhìn các brace đang mở để quyết định `for (const hole of holes) {`, `const holeKey = shapeKey(hole);`, `for (let index = 0; index < pieces.length; index++) {`, `if (used[index] \|\| pieces[index].length !== hole.length) continue;`, `let rotated = pieces[index];` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.`, và movement `hole loop, piece index, turn 0..3.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
  for (const hole of holes) {
    const holeKey = shapeKey(hole);
    for (let index = 0; index < pieces.length; index++) {
      if (used[index] || pieces[index].length !== hole.length) continue;
      let rotated = pieces[index];
      let matches = false;
      for (let turn = 0; turn < 4; turn++) {
        if (shapeKey(rotated) === holeKey) {
          matches = true;
          break;
        }
        rotated = rotateShape(rotated);
      }
      if (!matches) continue;
      used[index] = true;
      answer += hole.length;
      break;
    }
  }
}
```

**Bước 10 — cleanup/return**

> **Prediction checkpoint — finalization của Ghép mảnh puzzle:** Dòng `return answer;` chạy per current hay đúng một lần sau khi `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.` kết thúc? Trước return, cleanup `normalize sau mỗi rotation; consume theo index để duplicate shape độc lập.` và output `tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function filledPuzzleCells(gameBoard, table) {
  const holes = extractComponents(gameBoard, 0);
  const pieces = extractComponents(table, 1);
  const used = Array(pieces.length).fill(false);
  let answer = 0;
  for (const hole of holes) {
    const holeKey = shapeKey(hole);
    for (let index = 0; index < pieces.length; index++) {
      if (used[index] || pieces[index].length !== hole.length) continue;
      let rotated = pieces[index];
      let matches = false;
      for (let turn = 0; turn < 4; turn++) {
        if (shapeKey(rotated) === holeKey) {
          matches = true;
          break;
        }
        rotated = rotateShape(rotated);
      }
      if (!matches) continue;
      used[index] = true;
      answer += hole.length;
      break;
    }
  }
  return answer;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const holes = extractComponents(gameBoard, 0);` | Trước traversal trong main function | holes, pieces, used và answer. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Ghép mảnh puzzle |
| `holeKey, piece index và rotated shape.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (used[index] \|\| pieces[index].length !== hole.length) continue;` | Sau khi state guard/current đã sẵn | Condition đọc matches flag và turn. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `for (let index = 0; index < pieces.length; index++) {` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi piece unused/same size; shapeKey(rotated) === holeKey. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (let index = 0; index < pieces.length; index++) {` | hole loop, piece index, turn 0..3. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: normalize sau mỗi rotation; consume theo index để duplicate shape độc lập. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return answer;` | Sau finalization của main function | Output phải đạt tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Ghép mảnh puzzle** dùng `[(0,0),(1,0),(1,1)]`: dừng ngay khi `holes, pieces, used và answer.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `filledPuzzleCells` với `[(0,0),(1,0),(1,1)]`; theo dõi đúng output `tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | used false; answer=0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | holes, pieces, used và answer. | holeKey, piece index và rotated shape. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | matches flag và turn. | rotated=rotateShape(rotated); used[index]=true; answer += hole.length. | Đổi đúng như từng dòng dry run gốc | Condition piece unused/same size; shapeKey(rotated) === holeKey. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | hole loop, piece index, turn 0..3. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | normalize sau mỗi rotation; consume theo index để duplicate shape độc lập. | hết holes return answer; mỗi hole nhận tối đa một piece. | tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation. | Không còn transition bắt buộc nào chưa chạy |

Trong **Ghép mảnh puzzle**, chỉ dòng thực hiện `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.` được đổi output; state còn pending theo `holes, pieces, used và answer.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Không normalize sau rotate | `[(0,0),(1,0),(1,1)]` | Lượt đầu của `holeKey, piece index và rotated shape.` mà `piece unused/same size; shapeKey(rotated) === holeKey.` quyết định branch | Không normalize sau rotate | `for (let index = 0; index < pieces.length; index++) {` ở đúng scope |
| cho phép reflection | `[(2,3),(3,3),(3,4)]` | Ngay transition đầu tiên thực hiện `rotated=rotateShape(rotated); used[index]=true; answer += hole.length.` trên boundary/duplicate của input | cho phép reflection | `for (let index = 0; index < pieces.length; index++) {` với init/strictness của Ghép mảnh puzzle |
| dùng Set string unsorted | `[(0,0),(0,1),(1,0)]` | Lượt đầu movement `hole loop, piece index, turn 0..3.` xảy ra trước/sau sai assignment | dùng Set string unsorted | `const holes = extractComponents(gameBoard, 0);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Extract `O(N²)`; matching worst `O(H*P*4*S log S)`, đủ bound; space `O(N²)`.

**Proof từ operation của Ghép mảnh puzzle:** Tách chi phí chuẩn bị (nếu có sort) khỏi traversal. Trong traversal, mỗi item/candidate được xét đúng số lần mô tả bởi loop; không nhân các loop nối tiếp thành tích. Main loop được mô tả là `mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.`; movement là `hole loop, piece index, turn 0..3.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `holes, pieces, used và answer.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Ghép mảnh puzzle; test `[(0,0),(1,0),(1,1)]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `holes, pieces, used và answer.`, condition `piece unused/same size; shapeKey(rotated) === holeKey.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Ghép mảnh puzzle. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Mảnh `[(2,3),(3,3),(3,4)]` normalize thành `[(0,0),(1,0),(1,1)]`. Xoay 90° rồi normalize
thành `[(0,0),(0,1),(1,0)]`. Translation đã biến mất, nhưng reflection không tự xuất hiện.

```js
function sameShapeBrute(first, second) {
  if (first.length !== second.length) return false;

  function normalize(cells) {
    const minimumRow = Math.min(...cells.map(([row]) => row));
    const minimumColumn = Math.min(...cells.map(([, column]) => column));
    return cells
      .map(([row, column]) => [row - minimumRow, column - minimumColumn])
      .sort((a, b) => a[0] - b[0] || a[1] - b[1]);
  }

  const target = JSON.stringify(normalize(second));
  let current = normalize(first);
  for (let turn = 0; turn < 4; turn += 1) {
    if (JSON.stringify(current) === target) return true;
    current = normalize(current.map(([row, column]) => [column, -row]));
  }
  return false;
}
```

Helper brute đối chiếu một cặp shape; solution thêm extraction component và consume từng piece.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `visited` | cell đã thuộc một component | matrix độc lập cho mỗi board |
| `cells` | tọa độ component đang extract | rỗng, thêm khi dequeue |
| `holes`, `pieces` | shape đã normalize | extract target 0 và target 1 |
| `used[index]` | piece occurrence đã consume | toàn false |
| `rotated` | orientation đang thử | shape gốc, xoay tối đa bốn lần |

#### 21. Blueprint từ đề đến code

1. BFS/DFS bốn hướng để tách maximal components trên hai board.
2. Bỏ vị trí tuyệt đối bằng cách trừ minimum row/column và sort tọa độ.
3. Dùng canonical key để so shape, không so thứ tự discovery.
4. Với mỗi hole, chỉ xét piece cùng size và bốn rotation.
5. Match thành công thì mark đúng occurrence `used[index]`, cộng size và dừng tìm cho hole đó.

#### 22. Invariant, lỗi lộ diện và recall

Extraction tạo mỗi cell target vào đúng một maximal component. Normalize bảo toàn hình nhưng loại
translation; rotate + normalize liệt kê đúng bốn orientation được phép. `used` giữ invariant mỗi
piece occurrence ghép nhiều nhất một hole.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| nối diagonal thành component | `[[1,0],[0,1]]` | hai component |
| không normalize sau rotate | L-piece lệch tọa độ âm | vẫn match |
| cho phép mirror | hai shape đối xứng gương | không match |
| dùng Set theo shape key thay occurrence | hai piece giống nhau | dùng được cả hai |

Recall: **extract component → normalize translation → thử 4 rotations → consume occurrence.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: tổng số cell lấp được bằng matching hole-piece không xoay lật ngoài rotation.
PREPARE: extract components; normalize translation; chuẩn bị used theo piece occurrence.
GLOBAL STATE: holes, pieces, used và answer.
INIT: used false; answer=0.
MAIN LOOP: mỗi hole quét pieces chưa dùng cùng size; inner thử tối đa 4 rotation.
CURRENT ITEM: holeKey, piece index và rotated shape.
PER-ITERATION STATE: matches flag và turn.
CHECK: piece unused/same size; shapeKey(rotated) === holeKey.
BRANCH: mismatch rotate tiếp; match mark used, cộng size và break piece loop.
UPDATE: rotated=rotateShape(rotated); used[index]=true; answer += hole.length.
POINTER MOVEMENT: hole loop, piece index, turn 0..3.
STOP / RETURN: hết holes return answer; mỗi hole nhận tối đa một piece.
CLEANUP: normalize sau mỗi rotation; consume theo index để duplicate shape độc lập.
```

- Recall 1: Tách component, normalize tọa độ, thử bốn rotation và consume đúng occurrence khi match.
- Recall 2: STATE holes/pieces/used/answer → LOOP hole/piece/turn → CHECK key → UPDATE used/sum → RETURN.
- Recall 3: Điền `for (turn=0; turn<___; turn++)`; match thì `used[index]=___`, `answer += ___`.
<!-- dry-run-reconstruction:end -->
