# OF018 — Hình chữ nhật nhỏ nhất

- Nguồn đề: [Programmers lesson 86491](https://school.programmers.co.kr/learn/courses/30/lessons/86491?language=javascript)
- Pattern chính: normalize orientation + extrema
- Mức trong bank: TRANSFER / warmup

## 01. Contract

Mỗi danh thiếp có hai cạnh và được phép xoay. Tìm diện tích ví nhỏ nhất chứa được mọi thẻ.

## 02. Bound

Một scan tuyến tính đủ; không cần thử `2^n` cách xoay.

## 03. Brute force

Với mỗi thẻ chọn xoay/không xoay, tính max width × max height cho mọi configuration. Có `2^n` configuration.

## 04. Bottleneck

Hai orientation của từng thẻ tương đương nếu chuẩn hóa cạnh dài luôn nằm cùng một trục, cạnh ngắn ở trục kia. Sau đó chỉ cần hai maximum.

## 05. Pattern

Với `[a,b]`, lấy `long=max(a,b)`, `short=min(a,b)`. Ví cần `maxLong × maxShort`.

## 06. State

`maximumLongSide`, `maximumShortSide`; không cần giữ orientation từng thẻ.

## 07. Transition

Mỗi thẻ cập nhật hai maximum sau normalize.

## 08. Invariant

Sau prefix thẻ, hai state là kích thước nhỏ nhất chứa mọi thẻ prefix khi cạnh dài đã đặt cùng trục. Mọi orientation khác không thể giảm đồng thời hai cực trị so với normalization này.

## 09. Complexity

Time `O(n)`, space `O(1)`.

## 10. Pseudocode

```text
maxLong=0; maxShort=0
for [a,b]:
  maxLong=max(maxLong,max(a,b))
  maxShort=max(maxShort,min(a,b))
return maxLong*maxShort
```

## 11. Code bước 1 — dựng state

```js
let maximumLongSide=0;
let maximumShortSide=0;
```

## 12. Code bước 2 — viết transition

```js
function normalize(width,height) {
  return [Math.max(width,height),Math.min(width,height)];
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
for (const [width,height] of sizes) {
  const [long,short]=normalize(width,height);
  maximumLongSide=Math.max(maximumLongSide,long);
  maximumShortSide=Math.max(maximumShortSide,short);
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF018.js](../../../solutions/official/OF018.js).

```js
function minimumWalletArea(sizes) {
  let maxLong=0,maxShort=0;
  for (const [width,height] of sizes) {
    maxLong=Math.max(maxLong,Math.max(width,height));
    maxShort=Math.max(maxShort,Math.min(width,height));
  }
  return maxLong*maxShort;
}
```

## 15. Dry-run

`[60,50],[30,70]` normalize thành `[60,50],[70,30]`; wallet `70×50`, không phải lấy max width/max height của orientation ban đầu.

## 16. Edge cases

Một thẻ; thẻ vuông; cạnh dài nhất và cạnh ngắn-max nằm ở hai thẻ khác nhau; mọi thẻ cần xoay khác nhau.

## 17. Lỗi JavaScript thường gặp

Lấy max từng cột trước normalize; sort toàn input không cần; mutate pair; nhân maximum của toàn bộ 2n cạnh hai lần.

## 18. Bài transfer cùng pattern

- OF021: reduce geometry về factor equations.
- OF015: normalize index rồi sort slice.
- OF057: normalize decision qua endpoint.

Tín hiệu: mỗi item có orientation hoán đổi hai chiều ⇒ normalize local trước khi aggregate extrema.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF018 Hình chữ nhật nhỏ nhất

Phần này dùng chính contract, dry run, Blueprint và code executable của **Hình chữ nhật nhỏ nhất** để dựng lại lời giải; pattern kiểm soát là `Exhaustive-scan`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mỗi danh thiếp có hai cạnh và được phép xoay. Tìm diện tích ví nhỏ nhất chứa được mọi thẻ.

**Giới hạn quyết định implementation:** Một scan tuyến tính đủ; không cần thử `2^n` cách xoay.

### Concrete dry run

**Concrete input:** `[60,50],[30,70]`

`[60,50],[30,70]` normalize thành `[60,50],[70,30]`; wallet `70×50`, không phải lấy max width/max height của orientation ban đầu.



Decision ledger của **Hình chữ nhật nhỏ nhất** nối `maximumLongSide và maximumShortSide.` với đúng block code; mỗi inner action trong input `[60,50],[30,70]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[60,50],[30,70]` | Chưa có state của Hình chữ nhật nhỏ nhất | Contract và input đã hợp lệ | hai maximum = 0 vì chưa đọc card nào. | maximumLongSide và maximumShortSide. | Chưa chốt output |
| card hiện tại với longSide và shortSide. đi vào | hai cạnh normalized chỉ sống trong lượt. | Với `[60,50],[30,70]`, hỏi: cạnh hiện tại có lớn hơn maximum cùng vai trò không. | Math.max xử lý hai check, không cần if riêng.; for...of tự sang card kế tiếp. | maximumLongSide và maximumShortSide. sau branch vừa chọn | diện tích ví nhỏ nhất chứa mọi card sau khi được xoay. chỉ đổi ở branch ghi answer |
| Transition của current | maximumLongSide và maximumShortSide. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | cập nhật maximumLongSide và maximumShortSide độc lập. | maximumLongSide và maximumShortSide. đã commit current; invariant của Hình chữ nhật nhỏ nhất được phục hồi | diện tích ví nhỏ nhất chứa mọi card sau khi được xoay. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không mutate orientation gốc; không ghép long của card này với short chưa normalize.; hết cards thì return tích hai maximum. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Hình chữ nhật nhỏ nhất**, item/candidate/event hiện tại đi vào → đọc `hai cạnh normalized chỉ sống trong lượt.` từ state đang sống → hỏi đúng câu `cạnh hiện tại có lớn hơn maximum cùng vai trò không.` → đi nhánh `Math.max xử lý hai check, không cần if riêng.` → thực hiện `cập nhật maximumLongSide và maximumShortSide độc lập.` → chuyển sang candidate kế sau khi áp dụng đủ rule theo đúng thứ tự → chỉ chốt `diện tích ví nhỏ nhất chứa mọi card sau khi được xoay.` ở vị trí mà invariant đã đúng → không mutate orientation gốc; không ghép long của card này với short chưa normalize. → hết cards thì return tích hai maximum..

Với **Hình chữ nhật nhỏ nhất**, chuỗi “đọc card hiện tại với longSide và shortSide. → hỏi cạnh hiện tại có lớn hơn maximum cùng vai trò không. → đổi cập nhật maximumLongSide và maximumShortSide độc lập. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Hình chữ nhật nhỏ nhất | `maximumLongSide và maximumShortSide.` | State chung mà mọi current đọc hoặc mutate | `hai maximum = 0 vì chưa đọc card nào.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `card hiện tại với longSide và shortSide.` | Current của đúng iteration/candidate hiện tại | Do `quét từng [width,height].` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `hai cạnh normalized chỉ sống trong lượt.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `diện tích ví nhỏ nhất chứa mọi card sau khi được xoay.` | Contract output của Hình chữ nhật nhỏ nhất | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `let maxLong=0,maxShort=0;`; lựa chọn này giữ đúng thông tin mà check `cạnh hiện tại có lớn hơn maximum cùng vai trò không.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `quét từng [width,height].`. Đây là lý do code dùng block bắt đầu bởi `for (const [width,height] of sizes) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “cạnh hiện tại có lớn hơn maximum cùng vai trò không.”. Trong dry run `[60,50],[30,70]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `let maxLong=0,maxShort=0;`.
- **Tiến triển:** for...of tự sang card kế tiếp.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `cập nhật maximumLongSide và maximumShortSide độc lập.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (const [width,height] of sizes) {`.
- **Cleanup:** không mutate orientation gốc; không ghép long của card này với short chưa normalize.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Hình chữ nhật nhỏ nhất:** Condition của scan/simulation phải theo đúng tie-break và thứ tự rule của đề. So sánh strict hay non-strict chỉ đổi khi boundary/tie của bài chứng minh, không vì thói quen viết comparator.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Hình chữ nhật nhỏ nhất | maximumLongSide và maximumShortSide. | `let maxLong=0,maxShort=0;` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | quét từng [width,height]. | `for (const [width,height] of sizes) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | cạnh hiện tại có lớn hơn maximum cùng vai trò không. | `let maxLong=0,maxShort=0;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | cập nhật maximumLongSide và maximumShortSide độc lập. | `maxLong=Math.max(maxLong,Math.max(width,height));` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | for...of tự sang card kế tiếp. | `for (const [width,height] of sizes) {` | Sau read/update theo invariant của Hình chữ nhật nhỏ nhất |
| Hoàn tất và trả contract output | không mutate orientation gốc; không ghép long của card này với short chưa normalize. | `return maxLong*maxShort;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Hình chữ nhật nhỏ nhất** là **toàn bộ `minimumWalletArea` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Hình chữ nhật nhỏ nhất:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function minimumWalletArea(sizes) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Hình chữ nhật nhỏ nhất:** Những dữ kiện nào trong `maximumLongSide và maximumShortSide.` phải còn nguyên khi current kế tiếp tới? Block `let maxLong=0,maxShort=0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `quét từng [width,height].`, state nào sẽ bị reset?

```js
function minimumWalletArea(sizes) {
  let maxLong=0,maxShort=0;
}
```

**Bước 3 — main loop và scope**

> **Prediction checkpoint — loop của Hình chữ nhật nhỏ nhất:** Header `for (const [width,height] of sizes) {` lặp per item, per candidate hay tới khi state rỗng? Current `card hiện tại với longSide và shortSide.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumWalletArea(sizes) {
  let maxLong=0,maxShort=0;
  for (const [width,height] of sizes) {
  }
}
```

**Bước 4 — transition và movement**

> **Prediction checkpoint — transition của Hình chữ nhật nhỏ nhất:** Nhìn các brace đang mở để quyết định `for (const [width,height] of sizes) {`, `maxLong=Math.max(maxLong,Math.max(width,height));`, `maxShort=Math.max(maxShort,Math.min(width,height));` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `cập nhật maximumLongSide và maximumShortSide độc lập.`, và movement `for...of tự sang card kế tiếp.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumWalletArea(sizes) {
  let maxLong=0,maxShort=0;
  for (const [width,height] of sizes) {
    maxLong=Math.max(maxLong,Math.max(width,height));
    maxShort=Math.max(maxShort,Math.min(width,height));
  }
}
```

**Bước 5 — cleanup/return**

> **Prediction checkpoint — finalization của Hình chữ nhật nhỏ nhất:** Dòng `return maxLong*maxShort;` chạy per current hay đúng một lần sau khi `quét từng [width,height].` kết thúc? Trước return, cleanup `không mutate orientation gốc; không ghép long của card này với short chưa normalize.` và output `diện tích ví nhỏ nhất chứa mọi card sau khi được xoay.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function minimumWalletArea(sizes) {
  let maxLong=0,maxShort=0;
  for (const [width,height] of sizes) {
    maxLong=Math.max(maxLong,Math.max(width,height));
    maxShort=Math.max(maxShort,Math.min(width,height));
  }
  return maxLong*maxShort;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `let maxLong=0,maxShort=0;` | Trước traversal trong main function | maximumLongSide và maximumShortSide. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Hình chữ nhật nhỏ nhất |
| `card hiện tại với longSide và shortSide.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `let maxLong=0,maxShort=0;` | Sau khi state guard/current đã sẵn | Condition đọc hai cạnh normalized chỉ sống trong lượt. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `maxLong=Math.max(maxLong,Math.max(width,height));` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi cạnh hiện tại có lớn hơn maximum cùng vai trò không. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (const [width,height] of sizes) {` | for...of tự sang card kế tiếp. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không mutate orientation gốc; không ghép long của card này với short chưa normalize. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return maxLong*maxShort;` | Sau finalization của main function | Output phải đạt diện tích ví nhỏ nhất chứa mọi card sau khi được xoay. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Hình chữ nhật nhỏ nhất** dùng `[60,50],[30,70]`: dừng ngay khi `maximumLongSide và maximumShortSide.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `minimumWalletArea` với `[60,50],[30,70]`; theo dõi đúng output `diện tích ví nhỏ nhất chứa mọi card sau khi được xoay.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | hai maximum = 0 vì chưa đọc card nào. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | maximumLongSide và maximumShortSide. | card hiện tại với longSide và shortSide. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | hai cạnh normalized chỉ sống trong lượt. | cập nhật maximumLongSide và maximumShortSide độc lập. | Đổi đúng như từng dòng dry run gốc | Condition cạnh hiện tại có lớn hơn maximum cùng vai trò không. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | for...of tự sang card kế tiếp. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không mutate orientation gốc; không ghép long của card này với short chưa normalize. | hết cards thì return tích hai maximum. | diện tích ví nhỏ nhất chứa mọi card sau khi được xoay. | Không còn transition bắt buộc nào chưa chạy |

Trong **Hình chữ nhật nhỏ nhất**, chỉ dòng thực hiện `cập nhật maximumLongSide và maximumShortSide độc lập.` được đổi output; state còn pending theo `maximumLongSide và maximumShortSide.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Lấy max từng cột trước normalize | `[60,50],[30,70]` | Lượt đầu của `card hiện tại với longSide và shortSide.` mà `cạnh hiện tại có lớn hơn maximum cùng vai trò không.` quyết định branch | Lấy max từng cột trước normalize | `maxLong=Math.max(maxLong,Math.max(width,height));` ở đúng scope |
| sort toàn input không cần | `[60,50]` | Ngay transition đầu tiên thực hiện `cập nhật maximumLongSide và maximumShortSide độc lập.` trên boundary/duplicate của input | sort toàn input không cần | `maxLong=Math.max(maxLong,Math.max(width,height));` với init/strictness của Hình chữ nhật nhỏ nhất |
| mutate pair | `[30,70]` | Lượt đầu movement `for...of tự sang card kế tiếp.` xảy ra trước/sau sai assignment | mutate pair | `maxLong=Math.max(maxLong,Math.max(width,height));` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n)`, space `O(1)`.

**Proof từ operation của Hình chữ nhật nhỏ nhất:** Tách chi phí chuẩn bị (nếu có sort) khỏi traversal. Trong traversal, mỗi item/candidate được xét đúng số lần mô tả bởi loop; không nhân các loop nối tiếp thành tích. Main loop được mô tả là `quét từng [width,height].`; movement là `for...of tự sang card kế tiếp.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `maximumLongSide và maximumShortSide.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Hình chữ nhật nhỏ nhất; test `[60,50],[30,70]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `maximumLongSide và maximumShortSide.`, condition `cạnh hiện tại có lớn hơn maximum cùng vai trò không.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Hình chữ nhật nhỏ nhất. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm tay và brute force

`[60,50]`, `[30,70]` normalize thành `[60,50]`, `[70,30]`; ví cần `70×50`. Brute thử
hai orientation cho mọi card tạo `2^n` cấu hình. Vì từng card xoay độc lập, đặt cạnh dài
cùng một chiều loại toàn bộ nhánh, rồi lấy hai maximum.

#### 20. State, Blueprint, invariant

| State | Nghĩa | Init |
| --- | --- | --- |
| `maximumLongSide` | cạnh dài lớn nhất prefix | `0` |
| `maximumShortSide` | cạnh ngắn lớn nhất prefix | `0` |

```text
OUTPUT area; LOOP từng pair; CURRENT long=max(width,height), short=min;
UPDATE hai maximum; RETURN product; không mutate sizes.
```

Sau mỗi card, hai state là kích thước nhỏ nhất chứa toàn bộ prefix đã normalize. Mỗi maximum
có một card witness nên giảm bất kỳ chiều nào sẽ làm card đó không vừa.

#### 21. Lỗi/test và recall

| Lỗi | Test | Đúng |
| --- | --- | --- |
| không xoay | `[[10,100],[90,20]]` | `2000` |
| max theo cột gốc | cùng test | thừa diện tích |
| min toàn cục | `[[8,6],[7,5]]` | `48` |
| swap pair tại chỗ | lưu clone | input không đổi |

Recall: từng card `long=max`, `short=min`; lấy max của từng role; nhân.

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: diện tích ví nhỏ nhất chứa mọi card sau khi được xoay.
PREPARE: normalize mỗi pair thành long=max và short=min.
GLOBAL STATE: maximumLongSide và maximumShortSide.
INIT: hai maximum = 0 vì chưa đọc card nào.
MAIN LOOP: quét từng [width,height].
CURRENT ITEM: card hiện tại với longSide và shortSide.
PER-ITERATION STATE: hai cạnh normalized chỉ sống trong lượt.
CHECK: cạnh hiện tại có lớn hơn maximum cùng vai trò không.
BRANCH: Math.max xử lý hai check, không cần if riêng.
UPDATE: cập nhật maximumLongSide và maximumShortSide độc lập.
POINTER MOVEMENT: for...of tự sang card kế tiếp.
STOP / RETURN: hết cards thì return tích hai maximum.
CLEANUP: không mutate orientation gốc; không ghép long của card này với short chưa normalize.
```

- Recall 1: Xoay từng card để cạnh dài cùng một vai trò, lấy max của hai vai trò rồi nhân.
- Recall 2: STATE maxLong/maxShort → LOOP cards → CHECK max/min → UPDATE maxima → RETURN product.
- Recall 3: Điền `longSide = Math.___(width,height)` và `shortSide = Math.___(width,height)`.
<!-- dry-run-reconstruction:end -->
