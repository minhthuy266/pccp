# OF030 — Camera kiểm soát

- Nguồn đề: [Programmers lesson 42884](https://school.programmers.co.kr/learn/courses/30/lessons/42884?language=javascript)
- Pattern chính: interval stabbing theo earliest exit
- Mức trong bank: TRANSFER / variant

## 01. Contract

Mỗi xe đi qua đoạn đóng `[entry,exit]`; camera tại một tọa độ phát hiện xe nếu nằm trong đoạn. Đặt ít camera nhất để cover mọi route.

## 02. Bound

Sort interval theo exit rồi scan `O(n log n)`.

## 03. Brute force

Thử subset endpoint làm camera và kiểm cover, exponential.

## 04. Bottleneck

Route có exit sớm nhất chưa cover bắt buộc một camera không muộn hơn exit. Đặt đúng exit là vị trí muộn nhất còn hợp lệ và cover tốt nhất các route sau.

## 05. Pattern

Sort exit tăng; nếu entry `<= lastCamera`, route đã cover; nếu entry `> lastCamera`, đặt camera tại exit.

## 06. State

`lastCamera`, `cameras`, sorted routes.

## 07. Transition

Uncovered route tăng count và cập nhật lastCamera=exit; covered giữ state.

## 08. Invariant

Sau prefix sorted, cameras là minimum cho prefix và camera cuối nằm tại earliest exit của group cuối. Exchange mọi camera cover route earliest-exit sang endpoint đó không làm mất route nào có entry trước/equal endpoint.

## 09. Complexity

Time `O(n log n)`, space `O(n)` do copy.

## 10. Pseudocode

```text
sort by exit
last=-infinity; count=0
for [entry,exit]:
  if entry>last: count++; last=exit
return count
```

## 11. Code bước 1 — dựng state

```js
const sorted=[...routes].sort((a,b)=>a[1]-b[1]);
```

## 12. Code bước 2 — viết transition

```js
function isCovered(entry,lastCamera) { return entry<=lastCamera; }
```

## 13. Code bước 3 — ghép vòng lặp

```js
if (!isCovered(entry,lastCamera)) { cameras++; lastCamera=exit; }
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF030.js](../../../solutions/official/OF030.js).

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
  let lastCameraPosition = -Infinity;

  for (const [entry, exit] of sortedRoutes) {
    const alreadyCovered = entry <= lastCameraPosition;
    if (alreadyCovered) continue;
    cameraCount++;
    lastCameraPosition = exit;
  }

  return cameraCount;
}
```

## 15. Dry-run

`[1,2],[2,3]`: đặt camera tại2 cho route đầu; route hai có entry2≤camera2 nên cùng được cover, answer1.

## 16. Edge cases

Intervals chạm endpoint; nested; all overlap; all disjoint; tọa độ âm.

## 17. Lỗi JavaScript thường gặp

Dùng `<` thay `<=` cho interval đóng; sort entry; đặt midpoint; khởi tạo lastCamera=0 làm sai tọa độ âm.

## 18. Bài transfer cùng pattern

- OF057: interval mở nên boundary condition ngược khác.
- OF028: greedy pairing.
- OF025: greedy neighbor line.

Tín hiệu: minimum points cover closed intervals ⇒ earliest exit; đọc kỹ endpoint open/closed.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF030 Camera kiểm soát

Phần này dùng chính contract, dry run, Blueprint và code executable của **Camera kiểm soát** để dựng lại lời giải; pattern kiểm soát là `Interval-greedy`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mỗi xe đi qua đoạn đóng `[entry,exit]`; camera tại một tọa độ phát hiện xe nếu nằm trong đoạn. Đặt ít camera nhất để cover mọi route.

**Giới hạn quyết định implementation:** Sort interval theo exit rồi scan `O(n log n)`.

### Concrete dry run

**Concrete input:** `[1,2],[2,3]`

`[1,2],[2,3]`: đặt camera tại2 cho route đầu; route hai có entry2≤camera2 nên cùng được cover, answer1.



Decision ledger của **Camera kiểm soát** nối `lastCameraPosition và cameraCount.` với đúng block code; mỗi inner action trong input `[1,2],[2,3]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[1,2],[2,3]` | Chưa có state của Camera kiểm soát | Contract và input đã hợp lệ | lastCameraPosition = -Infinity; cameraCount = 0. | lastCameraPosition và cameraCount. | Chưa chốt output |
| route hiện tại với entry và exit. đi vào | alreadyCovered = entry <= lastCameraPosition. | Với `[1,2],[2,3]`, hỏi: camera cuối có nằm trong closed interval hiện tại không. | covered thì continue; uncovered thì đặt camera mới tại exit.; for...of tự sang route có exit kế tiếp. | lastCameraPosition và cameraCount. sau branch vừa chọn | số camera ít nhất cover mọi closed interval route. chỉ đổi ở branch ghi answer |
| Transition của current | lastCameraPosition và cameraCount. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered. | lastCameraPosition và cameraCount. đã commit current; invariant của Camera kiểm soát được phục hồi | số camera ít nhất cover mọi closed interval route. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không cần; camera cuối đại diện đủ cho quyết định suffix.; hết routes thì return cameraCount. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Camera kiểm soát**, item/candidate/event hiện tại đi vào → đọc `alreadyCovered = entry <= lastCameraPosition.` từ state đang sống → hỏi đúng câu `camera cuối có nằm trong closed interval hiện tại không.` → đi nhánh `covered thì continue; uncovered thì đặt camera mới tại exit.` → thực hiện `cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.` → chuyển sang candidate kế sau khi áp dụng đủ rule theo đúng thứ tự → chỉ chốt `số camera ít nhất cover mọi closed interval route.` ở vị trí mà invariant đã đúng → không cần; camera cuối đại diện đủ cho quyết định suffix. → hết routes thì return cameraCount..

Với **Camera kiểm soát**, chuỗi “đọc route hiện tại với entry và exit. → hỏi camera cuối có nằm trong closed interval hiện tại không. → đổi cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Camera kiểm soát | `lastCameraPosition và cameraCount.` | State chung mà mọi current đọc hoặc mutate | `lastCameraPosition = -Infinity; cameraCount = 0.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `route hiện tại với entry và exit.` | Current của đúng iteration/candidate hiện tại | Do `quét từng [entry, exit] trong sortedRoutes.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `alreadyCovered = entry <= lastCameraPosition.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `số camera ít nhất cover mọi closed interval route.` | Contract output của Camera kiểm soát | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);`; lựa chọn này giữ đúng thông tin mà check `camera cuối có nằm trong closed interval hiện tại không.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `quét từng [entry, exit] trong sortedRoutes.`. Đây là lý do code dùng block bắt đầu bởi `for (const [entry, exit] of sortedRoutes) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “camera cuối có nằm trong closed interval hiện tại không.”. Trong dry run `[1,2],[2,3]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (alreadyCovered) continue;`.
- **Tiến triển:** for...of tự sang route có exit kế tiếp.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `for (const [entry, exit] of sortedRoutes) {`.
- **Cleanup:** không cần; camera cuối đại diện đủ cho quyết định suffix.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Camera kiểm soát:** Condition của scan/simulation phải theo đúng tie-break và thứ tự rule của đề. So sánh strict hay non-strict chỉ đổi khi boundary/tie của bài chứng minh, không vì thói quen viết comparator.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Camera kiểm soát | lastCameraPosition và cameraCount. | `const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | quét từng [entry, exit] trong sortedRoutes. | `for (const [entry, exit] of sortedRoutes) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | camera cuối có nằm trong closed interval hiện tại không. | `if (alreadyCovered) continue;` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered. | `cameraCount++;` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | for...of tự sang route có exit kế tiếp. | `for (const [entry, exit] of sortedRoutes) {` | Sau read/update theo invariant của Camera kiểm soát |
| Hoàn tất và trả contract output | không cần; camera cuối đại diện đủ cho quyết định suffix. | `return cameraCount;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Camera kiểm soát** là **toàn bộ `minimumSpeedCameras` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Camera kiểm soát:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function minimumSpeedCameras(routes) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Camera kiểm soát:** Những dữ kiện nào trong `lastCameraPosition và cameraCount.` phải còn nguyên khi current kế tiếp tới? Block `const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);` chạy đúng một lần trước traversal. Nếu đặt nó trong `quét từng [entry, exit] trong sortedRoutes.`, state nào sẽ bị reset?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Camera kiểm soát:** Những dữ kiện nào trong `lastCameraPosition và cameraCount.` phải còn nguyên khi current kế tiếp tới? Block `let cameraCount = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `quét từng [entry, exit] trong sortedRoutes.`, state nào sẽ bị reset?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Camera kiểm soát:** Những dữ kiện nào trong `lastCameraPosition và cameraCount.` phải còn nguyên khi current kế tiếp tới? Block `let lastCameraPosition = -Infinity;` chạy đúng một lần trước traversal. Nếu đặt nó trong `quét từng [entry, exit] trong sortedRoutes.`, state nào sẽ bị reset?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
  let lastCameraPosition = -Infinity;
}
```

**Bước 5 — main loop và scope**

> **Prediction checkpoint — loop của Camera kiểm soát:** Header `for (const [entry, exit] of sortedRoutes) {` lặp per item, per candidate hay tới khi state rỗng? Current `route hiện tại với entry và exit.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
  let lastCameraPosition = -Infinity;
  for (const [entry, exit] of sortedRoutes) {
  }
}
```

**Bước 6 — transition và movement**

> **Prediction checkpoint — transition của Camera kiểm soát:** Nhìn các brace đang mở để quyết định `for (const [entry, exit] of sortedRoutes) {`, `const alreadyCovered = entry <= lastCameraPosition;`, `if (alreadyCovered) continue;`, `cameraCount++;`, `lastCameraPosition = exit;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.`, và movement `for...of tự sang route có exit kế tiếp.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
  let lastCameraPosition = -Infinity;
  for (const [entry, exit] of sortedRoutes) {
    const alreadyCovered = entry <= lastCameraPosition;
    if (alreadyCovered) continue;
    cameraCount++;
    lastCameraPosition = exit;
  }
}
```

**Bước 7 — cleanup/return**

> **Prediction checkpoint — finalization của Camera kiểm soát:** Dòng `return cameraCount;` chạy per current hay đúng một lần sau khi `quét từng [entry, exit] trong sortedRoutes.` kết thúc? Trước return, cleanup `không cần; camera cuối đại diện đủ cho quyết định suffix.` và output `số camera ít nhất cover mọi closed interval route.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function minimumSpeedCameras(routes) {
  const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);
  let cameraCount = 0;
  let lastCameraPosition = -Infinity;
  for (const [entry, exit] of sortedRoutes) {
    const alreadyCovered = entry <= lastCameraPosition;
    if (alreadyCovered) continue;
    cameraCount++;
    lastCameraPosition = exit;
  }
  return cameraCount;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const sortedRoutes = [...routes].sort((first, second) => first[1] - second[1]);` | Trước traversal trong main function | lastCameraPosition và cameraCount. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Camera kiểm soát |
| `route hiện tại với entry và exit.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (alreadyCovered) continue;` | Sau khi state guard/current đã sẵn | Condition đọc alreadyCovered = entry <= lastCameraPosition. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `cameraCount++;` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi camera cuối có nằm trong closed interval hiện tại không. | Đưa ra ngoài áp dụng update cho cả case false |
| `for (const [entry, exit] of sortedRoutes) {` | for...of tự sang route có exit kế tiếp. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không cần; camera cuối đại diện đủ cho quyết định suffix. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return cameraCount;` | Sau finalization của main function | Output phải đạt số camera ít nhất cover mọi closed interval route. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Camera kiểm soát** dùng `[1,2],[2,3]`: dừng ngay khi `lastCameraPosition và cameraCount.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `minimumSpeedCameras` với `[1,2],[2,3]`; theo dõi đúng output `số camera ít nhất cover mọi closed interval route.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | lastCameraPosition = -Infinity; cameraCount = 0. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | lastCameraPosition và cameraCount. | route hiện tại với entry và exit. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | alreadyCovered = entry <= lastCameraPosition. | cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered. | Đổi đúng như từng dòng dry run gốc | Condition camera cuối có nằm trong closed interval hiện tại không. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | for...of tự sang route có exit kế tiếp. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không cần; camera cuối đại diện đủ cho quyết định suffix. | hết routes thì return cameraCount. | số camera ít nhất cover mọi closed interval route. | Không còn transition bắt buộc nào chưa chạy |

Trong **Camera kiểm soát**, chỉ dòng thực hiện `cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.` được đổi output; state còn pending theo `lastCameraPosition và cameraCount.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Dùng '<' thay '<=' cho interval đóng | `[1,2],[2,3]` | Lượt đầu của `route hiện tại với entry và exit.` mà `camera cuối có nằm trong closed interval hiện tại không.` quyết định branch | Dùng '<' thay '<=' cho interval đóng | `if (alreadyCovered) continue;` ở đúng scope |
| sort entry | `sort exit → if entry>last place at exit` | Ngay transition đầu tiên thực hiện `cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.` trên boundary/duplicate của input | sort entry | `cameraCount++;` với init/strictness của Camera kiểm soát |
| đặt midpoint | `[1,2],[2,3]` | Lượt đầu movement `for...of tự sang route có exit kế tiếp.` xảy ra trước/sau sai assignment | đặt midpoint | `for (const [entry, exit] of sortedRoutes) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n log n)`, space `O(n)` do copy.

**Proof từ operation của Camera kiểm soát:** Tách chi phí chuẩn bị (nếu có sort) khỏi traversal. Trong traversal, mỗi item/candidate được xét đúng số lần mô tả bởi loop; không nhân các loop nối tiếp thành tích. Main loop được mô tả là `quét từng [entry, exit] trong sortedRoutes.`; movement là `for...of tự sang route có exit kế tiếp.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `lastCameraPosition và cameraCount.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Camera kiểm soát; test `[1,2],[2,3]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `lastCameraPosition và cameraCount.`, condition `camera cuối có nằm trong closed interval hiện tại không.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Camera kiểm soát. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Problem → Code hoàn chỉnh

**Làm tay:** lấy route chưa cover có exit sớm nhất. Một camera bắt buộc phải nằm không muộn hơn
exit đó; đặt đúng exit là vị trí muộn nhất còn hợp lệ, nên có cơ hội cover nhiều route sau nhất.

```js
function minimumCamerasBruteForce(routes) {
  const candidates = [...new Set(routes.flat())];
  let best = Infinity;
  function choose(index, cameras) {
    if (cameras.length >= best) return;
    if (index === candidates.length) {
      const coversAll = routes.every(([entry, exit]) =>
        cameras.some((position) => entry <= position && position <= exit),
      );
      if (coversAll) best = cameras.length;
      return;
    }
    choose(index + 1, cameras);
    cameras.push(candidates[index]);
    choose(index + 1, cameras);
    cameras.pop();
  }
  choose(0, []);
  return best;
}
```

| State | Kiểu | Lưu gì | Init/lý do | Scope |
| --- | --- | --- | --- | --- |
| `sortedRoutes` | interval[] | route theo exit tăng | sorted copy | toàn hàm |
| `lastCameraPosition` | number | camera cuối | `-Infinity` cho tọa độ âm | xuyên loop |
| `cameraCount` | number | camera đã đặt | `0` | output |

```text
OUTPUT: cameraCount; PREPARE: sort by exit; STATE: lastCamera,count; INIT: -Infinity,0
LOOP: for route; CURRENT: [entry,exit]; CHECK: entry<=lastCamera?
BRANCH: covered → continue; uncovered → count++,lastCamera=exit
POINTER: for; STOP: hết routes; CLEANUP: không; RETURN count
```

Invariant: camera set là minimum cho prefix sorted; exchange camera cover earliest-exit route
sang exit không làm mất route đang overlap. Sort `O(n log n)`, scan `O(n)`. **Lỗi/test:** interval
đóng dùng `<=`; `[1,2],[2,3]` cần một camera; sort entry sai proof; init 0 sai tọa độ âm;
mutate input khi sort. Recall: `sort exit → if entry>last place at exit`.

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: số camera ít nhất cover mọi closed interval route.
PREPARE: clone routes rồi sort theo exit tăng để không mutate input.
GLOBAL STATE: lastCameraPosition và cameraCount.
INIT: lastCameraPosition = -Infinity; cameraCount = 0.
MAIN LOOP: quét từng [entry, exit] trong sortedRoutes.
CURRENT ITEM: route hiện tại với entry và exit.
PER-ITERATION STATE: alreadyCovered = entry <= lastCameraPosition.
CHECK: camera cuối có nằm trong closed interval hiện tại không.
BRANCH: covered thì continue; uncovered thì đặt camera mới tại exit.
UPDATE: cameraCount += 1 và lastCameraPosition = exit trong cùng nhánh uncovered.
POINTER MOVEMENT: for...of tự sang route có exit kế tiếp.
STOP / RETURN: hết routes thì return cameraCount.
CLEANUP: không cần; camera cuối đại diện đủ cho quyết định suffix.
```

- Recall 1: Sort theo exit; route chưa được cover buộc đặt camera tại exit sớm nhất của nó.
- Recall 2: STATE last/count → LOOP sorted routes → CHECK entry<=last → UPDATE exit/count → RETURN count.
- Recall 3: Điền `alreadyCovered = entry ___ lastCameraPosition`; uncovered thì `lastCameraPosition = ___`.
<!-- dry-run-reconstruction:end -->
