# OF024 — Từ điển nguyên âm

- Nguồn đề: [Programmers lesson 84512](https://school.programmers.co.kr/learn/courses/30/lessons/84512?language=javascript)
- Pattern chính: preorder DFS enumeration
- Mức trong bank: TRANSFER / variant

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Từ điển chứa mọi string dài 1..5 dùng `A,E,I,O,U`, sắp xếp từ điển. Trả vị trí 1-based của `word`.

## 02. Bound

Tổng số từ chỉ `5+25+125+625+3125=3905`; DFS enumerate toàn bộ an toàn. Không cần công thức nếu mục tiêu luyện traversal order.

## 03. Brute force

Sinh mọi string, đưa vào array, sort rồi indexOf. Đúng nhưng sort thừa nếu sinh ngay theo lexicographic preorder.

## 04. Bottleneck

Trong dictionary, prefix đứng trước mọi extension của nó: `A`, `AA`, `AAA`... Đây chính là preorder DFS nếu duyệt vowels theo thứ tự.

## 05. Pattern

Tại prefix, lần lượt append A,E,I,O,U; count word mới ngay trước khi recurse sâu hơn. Dừng depth 5 hoặc khi tìm target.

## 06. State

- `prefix` path hiện tại;
- `rank` số từ đã emit;
- `answer`;
- alphabet cố định.

## 07. Transition

`next=prefix+vowel`; rank++; check target; nếu length<5 recurse next. Khi child trả true, propagate để dừng toàn traversal.

## 08. Invariant

Trước mỗi emit, rank bằng số word đứng trước next trong thứ tự từ điển. Preorder đúng vì prefix ngắn đứng trước descendants và sibling vowel được duyệt A→U.

## 09. Complexity

Worst `O(3905*5)` thao tác string, recursion depth 5, space `O(5)` ngoài strings.

## 10. Pseudocode

```text
rank=0
dfs(prefix):
  for vowel in A,E,I,O,U:
    next=prefix+vowel; rank++
    if next==word: save; return true
    if len(next)<5 and dfs(next): return true
  return false
```

## 11. Code bước 1 — dựng state

```js
const vowels=["A","E","I","O","U"];
```

## 12. Code bước 2 — viết transition

```js
function extend(prefix,vowel) {
  return prefix+vowel;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function canDescend(prefix) {
  return prefix.length<5;
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF024.js](../../../solutions/official/OF024.js).

```js
const VOWELS = ["A", "E", "I", "O", "U"];

function vowelDictionaryRank(word) {
  let rank = 0;
  let answer = 0;

  function dfs(prefix) {
    for (const vowel of VOWELS) {
      const next = prefix + vowel;
      rank++;
      if (next === word) {
        answer = rank;
        return true;
      }
      if (next.length < 5 && dfs(next)) {
        return true;
      }
    }
    return false;
  }

  dfs("");
  return answer;
}
```

## 15. Dry-run

Traversal đầu: `A` rank1, `AA` rank2, `AAA` rank3, `AAAA` rank4, `AAAAA` rank5, `AAAAE` rank6. Vì vậy sample `AAAAE` có rank 6.

## 16. Edge cases

1. `A` rank 1.
2. Word dài 5 ở nhánh sâu.
3. Word bắt đầu U nằm gần cuối.
4. Propagate return để không tiếp tục tăng rank sau khi tìm thấy.

## 17. Lỗi JavaScript thường gặp

- BFS tạo order theo length, không phải dictionary.
- Check target sau recurse, làm prefix đứng sau descendants.
- Recurse khi length đã 5.
- Quên propagate true.
- Sort alphabet sai order.

## 18. Bài transfer cùng pattern

- OF036: binary choice tree, đếm leaf target.
- OF020: permutation và deduplicate prime.
- OF041: DFS path với lexical ordering và used tickets.

Tín hiệu: cần enumerate string theo dictionary trong prefix tree ⇒ preorder DFS với alphabet sorted.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF024 Từ điển nguyên âm

Phần này dùng chính contract, dry run, Blueprint và code executable của **Từ điển nguyên âm** để dựng lại lời giải; pattern kiểm soát là `Enumeration-DFS`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Từ điển chứa mọi string dài 1..5 dùng `A,E,I,O,U`, sắp xếp từ điển. Trả vị trí 1-based của `word`.

**Giới hạn quyết định implementation:** Tổng số từ chỉ `5+25+125+625+3125=3905`; DFS enumerate toàn bộ an toàn. Không cần công thức nếu mục tiêu luyện traversal order.

### Concrete dry run

**Concrete input:** `A`

Traversal đầu: `A` rank1, `AA` rank2, `AAA` rank3, `AAAA` rank4, `AAAAA` rank5, `AAAAE` rank6. Vì vậy sample `AAAAE` có rank 6.



Decision ledger của **Từ điển nguyên âm** nối `rank và found flag/result.` với đúng block code; mỗi inner action trong input `A` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `A` | Chưa có state của Từ điển nguyên âm | Contract và input đã hợp lệ | rank=0; prefix rỗng. | rank và found flag/result. | Chưa chốt output |
| nextWord = prefix + character. đi vào | character và nextWord của branch. | Với `A`, hỏi: nextWord có bằng target; length đã bằng 5 chưa. | match thì lưu/trả true; chưa match và còn depth thì recurse.; for sang sibling; recursion tăng word length. | rank và found flag/result. sau branch vừa chọn | thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5. chỉ đổi ở branch ghi answer |
| Transition của current | rank và found flag/result. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | rank += 1 ngay khi tạo word, trước khi đi vào descendants. | rank và found flag/result. đã commit current; invariant của Từ điển nguyên âm được phục hồi | thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | prefix string immutable nên không cần pop/restore.; found truyền true ngược để dừng toàn traversal; cuối trả rank. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Từ điển nguyên âm**, đỉnh hoặc frontier entry hiện tại đi vào → đọc `character và nextWord của branch.` từ state đang sống → hỏi đúng câu `nextWord có bằng target; length đã bằng 5 chưa.` → đi nhánh `match thì lưu/trả true; chưa match và còn depth thì recurse.` → thực hiện `rank += 1 ngay khi tạo word, trước khi đi vào descendants.` → chuyển sang neighbor hợp lệ sau guard bounds/visited hoặc push candidate mới vào frontier → chỉ chốt `thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.` ở vị trí mà invariant đã đúng → prefix string immutable nên không cần pop/restore. → found truyền true ngược để dừng toàn traversal; cuối trả rank..

Với **Từ điển nguyên âm**, chuỗi “đọc nextWord = prefix + character. → hỏi nextWord có bằng target; length đã bằng 5 chưa. → đổi rank += 1 ngay khi tạo word, trước khi đi vào descendants. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Từ điển nguyên âm | `rank và found flag/result.` | State chung mà mọi current đọc hoặc mutate | `rank=0; prefix rỗng.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `nextWord = prefix + character.` | Current của đúng iteration/candidate hiện tại | Do `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `character và nextWord của branch.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.` | Contract output của Từ điển nguyên âm | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `let rank = 0;`; lựa chọn này giữ đúng thông tin mà check `nextWord có bằng target; length đã bằng 5 chưa.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.`. Đây là lý do code dùng block bắt đầu bởi `function dfs(prefix) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “nextWord có bằng target; length đã bằng 5 chưa.”. Trong dry run `A`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (next === word) {`.
- **Tiến triển:** for sang sibling; recursion tăng word length.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `rank += 1 ngay khi tạo word, trước khi đi vào descendants.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `rank++;`.
- **Cleanup:** prefix string immutable nên không cần pop/restore.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Từ điển nguyên âm:** Guard visited, bounds hoặc stale distance phải chạy trước transition tương ứng. Với BFS, mark visited lúc enqueue để một đỉnh không xếp hàng nhiều lần; với Dijkstra, bỏ entry stale trước khi relax cạnh.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Từ điển nguyên âm | rank và found flag/result. | `let rank = 0;` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order. | `function dfs(prefix) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | nextWord có bằng target; length đã bằng 5 chưa. | `if (next === word) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | rank += 1 ngay khi tạo word, trước khi đi vào descendants. | `rank++;` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | for sang sibling; recursion tăng word length. | `rank++;` | Sau read/update theo invariant của Từ điển nguyên âm |
| Hoàn tất và trả contract output | prefix string immutable nên không cần pop/restore. | `return answer;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Từ điển nguyên âm** là **toàn bộ `vowelDictionaryRank` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Từ điển nguyên âm:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function vowelDictionaryRank(word) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Từ điển nguyên âm:** Những dữ kiện nào trong `rank và found flag/result.` phải còn nguyên khi current kế tiếp tới? Block `let rank = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.`, state nào sẽ bị reset?

```js
function vowelDictionaryRank(word) {
  let rank = 0;
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Từ điển nguyên âm:** Những dữ kiện nào trong `rank và found flag/result.` phải còn nguyên khi current kế tiếp tới? Block `let answer = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.`, state nào sẽ bị reset?

```js
function vowelDictionaryRank(word) {
  let rank = 0;
  let answer = 0;
}
```

**Bước 4 — cleanup/return**

> **Prediction checkpoint — finalization của Từ điển nguyên âm:** Dòng `function dfs(prefix) {`, `for (const vowel of VOWELS) {`, `const next = prefix + vowel;`, `rank++;`, `if (next === word) {` chạy per current hay đúng một lần sau khi `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.` kết thúc? Trước return, cleanup `prefix string immutable nên không cần pop/restore.` và output `thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function vowelDictionaryRank(word) {
  let rank = 0;
  let answer = 0;
  function dfs(prefix) {
    for (const vowel of VOWELS) {
      const next = prefix + vowel;
      rank++;
      if (next === word) {
        answer = rank;
        return true;
      }
      if (next.length < 5 && dfs(next)) {
        return true;
      }
    }
    return false;
  }
}
```

**Bước 5 — transition và movement**

> **Prediction checkpoint — transition của Từ điển nguyên âm:** Nhìn các brace đang mở để quyết định `dfs("");` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `rank += 1 ngay khi tạo word, trước khi đi vào descendants.`, và movement `for sang sibling; recursion tăng word length.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function vowelDictionaryRank(word) {
  let rank = 0;
  let answer = 0;
  function dfs(prefix) {
    for (const vowel of VOWELS) {
      const next = prefix + vowel;
      rank++;
      if (next === word) {
        answer = rank;
        return true;
      }
      if (next.length < 5 && dfs(next)) {
        return true;
      }
    }
    return false;
  }
  dfs("");
}
```

**Bước 6 — cleanup/return**

> **Prediction checkpoint — finalization của Từ điển nguyên âm:** Dòng `return answer;` chạy per current hay đúng một lần sau khi `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.` kết thúc? Trước return, cleanup `prefix string immutable nên không cần pop/restore.` và output `thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function vowelDictionaryRank(word) {
  let rank = 0;
  let answer = 0;
  function dfs(prefix) {
    for (const vowel of VOWELS) {
      const next = prefix + vowel;
      rank++;
      if (next === word) {
        answer = rank;
        return true;
      }
      if (next.length < 5 && dfs(next)) {
        return true;
      }
    }
    return false;
  }
  dfs("");
  return answer;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `let rank = 0;` | Trước traversal trong main function | rank và found flag/result. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Từ điển nguyên âm |
| `nextWord = prefix + character.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (next === word) {` | Sau khi state guard/current đã sẵn | Condition đọc character và nextWord của branch. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `rank++;` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi nextWord có bằng target; length đã bằng 5 chưa. | Đưa ra ngoài áp dụng update cho cả case false |
| `rank++;` | for sang sibling; recursion tăng word length. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: prefix string immutable nên không cần pop/restore. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return answer;` | Sau finalization của main function | Output phải đạt thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Từ điển nguyên âm** dùng `A`: dừng ngay khi `rank và found flag/result.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `vowelDictionaryRank` với `A`; theo dõi đúng output `thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | rank=0; prefix rỗng. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | rank và found flag/result. | nextWord = prefix + character. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | character và nextWord của branch. | rank += 1 ngay khi tạo word, trước khi đi vào descendants. | Đổi đúng như từng dòng dry run gốc | Condition nextWord có bằng target; length đã bằng 5 chưa. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | for sang sibling; recursion tăng word length. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | prefix string immutable nên không cần pop/restore. | found truyền true ngược để dừng toàn traversal; cuối trả rank. | thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5. | Không còn transition bắt buộc nào chưa chạy |

Trong **Từ điển nguyên âm**, chỉ dòng thực hiện `rank += 1 ngay khi tạo word, trước khi đi vào descendants.` được đổi output; state còn pending theo `rank và found flag/result.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| BFS tạo order theo length, không phải dictionary. | `A, AA, AAA, AAAA, AAAAA, AAAAE, ...` | Lượt đầu của `nextWord = prefix + character.` mà `nextWord có bằng target; length đã bằng 5 chưa.` quyết định branch | BFS tạo order theo length, không phải dictionary. | `function dfs(prefix) {` ở đúng scope |
| Check target sau recurse, làm prefix đứng sau descendants. | `A,E,I,O,U` | Ngay transition đầu tiên thực hiện `rank += 1 ngay khi tạo word, trước khi đi vào descendants.` trên boundary/duplicate của input | Check target sau recurse, làm prefix đứng sau descendants. | `if (next === word) {` với init/strictness của Từ điển nguyên âm |
| Recurse khi length đã 5. | `"AA"` | Lượt đầu movement `for sang sibling; recursion tăng word length.` xảy ra trước/sau sai assignment | Recurse khi length đã 5. | `rank++;` trước/sau movement đúng như mapping |

### Complexity proof from movements

Worst `O(3905*5)` thao tác string, recursion depth 5, space `O(5)` ngoài strings.

**Proof từ operation của Từ điển nguyên âm:** Đếm theo representation của graph: traversal thường xử lý mỗi đỉnh hữu hạn lần và xét mỗi cạnh theo adjacency; nếu frontier là heap, mỗi push/pop hợp lệ cộng O(log V). Main loop được mô tả là `DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.`; movement là `for sang sibling; recursion tăng word length.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `rank và found flag/result.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Từ điển nguyên âm; test `A` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `rank và found flag/result.`, condition `nextWord có bằng target; length đã bằng 5 chưa.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Từ điển nguyên âm. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Preorder ở nhánh đầu là `A, AA, AAA, AAAA, AAAAA, AAAAE, ...`. Prefix phải được ghi nhận trước
mọi extension của nó; đây là điểm phân biệt với BFS theo độ dài.

```js
function vowelDictionaryRankBrute(word) {
  const vowels = ["A", "E", "I", "O", "U"];
  const words = [];

  function generate(prefix) {
    if (prefix.length > 0) words.push(prefix);
    if (prefix.length === 5) return;
    for (const vowel of vowels) {
      generate(prefix + vowel);
    }
  }

  generate("");
  words.sort();
  return words.indexOf(word) + 1;
}
```

Brute force sort lại 3.905 từ. Bản tối ưu sinh đúng lexical preorder nên có thể dừng ngay khi gặp target.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `prefix` | đường từ root tới node hiện tại | chuỗi rỗng |
| `rank` | số từ đã emit | `0` |
| `answer` | rank của target | `0` |
| `next` | child đang xét | `prefix + vowel` |
| giá trị trả về DFS | subtree đã tìm thấy target chưa | boolean |

#### 21. Blueprint từ đề đến code

1. Xem mỗi prefix như một node trong cây 5 nhánh.
2. Vì prefix đứng trước extension, chọn preorder chứ không chọn BFS/postorder.
3. Duyệt alphabet theo đúng `A,E,I,O,U` để sibling có lexical order.
4. Tăng rank khi tạo một word, kiểm target trước khi đi sâu.
5. Dừng ở length 5 và truyền `true` ngược lên để dừng toàn traversal.

#### 22. Invariant, lỗi lộ diện và recall

Ngay trước khi kiểm `next`, `rank` bằng số word đã xuất trước nó. Preorder bảo đảm toàn bộ subtree
của một vowel được xuất trước sibling vowel tiếp theo, đúng thứ tự từ điển. Khi child trả `true`,
mọi ancestor cũng phải trả ngay; nếu không, rank vẫn tăng sau khi đã tìm đáp án.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| BFS theo length | `"AA"` | 2 |
| check sau khi recurse | `"A"` | 1 |
| recurse quá depth 5 | `"AAAAE"` | 6 |
| không propagate early return | `"E"` | 782 |

Recall: **dictionary của prefix tree ⇒ alphabet sorted + preorder; emit prefix trước descendants.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: thứ tự 1-based của target trong dictionary A,E,I,O,U dài tối đa 5.
PREPARE: alphabet cố định đúng lexical order.
GLOBAL STATE: rank và found flag/result.
INIT: rank=0; prefix rỗng.
MAIN LOOP: DFS preorder; từ mỗi prefix thử năm chữ theo alphabet order.
CURRENT ITEM: nextWord = prefix + character.
PER-ITERATION STATE: character và nextWord của branch.
CHECK: nextWord có bằng target; length đã bằng 5 chưa.
BRANCH: match thì lưu/trả true; chưa match và còn depth thì recurse.
UPDATE: rank += 1 ngay khi tạo word, trước khi đi vào descendants.
POINTER MOVEMENT: for sang sibling; recursion tăng word length.
STOP / RETURN: found truyền true ngược để dừng toàn traversal; cuối trả rank.
CLEANUP: prefix string immutable nên không cần pop/restore.
```

- Recall 1: Dictionary là preorder của prefix tree; emit word trước descendants và duyệt A,E,I,O,U.
- Recall 2: STATE rank → LOOP DFS alphabet → CHECK target/length → UPDATE rank/recurse → RETURN rank.
- Recall 3: Điền `rank += ___` trước check target; recurse chỉ khi `nextWord.length < ___`.
<!-- dry-run-reconstruction:end -->
