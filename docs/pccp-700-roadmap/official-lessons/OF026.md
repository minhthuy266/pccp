# OF026 — Cần điều khiển

- Nguồn đề: [Programmers lesson 42860](https://school.programmers.co.kr/learn/courses/30/lessons/42860?language=javascript)
- Pattern chính: independent vertical cost + horizontal route case analysis
- Mức trong bank: STRETCH / special case

## 01. Contract

Từ chuỗi toàn `A`, đổi thành `name`. Lên/xuống đổi chữ vòng tròn A–Z; trái/phải di chuyển cursor vòng tròn. Tìm tổng thao tác nhỏ nhất.

## 02. Bound

Chi phí đổi ký tự độc lập theo vị trí; phần khó là route ngang bỏ qua các đoạn `A`. Scan mọi điểm quay đầu `O(n²)` worst case với tìm next, vẫn đủ bound.

## 03. Brute force

BFS trên toàn state `(string,cursor)` có không gian khổng lồ; hoặc thử mọi thứ tự thăm vị trí cần đổi.

## 04. Bottleneck

Vertical không phụ thuộc route. Horizontal optimum có thể đi thẳng, đi phải rồi quay trái, hoặc trái rồi quay phải quanh một run A. Chỉ cần xét boundary mỗi run A.

## 05. Pattern

Vertical tại chữ index x là `min(x,26-x)`. Horizontal ban đầu `n-1`; với vị trí i và next non-A sau run:

```text
min(2*i + n-next, i + 2*(n-next))
```

## 06. State

`verticalMoves`, `horizontalMoves`, current `index`, `next` non-A.

## 07. Transition

Cộng vertical từng char. Từ mỗi i, scan next qua A rồi relax hai route quay đầu.

## 08. Invariant

Vertical sum luôn là minimum độc lập cho prefix ký tự. Horizontal là minimum trong mọi route candidate đã xét; mọi route tối ưu chỉ cần đổi hướng tại boundary của vùng cần thăm, tương ứng một run A được bỏ qua.

## 09. Complexity

Time `O(n²)` worst case do scan run cho mỗi i; space `O(1)`.

## 10. Pseudocode

```text
vertical=0; horizontal=n-1
for i:
  vertical += min(letterIndex,26-letterIndex)
  next=i+1; skip A
  horizontal=min(horizontal,2*i+n-next,i+2*(n-next))
return vertical+horizontal
```

## 11. Code bước 1 — dựng state

```js
const alphabetIndex=name.charCodeAt(index)-"A".charCodeAt(0);
```

## 12. Code bước 2 — viết transition

```js
const vertical=Math.min(alphabetIndex,26-alphabetIndex);
```

## 13. Code bước 3 — ghép vòng lặp

```js
let next=index+1;
while (next<name.length && name[next]==="A") next++;
horizontal=Math.min(horizontal,index*2+name.length-next,index+(name.length-next)*2);
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF026.js](../../../solutions/official/OF026.js).

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);

  for (let index = 0; index < length; index++) {
    const alphabetIndex = name.charCodeAt(index) - 65;
    verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);

    let next = index + 1;
    while (next < length && name[next] === "A") {
      next += 1;
    }

    horizontalMoves = Math.min(
      horizontalMoves,
      index * 2 + length - next,
      index + (length - next) * 2,
    );
  }

  return verticalMoves + horizontalMoves;
}
```

## 15. Dry-run

Tên toàn A có vertical 0; mọi i có thể làm horizontal về 0. Với run A dài giữa các ký tự cần đổi, hai công thức so đi phải-quay lại và đi trái-quay lại.

## 16. Edge cases

Toàn A; một ký tự; run A ở giữa; ký tự cần đổi ở hai đầu; Z dùng một bước xuống.

## 17. Lỗi JavaScript thường gặp

Chỉ dùng n-1 horizontal; quên wrap alphabet; bỏ một trong hai công thức quay đầu; horizontal âm khi n=0; nhầm `next` là length A thay vì index sau run.

## 18. Bài transfer cùng pattern

- OF030: greedy có proof endpoint rõ hơn.
- OF028: two-end route decision.
- OF040: coordinate modeling đặc thù.

Tín hiệu: cost tách thành độc lập theo node + route thăm node, hãy giải hai phần riêng rồi xét đầy đủ route case.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF026 Cần điều khiển

Phần này dùng chính contract, dry run, Blueprint và code executable của **Cần điều khiển** để dựng lại lời giải; pattern kiểm soát là `Greedy-case-analysis`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Từ chuỗi toàn `A`, đổi thành `name`. Lên/xuống đổi chữ vòng tròn A–Z; trái/phải di chuyển cursor vòng tròn. Tìm tổng thao tác nhỏ nhất.

**Giới hạn quyết định implementation:** Chi phí đổi ký tự độc lập theo vị trí; phần khó là route ngang bỏ qua các đoạn `A`. Scan mọi điểm quay đầu `O(n²)` worst case với tìm next, vẫn đủ bound.

### Concrete dry run

**Concrete input:** `name="AAAA"`

Tên toàn A có vertical 0; mọi i có thể làm horizontal về 0. Với run A dài giữa các ký tự cần đổi, hai công thức so đi phải-quay lại và đi trái-quay lại.



Decision ledger của **Cần điều khiển** nối `verticalMoves và minimumHorizontalMoves.` với đúng block code; mỗi inner action trong input `name="AAAA"` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `name="AAAA"` | Chưa có state của Cần điều khiển | Contract và input đã hợp lệ | verticalMoves = 0; horizontal = max(0, length - 1). | verticalMoves và minimumHorizontalMoves. | Chưa chốt output |
| character hiện tại; ở horizontal là index và nextNonAIndex. đi vào | upward, downward và hai candidate route quay đầu. | Với `name="AAAA"`, hỏi: ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn. | vertical lấy min lên/wrap; horizontal relax cả phải-trước và trái-trước.; index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while. | verticalMoves và minimumHorizontalMoves. sau branch vừa chọn | tổng số lần điều khiển nhỏ nhất để đổi A-string thành name. chỉ đổi ở branch ghi answer |
| Transition của current | verticalMoves và minimumHorizontalMoves. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate. | verticalMoves và minimumHorizontalMoves. đã commit current; invariant của Cần điều khiển được phục hồi | tổng số lần điều khiển nhỏ nhất để đổi A-string thành name. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | all-A tự cho horizontal về 0; không cần lượt di chuyển giả.; hết mọi điểm quay thì return verticalMoves + minimumHorizontalMoves. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Cần điều khiển**, item/candidate/event hiện tại đi vào → đọc `upward, downward và hai candidate route quay đầu.` từ state đang sống → hỏi đúng câu `ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.` → đi nhánh `vertical lấy min lên/wrap; horizontal relax cả phải-trước và trái-trước.` → thực hiện `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.` → chuyển sang candidate kế sau khi áp dụng đủ rule theo đúng thứ tự → chỉ chốt `tổng số lần điều khiển nhỏ nhất để đổi A-string thành name.` ở vị trí mà invariant đã đúng → all-A tự cho horizontal về 0; không cần lượt di chuyển giả. → hết mọi điểm quay thì return verticalMoves + minimumHorizontalMoves..

Với **Cần điều khiển**, chuỗi “đọc character hiện tại; ở horizontal là index và nextNonAIndex. → hỏi ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn. → đổi cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Cần điều khiển | `verticalMoves và minimumHorizontalMoves.` | State chung mà mọi current đọc hoặc mutate | `verticalMoves = 0; horizontal = max(0, length - 1).` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `character hiện tại; ở horizontal là index và nextNonAIndex.` | Current của đúng iteration/candidate hiện tại | Do `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `upward, downward và hai candidate route quay đầu.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `tổng số lần điều khiển nhỏ nhất để đổi A-string thành name.` | Contract output của Cần điều khiển | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const length = name.length;`; lựa chọn này giữ đúng thông tin mà check `ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.`. Đây là lý do code dùng block bắt đầu bởi `for (let index = 0; index < length; index++) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.”. Trong dry run `name="AAAA"`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `while (next < length && name[next] === "A") {`.
- **Tiến triển:** index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `let next = index + 1;`.
- **Cleanup:** all-A tự cho horizontal về 0; không cần lượt di chuyển giả.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Cần điều khiển:** Condition của scan/simulation phải theo đúng tie-break và thứ tự rule của đề. So sánh strict hay non-strict chỉ đổi khi boundary/tie của bài chứng minh, không vì thói quen viết comparator.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Cần điều khiển | verticalMoves và minimumHorizontalMoves. | `const length = name.length;` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp. | `for (let index = 0; index < length; index++) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn. | `while (next < length && name[next] === "A") {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate. | `for (let index = 0; index < length; index++) {` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while. | `let next = index + 1;` | Sau read/update theo invariant của Cần điều khiển |
| Hoàn tất và trả contract output | all-A tự cho horizontal về 0; không cần lượt di chuyển giả. | `return verticalMoves + horizontalMoves;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Cần điều khiển** là **toàn bộ `joystickMinimumMoves` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Cần điều khiển:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function joystickMinimumMoves(name) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Cần điều khiển:** Những dữ kiện nào trong `verticalMoves và minimumHorizontalMoves.` phải còn nguyên khi current kế tiếp tới? Block `const length = name.length;` chạy đúng một lần trước traversal. Nếu đặt nó trong `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.`, state nào sẽ bị reset?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Cần điều khiển:** Những dữ kiện nào trong `verticalMoves và minimumHorizontalMoves.` phải còn nguyên khi current kế tiếp tới? Block `let verticalMoves = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.`, state nào sẽ bị reset?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Cần điều khiển:** Những dữ kiện nào trong `verticalMoves và minimumHorizontalMoves.` phải còn nguyên khi current kế tiếp tới? Block `let horizontalMoves = Math.max(0, length - 1);` chạy đúng một lần trước traversal. Nếu đặt nó trong `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.`, state nào sẽ bị reset?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
}
```

**Bước 5 — main loop và scope**

> **Prediction checkpoint — loop của Cần điều khiển:** Header `for (let index = 0; index < length; index++) {` lặp per item, per candidate hay tới khi state rỗng? Current `character hiện tại; ở horizontal là index và nextNonAIndex.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
  for (let index = 0; index < length; index++) {
  }
}
```

**Bước 6 — condition/inner branch**

> **Prediction checkpoint — condition của Cần điều khiển:** Trước khi chạy `while (next < length && name[next] === "A") {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
  for (let index = 0; index < length; index++) {
    const alphabetIndex = name.charCodeAt(index) - 65;
    verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);

    let next = index + 1;
    while (next < length && name[next] === "A") {
    }
  }
}
```

**Bước 7 — transition và movement**

> **Prediction checkpoint — transition của Cần điều khiển:** Nhìn các brace đang mở để quyết định `while (next < length && name[next] === "A") {`, `next += 1;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.`, và movement `index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
  for (let index = 0; index < length; index++) {
    const alphabetIndex = name.charCodeAt(index) - 65;
    verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);

    let next = index + 1;
    while (next < length && name[next] === "A") {
      next += 1;
    }
  }
}
```

**Bước 8 — transition và movement**

> **Prediction checkpoint — transition của Cần điều khiển:** Nhìn các brace đang mở để quyết định `for (let index = 0; index < length; index++) {`, `const alphabetIndex = name.charCodeAt(index) - 65;`, `verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);`, `let next = index + 1;`, `while (next < length && name[next] === "A") {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.`, và movement `index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
  for (let index = 0; index < length; index++) {
    const alphabetIndex = name.charCodeAt(index) - 65;
    verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);

    let next = index + 1;
    while (next < length && name[next] === "A") {
      next += 1;
    }

    horizontalMoves = Math.min(
      horizontalMoves,
      index * 2 + length - next,
      index + (length - next) * 2,
    );
  }
}
```

**Bước 9 — cleanup/return**

> **Prediction checkpoint — finalization của Cần điều khiển:** Dòng `return verticalMoves + horizontalMoves;` chạy per current hay đúng một lần sau khi `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.` kết thúc? Trước return, cleanup `all-A tự cho horizontal về 0; không cần lượt di chuyển giả.` và output `tổng số lần điều khiển nhỏ nhất để đổi A-string thành name.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function joystickMinimumMoves(name) {
  const length = name.length;
  let verticalMoves = 0;
  let horizontalMoves = Math.max(0, length - 1);
  for (let index = 0; index < length; index++) {
    const alphabetIndex = name.charCodeAt(index) - 65;
    verticalMoves += Math.min(alphabetIndex, 26 - alphabetIndex);

    let next = index + 1;
    while (next < length && name[next] === "A") {
      next += 1;
    }

    horizontalMoves = Math.min(
      horizontalMoves,
      index * 2 + length - next,
      index + (length - next) * 2,
    );
  }
  return verticalMoves + horizontalMoves;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const length = name.length;` | Trước traversal trong main function | verticalMoves và minimumHorizontalMoves. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Cần điều khiển |
| `character hiện tại; ở horizontal là index và nextNonAIndex.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `while (next < length && name[next] === "A") {` | Sau khi state guard/current đã sẵn | Condition đọc upward, downward và hai candidate route quay đầu. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `for (let index = 0; index < length; index++) {` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn. | Đưa ra ngoài áp dụng update cho cả case false |
| `let next = index + 1;` | index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: all-A tự cho horizontal về 0; không cần lượt di chuyển giả. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return verticalMoves + horizontalMoves;` | Sau finalization của main function | Output phải đạt tổng số lần điều khiển nhỏ nhất để đổi A-string thành name. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Cần điều khiển** dùng `name="AAAA"`: dừng ngay khi `verticalMoves và minimumHorizontalMoves.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `joystickMinimumMoves` với `name="AAAA"`; theo dõi đúng output `tổng số lần điều khiển nhỏ nhất để đổi A-string thành name.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | verticalMoves = 0; horizontal = max(0, length - 1). | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | verticalMoves và minimumHorizontalMoves. | character hiện tại; ở horizontal là index và nextNonAIndex. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | upward, downward và hai candidate route quay đầu. | cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate. | Đổi đúng như từng dòng dry run gốc | Condition ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | all-A tự cho horizontal về 0; không cần lượt di chuyển giả. | hết mọi điểm quay thì return verticalMoves + minimumHorizontalMoves. | tổng số lần điều khiển nhỏ nhất để đổi A-string thành name. | Không còn transition bắt buộc nào chưa chạy |

Trong **Cần điều khiển**, chỉ dòng thực hiện `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.` được đổi output; state còn pending theo `verticalMoves và minimumHorizontalMoves.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Chỉ dùng n-1 horizontal | `Math.min(code, 26 - ___)` | Lượt đầu của `character hiện tại; ở horizontal là index và nextNonAIndex.` mà `ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.` quyết định branch | Chỉ dùng n-1 horizontal | `for (let index = 0; index < length; index++) {` ở đúng scope |
| quên wrap alphabet | `name="AAAA"` | Ngay transition đầu tiên thực hiện `cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.` trên boundary/duplicate của input | quên wrap alphabet | `for (let index = 0; index < length; index++) {` với init/strictness của Cần điều khiển |
| bỏ một trong hai công thức quay đầu | `name="AAAA"` | Lượt đầu movement `index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.` xảy ra trước/sau sai assignment | bỏ một trong hai công thức quay đầu | `for (let index = 0; index < length; index++) {` trước/sau movement đúng như mapping |

### Complexity proof from movements

Time `O(n²)` worst case do scan run cho mỗi i; space `O(1)`.

**Proof từ operation của Cần điều khiển:** Tách chi phí chuẩn bị (nếu có sort) khỏi traversal. Trong traversal, mỗi item/candidate được xét đúng số lần mô tả bởi loop; không nhân các loop nối tiếp thành tích. Main loop được mô tả là `một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.`; movement là `index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `verticalMoves và minimumHorizontalMoves.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Cần điều khiển; test `name="AAAA"` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `verticalMoves và minimumHorizontalMoves.`, condition `ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Cần điều khiển. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với `ABAAAAAAAAABB`, vertical cost là 3 cho ba chữ B. Đi thẳng phải gần hết chuỗi rất tốn; route
quay trái qua vòng rồi đổi hướng quanh run A dài cho horizontal 4, tổng 7.

```js
function joystickMinimumMovesBrute(name) {
  const length = name.length;
  let vertical = 0;
  let targetMask = 0;
  for (let index = 0; index < length; index += 1) {
    const value = name.charCodeAt(index) - 65;
    vertical += Math.min(value, 26 - value);
    if (value !== 0) targetMask |= 1 << index;
  }

  const startMask = name[0] === "A" ? 0 : 1;
  const queue = [[0, startMask, 0]];
  const visited = new Set([`0:${startMask}`]);
  for (let head = 0; head < queue.length; head += 1) {
    const [cursor, mask, moves] = queue[head];
    if ((mask & targetMask) === targetMask) return vertical + moves;
    for (const next of [(cursor + 1) % length, (cursor - 1 + length) % length]) {
      const nextMask = name[next] === "A" ? mask : mask | (1 << next);
      const key = `${next}:${nextMask}`;
      if (visited.has(key)) continue;
      visited.add(key);
      queue.push([next, nextMask, moves + 1]);
    }
  }
  return vertical;
}
```

Brute BFS horizontal dùng bitmask, chỉ phù hợp tên ngắn. Bản tối ưu chứng minh chỉ cần xét boundary run A.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `verticalMoves` | tổng đổi ký tự độc lập | `0` |
| `horizontalMoves` | route ngang tốt nhất đã biết | `length-1` |
| `index` | điểm có thể quay đầu | quét 0..n-1 |
| `next` | vị trí non-A đầu sau run A | `index+1`, rồi skip A |

#### 21. Blueprint từ đề đến code

1. Tách vertical và horizontal vì đổi chữ không phụ thuộc route.
2. Vertical mỗi chữ là min đi lên và wrap đi xuống.
3. Horizontal baseline là đi thẳng phải `n-1`.
4. Với mỗi index, tìm run A kế tiếp có thể bỏ qua.
5. Relax hai hướng quay đầu: phải trước rồi trái, hoặc trái trước rồi phải.
6. Cộng hai cost độc lập.

#### 22. Invariant, lỗi lộ diện và recall

Vertical sum luôn tối ưu từng ký tự độc lập. Sau index i, horizontal là minimum của baseline và mọi
route quay đầu quanh run A đã xét. Một route tối ưu chỉ cần đổi hướng ở boundary vùng cần thăm; mọi
khả năng bỏ một run A được scan nên hai công thức phủ optimum.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| chỉ đi thẳng `n-1` | `ABAAAAAAAAABB` | 7 tổng |
| quên alphabet wrap | `Z` | 1 |
| chỉ xét một hướng quay đầu | non-A gần cả hai đầu | min hai công thức |
| all A vẫn di chuyển | `AAAA` | 0 |

Recall: **joystick = vertical độc lập + horizontal route; scan run A và xét cả hai kiểu quay đầu.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: tổng số lần điều khiển nhỏ nhất để đổi A-string thành name.
PREPARE: tính vertical cost từng ký tự và đặt horizontal baseline = n - 1.
GLOBAL STATE: verticalMoves và minimumHorizontalMoves.
INIT: verticalMoves = 0; horizontal = max(0, length - 1).
MAIN LOOP: một loop cộng vertical; một loop chọn điểm quay đầu và tìm run A kế tiếp.
CURRENT ITEM: character hiện tại; ở horizontal là index và nextNonAIndex.
PER-ITERATION STATE: upward, downward và hai candidate route quay đầu.
CHECK: ký tự cách A bao nhiêu; run A kéo dài tới đâu; route nào ngắn hơn.
BRANCH: vertical lấy min lên/wrap; horizontal relax cả phải-trước và trái-trước.
UPDATE: cộng verticalMoves; gán minimumHorizontalMoves bằng min của ba candidate.
POINTER MOVEMENT: index tăng; nextNonAIndex tiến qua toàn bộ run A bằng while.
STOP / RETURN: hết mọi điểm quay thì return verticalMoves + minimumHorizontalMoves.
CLEANUP: all-A tự cho horizontal về 0; không cần lượt di chuyển giả.
```

- Recall 1: Chi phí chữ độc lập; chi phí ngang phải thử bỏ qua từng run A theo cả hai hướng quay đầu.
- Recall 2: STATE vertical/horizontal → LOOP chars/runs → CHECK wrap/routes → UPDATE minima → RETURN sum.
- Recall 3: Điền `Math.min(code, 26 - ___)` và hai route `2 * index + ___`, `index + 2 * ___`.
<!-- dry-run-reconstruction:end -->
