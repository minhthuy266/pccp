# OF044 — Đá bước

- Nguồn đề: [Programmers lesson 43236](https://school.programmers.co.kr/learn/courses/30/lessons/43236?language=javascript)
- Pattern chính: binary search last feasible + greedy predicate
- Mức trong bank: STRETCH / advanced

## 01. Contract

Giữa start 0 và destination `distance` có rocks. Xóa tối đa n rock để maximize khoảng cách nhỏ nhất giữa các điểm liên tiếp còn lại.

## 02. Bound

Thử subset rock exponential. Answer integer và predicate “đạt minimum gap g được không” đơn điệu; binary search.

## 03. Brute force

Chọn mọi subset xóa, tính min gap, lấy maximum.

## 04. Bottleneck

Với target gap g, scan trái→phải: rock quá gần previous-kept phải xóa. Greedy giữ điểm sớm nhất hợp lệ minimize số removals.

## 05. Pattern

Predicate đếm removals khi `position-previous<g`, gồm destination sentinel. Feasible nếu removals≤n. Tìm last true.

## 06. State

Sorted positions + destination, `previous`, removed; binary low/high/mid.

## 07. Transition

Predicate: gap nhỏ thì remove current; đủ thì keep/update previous. Search feasible mid ⇒ low=mid+1; false ⇒ high=mid; answer low-1.

## 08. Invariant

Greedy previous là điểm kept sớm nhất có thể sau prefix và dùng minimum removals. Predicate true...true,false... theo g. Search interval giữ first false trong `[low,high]`.

## 09. Complexity

Sort `O(R log R)`; predicate `O(R)` × `O(log distance)`; space `O(R)`.

## 10. Pseudocode

```text
sort rocks; append destination
feasible(g): greedily count gaps<g removals
binary search first false g
return g-1
```

## 11. Code bước 1 — dựng state

```js
const positions=[...rocks].sort((a,b)=>a-b);
positions.push(distance);
```

## 12. Code bước 2 — viết transition

```js
if (position-previous<minimumGap) removed++;
else previous=position;
```

## 13. Code bước 3 — ghép vòng lặp

```js
while (low<high) {
  const middle=low+Math.floor((high-low)/2);
  if (feasible(middle)) low=middle+1; else high=middle;
}
```

## 14. Code hoàn chỉnh

Code executable: [solutions/official/OF044.js](../../../solutions/official/OF044.js).

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);

  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }

  let low = 0;
  let high = distance + 1;
  while (low < high) {
    const middle = low + Math.floor((high - low) / 2);
    if (feasible(middle)) {
      low = middle + 1;
    } else {
      high = middle;
    }
  }
  return low - 1;
}
```

## 15. Dry-run

Nếu gap4 cần≤2 removals nhưng gap5 cần>2, last feasible là4; binary search trả first false5 trừ1.

## 16. Edge cases

Không rock; xóa mọi rock; destination gap phải tính; answer 0/1; rock sát start.

## 17. Lỗi JavaScript thường gặp

Không append destination; khi remove vẫn update previous; convention binary lệch; dùng bitwise midpoint; predicate đảo monotonicity.

## 18. Bài transfer cùng pattern

- OF043: first feasible minimum time.
- OF057: direct interval greedy không binary search.

Tín hiệu: maximize minimum + kiểm target bằng greedy monotone ⇒ binary search on answer.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF044 Đá bước

Phần này dùng chính contract, dry run, Blueprint và code executable của **Đá bước** để dựng lại lời giải; pattern kiểm soát là `Binary-search-answer`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Giữa start 0 và destination `distance` có rocks. Xóa tối đa n rock để maximize khoảng cách nhỏ nhất giữa các điểm liên tiếp còn lại.

**Giới hạn quyết định implementation:** Thử subset rock exponential. Answer integer và predicate “đạt minimum gap g được không” đơn điệu; binary search.

### Concrete dry run

**Concrete input:** `distance=25, rocks=[2,14,11,21,17], n=2`

Nếu gap4 cần≤2 removals nhưng gap5 cần>2, last feasible là4; binary search trả first false5 trừ1.



Decision ledger của **Đá bước** nối `binary low/high và predicate removalsNeeded(gap).` với đúng block code; mỗi inner action trong input `distance=25, rocks=[2,14,11,21,17], n=2` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `distance=25, rocks=[2,14,11,21,17], n=2` | Chưa có state của Đá bước | Contract và input đã hợp lệ | low=0 feasible; high=distance+1 infeasible sentinel. | binary low/high và predicate removalsNeeded(gap). | Chưa chốt output |
| candidate gap; trong predicate là currentPosition. đi vào | previousKept và removedCount. | Với `distance=25, rocks=[2,14,11,21,17], n=2`, hỏi: currentPosition-previousKept < gap; removedCount > n. | gap thiếu thì remove current; đủ thì commit previousKept=current.; position loop tăng; binary interval co. | binary low/high và predicate removalsNeeded(gap). sau branch vừa chọn | khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals. chỉ đổi ở branch ghi answer |
| Transition của current | binary low/high và predicate removalsNeeded(gap). trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid. | binary low/high và predicate removalsNeeded(gap). đã commit current; invariant của Đá bước được phục hồi | khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | destination phải tham gia predicate; sorted copy giữ rocks input.; low===high là first false, return low-1. | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Đá bước**, candidate middle của miền tìm kiếm đi vào → đọc `previousKept và removedCount.` từ state đang sống → hỏi đúng câu `currentPosition-previousKept < gap; removedCount > n.` → đi nhánh `gap thiếu thì remove current; đủ thì commit previousKept=current.` → thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.` → loại đúng nửa đã được predicate chứng minh không thể chứa boundary → chỉ chốt `khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.` ở vị trí mà invariant đã đúng → destination phải tham gia predicate; sorted copy giữ rocks input. → low===high là first false, return low-1..

Với **Đá bước**, chuỗi “đọc candidate gap; trong predicate là currentPosition. → hỏi currentPosition-previousKept < gap; removedCount > n. → đổi predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Đá bước | `binary low/high và predicate removalsNeeded(gap).` | State chung mà mọi current đọc hoặc mutate | `low=0 feasible; high=distance+1 infeasible sentinel.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `candidate gap; trong predicate là currentPosition.` | Current của đúng iteration/candidate hiện tại | Do `binary first-false; predicate scan positions trái sang phải.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `previousKept và removedCount.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.` | Contract output của Đá bước | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const positions = [...rocks].sort((a, b) => a - b);`; lựa chọn này giữ đúng thông tin mà check `currentPosition-previousKept < gap; removedCount > n.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `binary first-false; predicate scan positions trái sang phải.`. Đây là lý do code dùng block bắt đầu bởi `while (low < high) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “currentPosition-previousKept < gap; removedCount > n.”. Trong dry run `distance=25, rocks=[2,14,11,21,17], n=2`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (position - previous < minimumGap) {`.
- **Tiến triển:** position loop tăng; binary interval co.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `removed += 1;`.
- **Cleanup:** destination phải tham gia predicate; sorted copy giữ rocks input.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Đá bước:** Predicate phải đơn điệu và interval convention phải nhất quán. Nhánh true có giữ middle hay loại middle phụ thuộc việc tìm first/last feasible; không trộn closed với half-open boundary.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Đá bước | binary low/high và predicate removalsNeeded(gap). | `const positions = [...rocks].sort((a, b) => a - b);` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | binary first-false; predicate scan positions trái sang phải. | `while (low < high) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | currentPosition-previousKept < gap; removedCount > n. | `if (position - previous < minimumGap) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid. | `positions.push(distance);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | position loop tăng; binary interval co. | `removed += 1;` | Sau read/update theo invariant của Đá bước |
| Hoàn tất và trả contract output | destination phải tham gia predicate; sorted copy giữ rocks input. | `return low - 1;` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Đá bước** là **toàn bộ `maximumMinimumRockDistance` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Đá bước:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Đá bước:** Những dữ kiện nào trong `binary low/high và predicate removalsNeeded(gap).` phải còn nguyên khi current kế tiếp tới? Block `const positions = [...rocks].sort((a, b) => a - b);` chạy đúng một lần trước traversal. Nếu đặt nó trong `binary first-false; predicate scan positions trái sang phải.`, state nào sẽ bị reset?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
}
```

**Bước 3 — transition và movement**

> **Prediction checkpoint — transition của Đá bước:** Nhìn các brace đang mở để quyết định `positions.push(distance);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.`, và movement `position loop tăng; binary interval co.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
}
```

**Bước 4 — cleanup/return**

> **Prediction checkpoint — finalization của Đá bước:** Dòng `function feasible(minimumGap) {`, `let removed = 0;`, `let previous = 0;`, `for (const position of positions) {`, `if (position - previous < minimumGap) {` chạy per current hay đúng một lần sau khi `binary first-false; predicate scan positions trái sang phải.` kết thúc? Trước return, cleanup `destination phải tham gia predicate; sorted copy giữ rocks input.` và output `khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Đá bước:** Những dữ kiện nào trong `binary low/high và predicate removalsNeeded(gap).` phải còn nguyên khi current kế tiếp tới? Block `let low = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `binary first-false; predicate scan positions trái sang phải.`, state nào sẽ bị reset?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
}
```

**Bước 6 — state/initialization**

> **Prediction checkpoint — state của Đá bước:** Những dữ kiện nào trong `binary low/high và predicate removalsNeeded(gap).` phải còn nguyên khi current kế tiếp tới? Block `let high = distance + 1;` chạy đúng một lần trước traversal. Nếu đặt nó trong `binary first-false; predicate scan positions trái sang phải.`, state nào sẽ bị reset?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
}
```

**Bước 7 — main loop và scope**

> **Prediction checkpoint — loop của Đá bước:** Header `while (low < high) {` lặp per item, per candidate hay tới khi state rỗng? Current `candidate gap; trong predicate là currentPosition.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
  while (low < high) {
  }
}
```

**Bước 8 — condition/inner branch**

> **Prediction checkpoint — condition của Đá bước:** Trước khi chạy `if (feasible(middle)) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `currentPosition-previousKept < gap; removedCount > n.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
  while (low < high) {
    const middle = low + Math.floor((high - low) / 2);
    if (feasible(middle)) {
    }
  }
}
```

**Bước 9 — transition và movement**

> **Prediction checkpoint — transition của Đá bước:** Nhìn các brace đang mở để quyết định `if (feasible(middle)) {`, `low = middle + 1;`, `} else {`, `high = middle;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.`, và movement `position loop tăng; binary interval co.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
  while (low < high) {
    const middle = low + Math.floor((high - low) / 2);
    if (feasible(middle)) {
      low = middle + 1;
    } else {
      high = middle;
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Đá bước:** Nhìn các brace đang mở để quyết định `while (low < high) {`, `const middle = low + Math.floor((high - low) / 2);`, `if (feasible(middle)) {`, `low = middle + 1;`, `} else {` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.`, và movement `position loop tăng; binary interval co.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
  while (low < high) {
    const middle = low + Math.floor((high - low) / 2);
    if (feasible(middle)) {
      low = middle + 1;
    } else {
      high = middle;
    }
  }
}
```

**Bước 11 — cleanup/return**

> **Prediction checkpoint — finalization của Đá bước:** Dòng `return low - 1;` chạy per current hay đúng một lần sau khi `binary first-false; predicate scan positions trái sang phải.` kết thúc? Trước return, cleanup `destination phải tham gia predicate; sorted copy giữ rocks input.` và output `khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function maximumMinimumRockDistance(distance, rocks, removalsAllowed) {
  const positions = [...rocks].sort((a, b) => a - b);
  positions.push(distance);
  function feasible(minimumGap) {
    let removed = 0;
    let previous = 0;
    for (const position of positions) {
      if (position - previous < minimumGap) {
        removed += 1;
      } else {
        previous = position;
      }
    }
    return removed <= removalsAllowed;
  }
  let low = 0;
  let high = distance + 1;
  while (low < high) {
    const middle = low + Math.floor((high - low) / 2);
    if (feasible(middle)) {
      low = middle + 1;
    } else {
      high = middle;
    }
  }
  return low - 1;
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const positions = [...rocks].sort((a, b) => a - b);` | Trước traversal trong main function | binary low/high và predicate removalsNeeded(gap). phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Đá bước |
| `candidate gap; trong predicate là currentPosition.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (position - previous < minimumGap) {` | Sau khi state guard/current đã sẵn | Condition đọc previousKept và removedCount. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `positions.push(distance);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi currentPosition-previousKept < gap; removedCount > n. | Đưa ra ngoài áp dụng update cho cả case false |
| `removed += 1;` | position loop tăng; binary interval co. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: destination phải tham gia predicate; sorted copy giữ rocks input. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return low - 1;` | Sau finalization của main function | Output phải đạt khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Đá bước** dùng `distance=25, rocks=[2,14,11,21,17], n=2`: dừng ngay khi `binary low/high và predicate removalsNeeded(gap).` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `maximumMinimumRockDistance` với `distance=25, rocks=[2,14,11,21,17], n=2`; theo dõi đúng output `khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | low=0 feasible; high=distance+1 infeasible sentinel. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | binary low/high và predicate removalsNeeded(gap). | candidate gap; trong predicate là currentPosition. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | previousKept và removedCount. | predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid. | Đổi đúng như từng dòng dry run gốc | Condition currentPosition-previousKept < gap; removedCount > n. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | position loop tăng; binary interval co. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | destination phải tham gia predicate; sorted copy giữ rocks input. | low===high là first false, return low-1. | khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals. | Không còn transition bắt buộc nào chưa chạy |

Trong **Đá bước**, chỉ dòng thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.` được đổi output; state còn pending theo `binary low/high và predicate removalsNeeded(gap).` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Không append destination | `distance=25, rocks=[2,14,11,21,17], n=2` | Lượt đầu của `candidate gap; trong predicate là currentPosition.` mà `currentPosition-previousKept < gap; removedCount > n.` quyết định branch | Không append destination | `positions.push(distance);` ở đúng scope |
| khi remove vẫn update previous | `[2,11,14,17,21]` | Ngay transition đầu tiên thực hiện `predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.` trên boundary/duplicate của input | khi remove vẫn update previous | `positions.push(distance);` với init/strictness của Đá bước |
| convention binary lệch | `0 → 11 → 17 → 21 → 25` | Lượt đầu movement `position loop tăng; binary interval co.` xảy ra trước/sau sai assignment | convention binary lệch | `positions.push(distance);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Sort `O(R log R)`; predicate `O(R)` × `O(log distance)`; space `O(R)`.

**Proof từ operation của Đá bước:** Mỗi decision giảm miền ứng viên ít nhất gần một nửa, nên số lượt là log₂ của độ rộng ban đầu; chi phí tổng còn nhân với chi phí predicate của một lượt. Main loop được mô tả là `binary first-false; predicate scan positions trái sang phải.`; movement là `position loop tăng; binary interval co.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `binary low/high và predicate removalsNeeded(gap).`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Đá bước; test `distance=25, rocks=[2,14,11,21,17], n=2` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `binary low/high và predicate removalsNeeded(gap).`, condition `currentPosition-previousKept < gap; removedCount > n.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Đá bước. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với `distance=25`, rocks `[2,11,14,17,21]`, xóa tối đa 2 rock:

- target gap 4: xóa 2 và 14, giữ `0 → 11 → 17 → 21 → 25`; feasible;
- target gap 5: greedy buộc xóa ít nhất 3 rock; infeasible.

Vì predicate true tới 4 rồi false từ 5, last feasible là 4. Brute force đúng nghĩa có thể thử
mọi tập rock được giữ:

```js
function maximumMinimumRockDistanceBrute(distance, rocks, removalsAllowed) {
  let best = 0;

  function search(index, kept, removed) {
    if (removed > removalsAllowed) {
      return;
    }
    if (index === rocks.length) {
      const route = [0, ...kept, distance].sort((a, b) => a - b);
      let minimumGap = distance;
      for (let i = 1; i < route.length; i += 1) {
        minimumGap = Math.min(minimumGap, route[i] - route[i - 1]);
      }
      best = Math.max(best, minimumGap);
      return;
    }

    search(index + 1, [...kept, rocks[index]], removed);
    search(index + 1, kept, removed + 1);
  }

  search(0, [], 0);
  return best;
}
```

Brute force có `2^R` nhánh. Bản tối ưu thay việc chọn subset bằng greedy predicate `O(R)`, rồi
binary search trên `gap`.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo | Vì sao đúng |
| --- | --- | --- | --- |
| `positions` | rocks đã sort và destination | copy, sort, push `distance` | scan theo thứ tự và không mutate input |
| `previous` | điểm gần nhất được giữ | `0` | start luôn tồn tại |
| `removed` | số điểm đã phải xóa | `0` mỗi predicate | so với budget `removalsAllowed` |
| `low` | gap còn biết là feasible | `0` | gap 0 luôn đạt được |
| `high` | first-false candidate | `distance + 1` | không thể có gap lớn hơn toàn tuyến |

#### 21. Blueprint từ đề đến code

1. Nhận dạng cụm “maximize khoảng cách nhỏ nhất” và đặt candidate `minimumGap`.
2. Với candidate cố định, scan trái sang phải và xóa điểm có gap quá nhỏ.
3. Append destination để đoạn cuối tham gia cùng một transition.
4. Chứng minh greedy giữ điểm hợp lệ sớm nhất dùng ít removals nhất trên mỗi prefix.
5. Nhận ra predicate `true → false` khi gap tăng.
6. Binary search first false trong `[0, distance + 1]`, rồi trả `low - 1`.

#### 22. Chứng minh, lỗi lộ diện và recall

Khi `position - previous < minimumGap`, giữ cả hai là bất khả thi; xóa `position` giữ
`previous` sớm nhất cho điểm tiếp theo và không dùng nhiều removals hơn lựa chọn khác. Bởi vậy
greedy tính đúng số xóa tối thiểu. Nếu gap `g` feasible thì mọi gap nhỏ hơn cũng feasible, nên
binary search first false và lùi một đơn vị trả đúng last true.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| quên destination sentinel | `10, [2,3,7], 1` | 3 |
| khi xóa vẫn cập nhật `previous` | `25, [2,11,14,17,21], 2` | 4 |
| dùng `<`/`<=` sai ở predicate | `10, [5], 0` | 5 |
| không cho phép xóa toàn bộ rock | `10, [2,3,7], 3` | 10 |
| sort trực tiếp làm mutate input | rocks `[17,2,11]` | input giữ nguyên |

Recall: **maximize minimum ⇒ thử một gap; gap thiếu thì xóa current, đủ mới cập nhật previous;
append destination; tìm first false rồi trừ 1.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: khoảng cách nhỏ nhất lớn nhất có thể giữ sau tối đa removals.
PREPARE: sort rocks tăng và append destination làm sentinel cuối.
GLOBAL STATE: binary low/high và predicate removalsNeeded(gap).
INIT: low=0 feasible; high=distance+1 infeasible sentinel.
MAIN LOOP: binary first-false; predicate scan positions trái sang phải.
CURRENT ITEM: candidate gap; trong predicate là currentPosition.
PER-ITERATION STATE: previousKept và removedCount.
CHECK: currentPosition-previousKept < gap; removedCount > n.
BRANCH: gap thiếu thì remove current; đủ thì commit previousKept=current.
UPDATE: predicate tăng removed hoặc cập nhật previous; binary true low=mid+1, false high=mid.
POINTER MOVEMENT: position loop tăng; binary interval co.
STOP / RETURN: low===high là first false, return low-1.
CLEANUP: destination phải tham gia predicate; sorted copy giữ rocks input.
```

- Recall 1: Với gap cố định giữ điểm hợp lệ sớm nhất; binary tìm gap cuối cần không quá n removals.
- Recall 2: STATE predicate+low/high → LOOP positions/binary → CHECK gap/removals → UPDATE → RETURN low-1.
- Recall 3: Điền thiếu gap thì `removedCount += ___`; feasible mid làm `low = middle + ___`.
<!-- dry-run-reconstruction:end -->
