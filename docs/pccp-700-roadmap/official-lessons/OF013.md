# OF013 — Bộ điều khiển đĩa

- Nguồn đề: [Programmers lesson 42627](https://school.programmers.co.kr/learn/courses/30/lessons/42627?language=javascript)
- Pattern chính: event sweep + shortest-job min-heap
- Mức trong bank: CORE / integration

Tài liệu diễn giải lại contract để học; trang Programmers ở trên là nguồn đề chuẩn.

## 01. Contract

Mỗi job `[request,duration]`. Disk xử lý một job tại một thời điểm, không preempt. Khi rảnh, chọn trong các job đã request theo duration ngắn nhất. Tính floor của average turnaround `completion-request`.

## 02. Bound

Mỗi lần scan toàn jobs chờ để tìm shortest thành `O(n²)`. Sort request một lần và min-heap waiting theo duration cho `O(n log n)`.

## 03. Brute force

Mỗi tick thêm job mới, scan list waiting tìm duration min, xóa nó, rồi tăng time từng đơn vị. Lặp scan và lãng phí idle ticks.

## 04. Bottleneck

Có hai order khác nhau: arrivals theo request time và selection theo duration. Một cấu trúc không phục vụ tốt cả hai; dùng sorted input + heap. Khi heap rỗng phải jump tới request kế, không tick qua khoảng trống.

## 05. Pattern

```text
if waiting empty: time=max(time,next request)
push all request<=time
pop shortest duration
time += duration
turnaround += time-request
```

## 06. State

- jobs sort request và pointer `next`;
- min-heap waiting theo `(duration,request)`;
- `time`, `completed`, `totalTurnaround`.

## 07. Transition

Jump idle; batch enqueue mọi arrival tính đến current time; pop một job, advance time đến completion, cộng turnaround.

## 08. Invariant

Trước selection, heap chứa đúng mọi job request `<=time` chưa xử lý. Pointer trỏ arrival đầu chưa enqueue. Heap root là job duration nhỏ nhất hợp lệ. Sau execution, time là completion của job vừa chọn.

## 09. Complexity

Sort `O(n log n)`, mỗi job heap push/pop một lần `O(n log n)`, space `O(n)`.

## 10. Pseudocode

```text
sort by request
while completed<n:
  if heap empty and next request>time: jump time
  enqueue all request<=time
  job=pop shortest
  time+=duration
  total+=time-request
return floor(total/n)
```

## 11. Code bước 1 — dựng state

```js
function orderedJobs(jobs) {
  return jobs.map(([request,duration])=>({request,duration}))
    .sort((a,b)=>a.request-b.request);
}
```

## 12. Code bước 2 — viết transition

```js
function turnaround(completion,request) {
  return completion-request;
}
```

## 13. Code bước 3 — ghép vòng lặp

```js
function jumpIfIdle(time,nextRequest,heapSize) {
  return heapSize===0 && time<nextRequest ? nextRequest : time;
}
```

## 14. Code hoàn chỉnh

Code executable gồm heap: [solutions/official/OF013.js](../../../solutions/official/OF013.js).

```js
class JobMinHeap {
  constructor() {
    this.values = [];
  }
  get size() {
    return this.values.length;
  }
  compare(first, second) {
    return first.duration - second.duration || first.request - second.request;
  }
  push(job) {
    const heap = this.values;
    heap.push(job);
    let child = heap.length - 1;
    while (child > 0) {
      const parent = Math.floor((child - 1) / 2);
      if (this.compare(heap[parent], heap[child]) <= 0) {
        break;
      }
      [heap[parent], heap[child]] = [heap[child], heap[parent]];
      child = parent;
    }
  }
  pop() {
    const heap = this.values;
    if (heap.length === 1) {
      return heap.pop();
    }
    const minimum = heap[0];
    heap[0] = heap.pop();
    let parent = 0;
    while (true) {
      const left = parent * 2 + 1;
      const right = left + 1;
      let smallest = parent;
      if (left < heap.length && this.compare(heap[left], heap[smallest]) < 0) {
        smallest = left;
      }
      if (right < heap.length && this.compare(heap[right], heap[smallest]) < 0) {
        smallest = right;
      }
      if (smallest === parent) {
        break;
      }
      [heap[parent], heap[smallest]] = [heap[smallest], heap[parent]];
      parent = smallest;
    }
    return minimum;
  }
}

function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;

  while (completed < ordered.length) {
    if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {
      time = ordered[next].request;
    }

    while (next < ordered.length && ordered[next].request <= time) {
      waiting.push(ordered[next]);
      next += 1;
    }

    const job = waiting.pop();
    time += job.duration;
    totalTurnaround += time - job.request;
    completed += 1;
  }

  return Math.floor(totalTurnaround / jobs.length);
}
```

## 15. Dry-run

`[0,3],[1,9],[2,6]`: time0 chọn 3 → complete3/turn3; lúc3 heap có duration9,6 nên chọn6 → complete9/turn7; rồi9 → complete18/turn17; floor `(3+7+17)/3=9`.

## 16. Edge cases

1. Job đầu request sau time 0.
2. Nhiều job cùng request.
3. Heap rỗng giữa hai arrival group.
4. Tie duration; request tie-break giúp deterministic.

## 17. Lỗi JavaScript thường gặp

- Heap sort theo request thay duration.
- Chỉ enqueue một arrival, bỏ các job cùng thời điểm.
- Tăng time từng tick khi idle.
- Turnaround dùng duration thay `completion-request`.
- Dùng `shift()` sorted jobs.

## 18. Bài transfer cùng pattern

- OF012: heap pure combine, không có event time.
- OF010: event queue theo exit time, không priority selection.
- SR005: preemption và resume LIFO.

Tín hiệu: arrivals theo time nhưng chọn waiting theo tiêu chí khác ⇒ sort event + priority queue.

<!-- dry-run-reconstruction:start -->
## Dry run → Code reconstruction — OF013 Bộ điều khiển đĩa

Phần này dùng chính contract, dry run, Blueprint và code executable của **Bộ điều khiển đĩa** để dựng lại lời giải; pattern kiểm soát là `Heap-event-simulation`. Các section 01–18 phía trên vẫn là theory/reference, còn pipeline dưới đây tập trung vào việc tự đặt state, loop, brace và update order.

### Problem contract

Mỗi job `[request,duration]`. Disk xử lý một job tại một thời điểm, không preempt. Khi rảnh, chọn trong các job đã request theo duration ngắn nhất. Tính floor của average turnaround `completion-request`.

**Giới hạn quyết định implementation:** Mỗi lần scan toàn jobs chờ để tìm shortest thành `O(n²)`. Sort request một lần và min-heap waiting theo duration cho `O(n log n)`.

### Concrete dry run

**Concrete input:** `[0,3],[1,9],[2,6]`

`[0,3],[1,9],[2,6]`: time0 chọn 3 → complete3/turn3; lúc3 heap có duration9,6 nên chọn6 → complete9/turn7; rồi9 → complete18/turn17; floor `(3+7+17)/3=9`.



Decision ledger của **Bộ điều khiển đĩa** nối `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` với đúng block code; mỗi inner action trong input `[0,3],[1,9],[2,6]` là một lượt riêng.

| Iteration/current item | State before | Exact condition with real dry-run values | Action / pointer movement | State after | Answer/output change |
| --- | --- | --- | --- | --- | --- |
| Khởi tạo cho `[0,3],[1,9],[2,6]` | Chưa có state của Bộ điều khiển đĩa | Contract và input đã hợp lệ | pointer/completed/total = 0; time = 0; heap rỗng. | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. | Chưa chốt output |
| job vừa enqueue hoặc shortestJob vừa pop. đi vào | nextRequest và completionTime của job được chọn. | Với `[0,3],[1,9],[2,6]`, hỏi: request <= currentTime; heap rỗng; còn job tương lai không. | heap rỗng thì jump time; có ready job thì pop và chạy tới completion.; nextJobIndex tăng sau enqueue; heap pop chọn job kế. | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. sau branch vừa chọn | floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến. chỉ đổi ở branch ghi answer |
| Transition của current | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. trước mutation trong dòng dry run tương ứng | Condition ở dòng concrete phía trên vừa true/false đúng như giá trị đã ghi | tăng time bằng duration; cộng time-request; tăng completed. | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. đã commit current; invariant của Bộ điều khiển đĩa được phục hồi | floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến. sau đúng assignment của dòng đó |
| Kết thúc traversal | Không còn current/candidate cần xét | Điều kiện stop của loop không còn đúng | không tick idle; jump không tạo turnaround giả.; hoàn tất mọi job thì return floor(total/jobs.length). | Không còn state pending ngoài contract | Return output hoàn tất |

### Vietnamese action narrative

> Trong **Bộ điều khiển đĩa**, phần tử có priority cao nhất ở heap top đi vào → đọc `nextRequest và completionTime của job được chọn.` từ state đang sống → hỏi đúng câu `request <= currentTime; heap rỗng; còn job tương lai không.` → đi nhánh `heap rỗng thì jump time; có ready job thì pop và chạy tới completion.` → thực hiện `tăng time bằng duration; cộng time-request; tăng completed.` → push candidate khi nó trở nên khả dụng và pop đúng top khi quyết định → chỉ chốt `floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến.` ở vị trí mà invariant đã đúng → không tick idle; jump không tạo turnaround giả. → hoàn tất mọi job thì return floor(total/jobs.length)..

Với **Bộ điều khiển đĩa**, chuỗi “đọc job vừa enqueue hoặc shortestJob vừa pop. → hỏi request <= currentTime; heap rỗng; còn job tương lai không. → đổi tăng time bằng duration; cộng time-request; tăng completed. → di chuyển” là nguồn trực tiếp của từng dòng code; không thêm mutation ngoài chuỗi này.

### State derivation

| Information that must survive | Variable/state | Exact meaning | Initial value | Why |
| --- | --- | --- | --- | --- |
| Thông tin toàn cục còn cần cho lượt sau của Bộ điều khiển đĩa | `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` | State chung mà mọi current đọc hoặc mutate | `pointer/completed/total = 0; time = 0; heap rỗng.` | Trước lượt đầu chưa có transition nào được commit |
| Đối tượng đang được xét | `job vừa enqueue hoặc shortestJob vừa pop.` | Current của đúng iteration/candidate hiện tại | Do `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.` cung cấp | Không cần giữ sau khi transition của current hoàn tất |
| Dữ kiện tạm để chọn branch | `nextRequest và completionTime của job được chọn.` | Giá trị chỉ đúng cho current đang xét | Tính sau khi current tồn tại | Tính sớm hơn sẽ dùng index/state cũ |
| Kết quả phải sống tới return | `floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến.` | Contract output của Bộ điều khiển đĩa | Theo init và sentinel trong code | Cho phép answer update đúng tie/default của bài |

Representation không được đổi tùy tiện: code bắt đầu state bằng `const ordered = jobs`; lựa chọn này giữ đúng thông tin mà check `request <= currentTime; heap rỗng; còn job tương lai không.` cần ở lượt sau.

### Loop and condition derivation

- **Nhịp lặp:** `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`. Đây là lý do code dùng block bắt đầu bởi `while (completed < ordered.length) {`, thay vì chọn loop theo thói quen.
- **Câu hỏi trước branch:** “request <= currentTime; heap rỗng; còn job tương lai không.”. Trong dry run `[0,3],[1,9],[2,6]`, đối chiếu dòng làm condition true và dòng làm condition false; code tương ứng bắt đầu tại `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {`.
- **Tiến triển:** nextJobIndex tăng sau enqueue; heap pop chọn job kế.. Nếu state/pointer không đổi, loop có thể xét lại cùng candidate hoặc giữ representation lệch current.
- **Thứ tự:** phải hoàn thành `tăng time bằng duration; cộng time-request; tăng completed.` ở scope sở hữu current rồi mới chuyển nhịp. Dòng movement chính là `const job = waiting.pop();`.
- **Cleanup:** không tick idle; jump không tạo turnaround giả.. Nếu là “không cần”, state đã hoàn tất ngay trong main loop; nếu có, block chỉ chạy sau khi traversal đã cung cấp đủ thông tin.

**Kiểm tra theo family cho Bộ điều khiển đĩa:** Condition phải hỏi đúng ý nghĩa heap top sau comparator và sau khi stale entry (nếu có) đã được dọn. Equal priority tiếp tục theo tie-break ghi trong contract, không theo thứ tự tình cờ của array heap.

### Vietnamese logic → code

| Vietnamese action from the dry run | Information required | Exact code | Why here |
| --- | --- | --- | --- |
| Tạo state sống qua các lượt của Bộ điều khiển đĩa | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. | `const ordered = jobs` | Trước main traversal để không reset lịch sử |
| Mở nhịp xét current/candidate | lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job. | `while (completed < ordered.length) {` | Block sở hữu current và mọi branch của nó |
| Hỏi câu quyết định | request <= currentTime; heap rỗng; còn job tương lai không. | `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {` | Chỉ đọc sau khi guard/state cần thiết đã tồn tại |
| Commit transition của bài | tăng time bằng duration; cộng time-request; tăng completed. | `waiting.push(ordered[next]);` | Ở đúng branch/inner iteration đã chứng minh mutation hợp lệ |
| Tiến sang state/candidate kế | nextJobIndex tăng sau enqueue; heap pop chọn job kế. | `const job = waiting.pop();` | Sau read/update theo invariant của Bộ điều khiển đĩa |
| Hoàn tất và trả contract output | không tick idle; jump không tạo turnaround giả. | `return Math.floor(totalTurnaround / jobs.length);` | Sau mọi finalization bắt buộc, không return non-final state |

### Cumulative code construction

Mỗi bước của **Bộ điều khiển đĩa** là **toàn bộ `averageDiskTurnaround` đã dựng tới lúc đó**; brace đang thấy chính là scope thật. Helper của lời giải (nếu có) chỉ được gọi sau khi state mà nó cần đã tồn tại.

**Bước 1 — function contract/shell**

> **Prediction checkpoint — Bộ điều khiển đĩa:** Function này bao toàn bộ lời giải, không chạy per item. Input phải đúng contract ở trên. Nếu đóng brace rồi đặt state bên ngoài, state không còn thuộc lời giải.

```js
function averageDiskTurnaround(jobs) {
}
```

**Bước 2 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `const ordered = jobs`, `.map(([request, duration]) => ({ request, duration }))`, `.sort((a, b) => a.request - b.request);` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
}
```

**Bước 3 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `const waiting = new JobMinHeap();` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
}
```

**Bước 4 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `let next = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
}
```

**Bước 5 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `let completed = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
}
```

**Bước 6 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `let time = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
}
```

**Bước 7 — state/initialization**

> **Prediction checkpoint — state của Bộ điều khiển đĩa:** Những dữ kiện nào trong `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải còn nguyên khi current kế tiếp tới? Block `let totalTurnaround = 0;` chạy đúng một lần trước traversal. Nếu đặt nó trong `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`, state nào sẽ bị reset?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
}
```

**Bước 8 — main loop và scope**

> **Prediction checkpoint — loop của Bộ điều khiển đĩa:** Header `while (completed < ordered.length) {` lặp per item, per candidate hay tới khi state rỗng? Current `job vừa enqueue hoặc shortestJob vừa pop.` chỉ tồn tại sau khi loop mở. Hãy chỉ ra brace đóng loop trước khi xem bước kế tiếp; đặt return/cleanup bên trong brace đó sẽ bỏ phần input nào?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
  while (completed < ordered.length) {
  }
}
```

**Bước 9 — condition/inner branch**

> **Prediction checkpoint — condition của Bộ điều khiển đĩa:** Trước khi chạy `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {`, khoanh từng identifier mà condition đọc và tìm dòng đã gán nó trong function tích lũy đang thấy. Với input dry run, current nào làm câu `request <= currentTime; heap rỗng; còn job tương lai không.` true hoặc false? Đưa branch ra khỏi loop sẽ làm identifier current nào hết scope?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
  while (completed < ordered.length) {
    if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {
    }
  }
}
```

**Bước 10 — transition và movement**

> **Prediction checkpoint — transition của Bộ điều khiển đĩa:** Nhìn các brace đang mở để quyết định `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {`, `time = ordered[next].request;` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `tăng time bằng duration; cộng time-request; tăng completed.`, và movement `nextJobIndex tăng sau enqueue; heap pop chọn job kế.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
  while (completed < ordered.length) {
    if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {
      time = ordered[next].request;
    }
  }
}
```

**Bước 11 — transition và movement**

> **Prediction checkpoint — transition của Bộ điều khiển đĩa:** Nhìn các brace đang mở để quyết định `while (completed < ordered.length) {`, `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {`, `time = ordered[next].request;`, `while (next < ordered.length && ordered[next].request <= time) {`, `waiting.push(ordered[next]);` chạy lúc setup, per current hay sau traversal. Dòng nào trong block thật sự thực hiện `tăng time bằng duration; cộng time-request; tăng completed.`, và movement `nextJobIndex tăng sau enqueue; heap pop chọn job kế.` đã xảy ra hay chưa? Nếu chuyển block qua brace sở hữu current, dữ kiện nào sẽ bị reset hoặc hết scope?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
  while (completed < ordered.length) {
    if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {
      time = ordered[next].request;
    }

    while (next < ordered.length && ordered[next].request <= time) {
      waiting.push(ordered[next]);
      next += 1;
    }

    const job = waiting.pop();
    time += job.duration;
    totalTurnaround += time - job.request;
    completed += 1;
  }
}
```

**Bước 12 — cleanup/return**

> **Prediction checkpoint — finalization của Bộ điều khiển đĩa:** Dòng `return Math.floor(totalTurnaround / jobs.length);` chạy per current hay đúng một lần sau khi `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.` kết thúc? Trước return, cleanup `không tick idle; jump không tạo turnaround giả.` và output `floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến.` phải ở trạng thái nào? Return sớm sẽ bỏ state pending nào?

```js
function averageDiskTurnaround(jobs) {
  const ordered = jobs
    .map(([request, duration]) => ({ request, duration }))
    .sort((a, b) => a.request - b.request);
  const waiting = new JobMinHeap();
  let next = 0;
  let completed = 0;
  let time = 0;
  let totalTurnaround = 0;
  while (completed < ordered.length) {
    if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {
      time = ordered[next].request;
    }

    while (next < ordered.length && ordered[next].request <= time) {
      waiting.push(ordered[next]);
      next += 1;
    }

    const job = waiting.pop();
    time += job.duration;
    totalTurnaround += time - job.request;
    completed += 1;
  }
  return Math.floor(totalTurnaround / jobs.length);
}
```

### Block scope and placement

| Block | Correct scope/location | Why | What breaks if moved |
| --- | --- | --- | --- |
| `const ordered = jobs` | Trước traversal trong main function | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. phải sống qua các lượt | Đặt trong loop làm reset lịch sử của Bộ điều khiển đĩa |
| `job vừa enqueue hoặc shortestJob vừa pop.` | Bên trong iteration/candidate hiện tại | Current phụ thuộc loop hiện tại | Đặt ngoài dùng current cũ hoặc chưa có index |
| `if (waiting.size === 0 && next < ordered.length && time < ordered[next].request) {` | Sau khi state guard/current đã sẵn | Condition đọc nextRequest và completionTime của job được chọn. | Check sớm đọc state thiếu; check muộn có thể mutate nhầm branch |
| `waiting.push(ordered[next]);` | Trong branch/inner loop đã chọn | Mutation chỉ hợp lệ khi request <= currentTime; heap rỗng; còn job tương lai không. | Đưa ra ngoài áp dụng update cho cả case false |
| `const job = waiting.pop();` | nextJobIndex tăng sau enqueue; heap pop chọn job kế. | Bảo đảm progress và đồng bộ representation | Move trước read/update bỏ current; không move có thể lặp vô hạn |
| Cleanup: không tick idle; jump không tạo turnaround giả. | Sau main traversal nếu contract yêu cầu | Chỉ lúc đó mới biết state nào còn pending | Đặt trong main loop kết luận sớm hoặc xóa state cần cho current tương lai |
| `return Math.floor(totalTurnaround / jobs.length);` | Sau finalization của main function | Output phải đạt floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến. | Return sớm bỏ candidate/state chưa xử lý |

Scope checkpoint của **Bộ điều khiển đĩa** dùng `[0,3],[1,9],[2,6]`: dừng ngay khi `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` lệch invariant, ghi state trước/sau block bị chuyển và không chờ tới output cuối.



### Full-code dry run

Chạy toàn bộ `averageDiskTurnaround` với `[0,3],[1,9],[2,6]`; theo dõi đúng output `floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến.` qua các block sau:

| Code block reached | Pending state before | Mutation / assignment now | Intermediate output | Why assignment is valid now |
| --- | --- | --- | --- | --- |
| State initialization | Chưa có state | pointer/completed/total = 0; time = 0; heap rỗng. | Output ở default/sentinel | Chưa có current nào đủ dữ kiện để chốt |
| Main loop lấy current | sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed. | job vừa enqueue hoặc shortestJob vừa pop. | Chưa tự động đổi answer | Current mới chỉ được đọc |
| Check và branch | nextRequest và completionTime của job được chọn. | tăng time bằng duration; cộng time-request; tăng completed. | Đổi đúng như từng dòng dry run gốc | Condition request <= currentTime; heap rỗng; còn job tương lai không. vừa được xác nhận |
| Pointer/frontier/index movement | State của current đã commit | nextJobIndex tăng sau enqueue; heap pop chọn job kế. | Giữ output vừa chốt | Không đọc lại cùng current với state cũ |
| Cleanup/return | không tick idle; jump không tạo turnaround giả. | hoàn tất mọi job thì return floor(total/jobs.length). | floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến. | Không còn transition bắt buộc nào chưa chạy |

Trong **Bộ điều khiển đĩa**, chỉ dòng thực hiện `tăng time bằng duration; cộng time-request; tăng completed.` được đổi output; state còn pending theo `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.` phải sống tới branch hoặc cleanup sở hữu nó.

### Realistic wrong implementations

| Wrong implementation | Small failing input | First wrong iteration | Root cause | Correct block |
| --- | --- | --- | --- | --- |
| Heap sort theo request thay duration. | `[0,3],[1,9],[2,6]` | Lượt đầu của `job vừa enqueue hoặc shortestJob vừa pop.` mà `request <= currentTime; heap rỗng; còn job tương lai không.` quyết định branch | Heap sort theo request thay duration. | `waiting.push(ordered[next]);` ở đúng scope |
| Chỉ enqueue một arrival, bỏ các job cùng thời điểm. | `3,7,17` | Ngay transition đầu tiên thực hiện `tăng time bằng duration; cộng time-request; tăng completed.` trên boundary/duplicate của input | Chỉ enqueue một arrival, bỏ các job cùng thời điểm. | `waiting.push(ordered[next]);` với init/strictness của Bộ điều khiển đĩa |
| Tăng time từng tick khi idle. | `(duration,request)` | Lượt đầu movement `nextJobIndex tăng sau enqueue; heap pop chọn job kế.` xảy ra trước/sau sai assignment | Tăng time từng tick khi idle. | `waiting.push(ordered[next]);` trước/sau movement đúng như mapping |

### Complexity proof from movements

Sort `O(n log n)`, mỗi job heap push/pop một lần `O(n log n)`, space `O(n)`.

**Proof từ operation của Bộ điều khiển đĩa:** Mỗi item vào heap hữu hạn lần và rời hữu hạn lần; mỗi push/pop tốn O(log n). Cộng thêm bước sort event nếu bài cần sắp arrival. Main loop được mô tả là `lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.`; movement là `nextJobIndex tăng sau enqueue; heap pop chọn job kế.`. Vì vậy phải cộng chi phí các phase nối tiếp và chỉ nhân khi một phase thật sự chạy trọn bên trong mỗi lượt ngoài. Auxiliary space chính là state `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.`, không tính output nếu convention của bài tách output.

### Reconstruction exercises

1. **Level 1 — Mapping support:** Che section 14 và toàn bộ cumulative code. Dùng bảng Vietnamese logic → code để dựng lại Bộ điều khiển đĩa; test `[0,3],[1,9],[2,6]` và test boundary ở section 16. Không mở code cho tới khi tự đặt xong brace.
2. **Level 2 — Action support:** Che cả mapping. Chỉ dùng action narrative và concrete dry run; tự suy ra `sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.`, condition `request <= currentTime; heap rỗng; còn job tương lai không.`, update order và vị trí movement. Thêm một input làm branch ngược với dry run.
3. **Level 3 — Exam simulation:** Chỉ xem Problem contract và bound của Bộ điều khiển đĩa. Trong một trang trắng, viết state meaning/init, loop ownership, strictness, transition order, movement proof, complexity và complete function. Chạy ít nhất ba test: normal branch, edge/boundary, và revealing test từ bảng lỗi; không xem đáp án trong lúc dựng.

### Additional preserved problem-specific drills and notes

#### 19. Làm bằng tay và brute force executable

Với jobs `[0,3],[1,9],[2,6]`: tại time 0 chỉ job duration 3 eligible; hoàn tất ở 3. Lúc đó
hai job còn lại đều đã tới, nên chọn duration 6 trước 9. Turnaround là `3,7,17`, average floor 9.

```js
function averageDiskTurnaroundBrute(jobs) {
  const remaining = jobs.map(([request, duration]) => ({ request, duration }));
  let time = 0;
  let total = 0;

  while (remaining.length > 0) {
    const eligible = remaining
      .map((job, index) => ({ ...job, index }))
      .filter((job) => job.request <= time)
      .sort((a, b) => a.duration - b.duration || a.request - b.request);

    if (eligible.length === 0) {
      time = Math.min(...remaining.map((job) => job.request));
      continue;
    }

    const job = eligible[0];
    remaining.splice(job.index, 1);
    time += job.duration;
    total += time - job.request;
  }
  return Math.floor(total / jobs.length);
}
```

Brute scan/sort toàn waiting set mỗi lần. Bản tối ưu tách arrival order khỏi selection order.

#### 20. Bảng state và khởi tạo

| State | Ý nghĩa | Khởi tạo |
| --- | --- | --- |
| `ordered` | jobs theo request time | copy + sort input |
| `next` | arrival đầu chưa enqueue | `0` |
| `waiting` | jobs eligible theo `(duration,request)` | heap rỗng |
| `time` | thời điểm disk rảnh kế tiếp | `0` |
| `completed` | số job đã chạy xong | `0` |
| `totalTurnaround` | tổng `completion-request` | `0` |

#### 21. Blueprint từ đề đến code

1. Tách hai order: job trở nên eligible theo request, job được chọn theo duration.
2. Sort input một lần theo request và giữ pointer `next`.
3. Trước mỗi selection, enqueue **tất cả** job có `request <= time`.
4. Heap rỗng thì jump time tới arrival kế, không tick từng đơn vị.
5. Pop shortest eligible job, advance tới completion, cộng turnaround.
6. Lặp tới khi completed bằng số job; floor average ở cuối.

#### 22. Invariant, lỗi lộ diện và recall

Ngay trước mỗi pop, heap chứa đúng mọi job đã request nhưng chưa chạy; job sau `next` chưa eligible.
Vì vậy root là shortest trong đúng tập được phép chọn. Sau khi chạy non-preemptive, `time` là thời
điểm disk rảnh mới và batch arrival tiếp theo được đưa vào trước quyết định kế.

| Sai lầm | Test làm lộ | Kết quả đúng |
| --- | --- | ---: |
| đưa job tương lai vào heap | `[0,10],[1,1]` | job 10 phải chạy trước |
| chỉ enqueue một arrival | ba job cùng request 0 | chọn duration tăng dần |
| không jump khi idle | `[[5,2]]` | 2 |
| cộng waiting time thay turnaround | `[0,3],[1,9],[2,6]` | 9 |

Recall: **event order và priority order khác nhau ⇒ sorted pointer + heap; enqueue toàn eligible rồi mới pop.**

#### A17. Blueprint đủ trường và Recall ba tầng

```text
OUTPUT: floor của turnaround trung bình khi luôn chọn job ngắn nhất đã đến.
PREPARE: clone/sort jobs theo request; dùng min-heap theo duration rồi request.
GLOBAL STATE: sortedJobs, nextJobIndex, readyHeap, currentTime, totalTurnaround, completed.
INIT: pointer/completed/total = 0; time = 0; heap rỗng.
MAIN LOOP: lặp đến completed === jobs.length; enqueue eligible rồi xử lý một job.
CURRENT ITEM: job vừa enqueue hoặc shortestJob vừa pop.
PER-ITERATION STATE: nextRequest và completionTime của job được chọn.
CHECK: request <= currentTime; heap rỗng; còn job tương lai không.
BRANCH: heap rỗng thì jump time; có ready job thì pop và chạy tới completion.
UPDATE: tăng time bằng duration; cộng time-request; tăng completed.
POINTER MOVEMENT: nextJobIndex tăng sau enqueue; heap pop chọn job kế.
STOP / RETURN: hoàn tất mọi job thì return floor(total/jobs.length).
CLEANUP: không tick idle; jump không tạo turnaround giả.
```

- Recall 1: Sort theo arrival, enqueue tất cả job đã đến, heap chọn duration nhỏ nhất; rỗng thì nhảy thời gian.
- Recall 2: STATE pointer/heap/time/total → LOOP completed → CHECK eligible/empty → UPDATE run → RETURN floor average.
- Recall 3: Điền `request <= ___`; sau pop `currentTime += ___`, `total += currentTime - ___`.
<!-- dry-run-reconstruction:end -->
